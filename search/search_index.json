{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Provena","text":"<p>Context governance for agentic AI systems.</p> <p>Your AI agent just made a decision based on data from 6 different sources. Can you tell me which ones? Can you prove the data wasn't tampered with? Can you verify it was still current?</p> <p>Provena adds tamper-evident audit trails to any AI agent's context pipeline \u2014 in 3 lines of Python.</p> <pre><code>from provena import ContextTrail\n\ntrail = ContextTrail()\n\n@trail.track(source=\"retriever\")\ndef search(query):\n    return retriever.search(query)\n</code></pre> <p>Every call to <code>search()</code> is now logged with a SHA-256 content hash, provenance validation, and a hash-chained audit trail that detects tampering.</p>"},{"location":"#why-provena","title":"Why Provena?","text":"<p>AGT governs what agents DO. Guardrails AI governs what agents SAY. Provena governs what agents KNOW.</p> <p>No existing tool governs the context input layer. Provena fills this gap with:</p> <ul> <li>Tamper-evident audit trails \u2014 SHA-256 hash-chained (Merkle-style) logging with optional HMAC signing</li> <li>Provenance validation \u2014 Verify that context carries proper source metadata (VALID / MISSING / INCOMPLETE)</li> <li>Freshness checking \u2014 Detect stale context via metadata timestamps and regex temporal detection (FRESH / STALE / UNKNOWN)</li> <li>Any context source \u2014 RAG retrievers, tool outputs, agent messages, memory recalls, MCP resources</li> <li>Sub-1ms overhead \u2014 Pure Python, no ML models, no downloads</li> <li>Zero dependencies \u2014 Core library uses only the Python standard library</li> </ul>"},{"location":"#install","title":"Install","text":"<pre><code>pip install provena              # core (zero dependencies)\npip install provena[cli]         # + CLI tools (click, rich)\npip install provena[otel]        # + OpenTelemetry export\npip install provena[langchain]   # + LangChain adapter\npip install provena[llamaindex]  # + LlamaIndex adapter\npip install provena[all]         # everything\n</code></pre>"},{"location":"#architecture","title":"Architecture","text":"<pre><code>Your Application\n|\n|  Retriever ---+\n|  Tool Call ---+\n|  Agent Msg ---+---&gt; ContextTrail ---&gt; LLM Context Window\n|  Memory    ---+        |\n|  MCP       ---+        |\n|                  +-----+------------------+\n|                  | ProvenanceValidator     |\n|                  | FreshnessChecker        |\n|                  | HashChain (SHA-256)     |\n|                  | SQLite Backend          |\n|                  | OTel Exporter           |\n|                  +------------------------+\n</code></pre>"},{"location":"#compliance","title":"Compliance","text":"<p>Provena maps directly to EU AI Act requirements:</p> Article Requirement Provena Feature Art. 10 Data lineage Provenance validation for every context input Art. 12 Tamper-evident logging SHA-256 hash-chained audit trail with HMAC signing Art. 13 Transparency <code>trail.summary()</code> and source tracking Art. 14 Human oversight <code>trail.annotate()</code> for reviewer decisions <p>Also addresses OWASP ASI06 (Memory &amp; Context Poisoning).</p>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Getting Started \u2014 Install, first trail, verify chain in 5 minutes</li> <li>Guide \u2014 Deep-dive into tracking, provenance, freshness, and verification</li> <li>Integrations \u2014 LangChain, LlamaIndex, OpenTelemetry, CLI</li> <li>API Reference \u2014 Auto-generated from docstrings</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Get context governance running in under 5 minutes.</p>"},{"location":"getting-started/#install","title":"Install","text":"<pre><code>pip install provena\n</code></pre> <p>Zero dependencies. Python 3.10+.</p>"},{"location":"getting-started/#step-1-create-an-audit-trail","title":"Step 1: Create an Audit Trail","text":"<pre><code>from provena import ContextTrail\n\ntrail = ContextTrail(backend=\"memory\")\n\nrecord = trail.log(\n    content=\"Kubernetes supports rolling updates for Deployments.\",\n    source=\"retriever\",\n    source_name=\"k8s_docs\",\n)\n\nprint(f\"Logged: {record.entry.source_name}\")\nprint(f\"Hash:   {record.entry.content_hash[:16]}...\")\nprint(f\"Provenance: {record.provenance_result.status}\")\n</code></pre> <p>Output:</p> <pre><code>Logged: k8s_docs\nHash:   a1b2c3d4e5f6...\nProvenance: MISSING\n</code></pre> <p>Provenance is <code>MISSING</code> because we didn't attach any source metadata. Let's fix that.</p>"},{"location":"getting-started/#step-2-track-a-rag-pipeline","title":"Step 2: Track a RAG Pipeline","text":"<p>The <code>@trail.track()</code> decorator automatically logs function return values:</p> <pre><code>trail = ContextTrail(backend=\"memory\")\n\n@trail.track(source=\"retriever\")\ndef search(query):\n    return [\n        \"Result 1: Pod scheduling uses the kube-scheduler.\",\n        \"Result 2: Services provide stable networking for Pods.\",\n    ]\n\nresults = search(\"kubernetes networking\")\n# Both results are now logged as separate audit records\n</code></pre> <p>Each list item gets its own audit record with a content hash and chain link.</p>"},{"location":"getting-started/#step-3-add-provenance-metadata","title":"Step 3: Add Provenance Metadata","text":"<p>Attach origin metadata to make provenance validation pass:</p> <pre><code>from provena import ContextTrail, ProvenanceMetadata\nfrom datetime import datetime, timezone\n\ntrail = ContextTrail(backend=\"memory\", max_age_days=90)\n\nrecord = trail.log(\n    content=\"The enterprise plan costs $499/month.\",\n    source=\"tool:pricing_api\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://api.example.com/pricing\",\n        created_at=datetime.now(timezone.utc),\n    ),\n)\n\nprint(f\"Provenance: {record.provenance_result.status}\")  # VALID\nprint(f\"Freshness:  {record.freshness_result.status}\")   # FRESH\n</code></pre>"},{"location":"getting-started/#step-4-detect-stale-content","title":"Step 4: Detect Stale Content","text":"<p>Provena detects stale content in two ways: metadata timestamps and regex temporal markers in the text itself.</p> <pre><code>from datetime import datetime, timezone, timedelta\n\ntrail = ContextTrail(backend=\"memory\", max_age_days=90)\n\n# Stale via metadata\nrecord = trail.log(\n    content=\"API pricing document\",\n    source=\"retriever\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://docs.example.com/pricing\",\n        created_at=datetime(2023, 1, 15, tzinfo=timezone.utc),\n    ),\n)\nprint(f\"Freshness: {record.freshness_result.status}\")   # STALE\nprint(f\"Details:   {record.freshness_result.details}\")\n\n# Stale via temporal detection in content\nrecord = trail.log(\n    content=\"As of January 2023, the API supports 3 endpoints.\",\n    source=\"retriever\",\n)\nprint(f\"Freshness: {record.freshness_result.status}\")   # STALE\n</code></pre> <p>Temporal patterns detected include ISO dates (<code>2023-06-15</code>), month-year (<code>January 2024</code>), quarters (<code>Q3 2024</code>), and contextual phrases (<code>as of 2023</code>, <code>last updated: March 2024</code>).</p>"},{"location":"getting-started/#step-5-verify-chain-integrity","title":"Step 5: Verify Chain Integrity","text":"<p>Every record is hash-chained to its predecessor. Verify the entire chain:</p> <pre><code>trail = ContextTrail(backend=\"memory\")\n\ntrail.log(\"First context\", source=\"retriever\")\ntrail.log(\"Second context\", source=\"tool\")\ntrail.log(\"Third context\", source=\"agent\")\n\nverdict = trail.verify_chain()\nprint(f\"Chain intact: {verdict.intact}\")       # True\nprint(f\"Records:      {verdict.total_records}\") # 3\n</code></pre> <p>For HMAC-signed chains (required for compliance):</p> <pre><code>trail = ContextTrail(\n    backend=\"memory\",\n    signing_key=\"my-secret-key\",\n)\n</code></pre> <p>Or via environment variable:</p> <pre><code>export PROVENA_SIGNING_KEY=\"my-secret-key\"\n</code></pre>"},{"location":"getting-started/#step-6-view-the-summary","title":"Step 6: View the Summary","text":"<pre><code>summary = trail.summary()\nprint(f\"Total records: {summary['total']}\")\nprint(f\"Provenance:    {summary['provenance']}\")\nprint(f\"Freshness:     {summary['freshness']}\")\nprint(f\"Sources:       {summary['sources']}\")\n</code></pre>"},{"location":"getting-started/#step-7-use-the-cli","title":"Step 7: Use the CLI","text":"<p>Install the CLI extras:</p> <pre><code>pip install provena[cli]\n</code></pre> <pre><code># Verify chain integrity\nprovena --db audit.db verify\n# PASS -- Chain intact (42 records verified)\n\n# Query the audit log\nprovena --db audit.db audit --source retriever --limit 10\n\n# Generate a compliance report\nprovena --db audit.db report --format text\n\n# Quick summary\nprovena --db audit.db summary\n</code></pre>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<ul> <li>Tracking Context \u2014 Deep-dive into <code>@trail.track()</code></li> <li>Provenance Validation \u2014 Configure required metadata fields</li> <li>Freshness Checking \u2014 Temporal detection patterns</li> <li>Chain Verification \u2014 HMAC signing and tamper detection</li> <li>LangChain Integration \u2014 Add governance to existing chains</li> <li>CLI Reference \u2014 All commands and options</li> </ul>"},{"location":"api/SUMMARY/","title":"SUMMARY","text":"<ul> <li>provena<ul> <li>aggregator</li> <li>buffer</li> <li>cli<ul> <li>main</li> </ul> </li> <li>exporters<ul> <li>otel</li> </ul> </li> <li>hasher</li> <li>integrations<ul> <li>autogen</li> <li>crewai</li> <li>google_adk</li> <li>langchain</li> <li>llamaindex</li> <li>openai_agents</li> </ul> </li> <li>mcp_server</li> <li>models</li> <li>policy</li> <li>report</li> <li>retention</li> <li>storage</li> <li>storage_pg</li> <li>trail</li> <li>validators<ul> <li>freshness</li> <li>provenance</li> </ul> </li> </ul> </li> </ul>"},{"location":"api/provena/","title":"provena","text":""},{"location":"api/provena/#provena","title":"<code>provena</code>","text":"<p>Provena \u2014 Context governance for agentic AI systems.</p>"},{"location":"api/provena/#provena.AggregateVerdict","title":"<code>AggregateVerdict(all_intact, trail_verdicts, total_records)</code>  <code>dataclass</code>","text":"<p>Aggregate chain verification across all trails.</p> <p>Attributes:</p> Name Type Description <code>all_intact</code> <code>bool</code> <p>True only if every trail's chain is intact.</p> <code>trail_verdicts</code> <code>tuple[TrailVerdict, ...]</code> <p>Per-trail verification results.</p> <code>total_records</code> <code>int</code> <p>Sum of records across all trails.</p>"},{"location":"api/provena/#provena.EvidenceGap","title":"<code>EvidenceGap(trail, gap_type, details, record_id=None)</code>  <code>dataclass</code>","text":"<p>A detected governance gap in the multi-agent pipeline.</p> <p>Attributes:</p> Name Type Description <code>trail</code> <code>str</code> <p>Label of the trail where the gap was found.</p> <code>gap_type</code> <code>str</code> <p>Category of gap (broken_chain, stale_context, missing_provenance, unlinked_handoff).</p> <code>details</code> <code>str</code> <p>Human-readable description.</p> <code>record_id</code> <code>int | None</code> <p>Optional record ID related to this gap.</p>"},{"location":"api/provena/#provena.HandoffEdge","title":"<code>HandoffEdge(from_trail, from_record_id, to_trail, to_record_id, run_id='')</code>  <code>dataclass</code>","text":"<p>A tracked context handoff between two agents' trails.</p> <p>Attributes:</p> Name Type Description <code>from_trail</code> <code>str</code> <p>Label of the source trail.</p> <code>from_record_id</code> <code>int</code> <p>Record ID in the source trail.</p> <code>to_trail</code> <code>str</code> <p>Label of the destination trail.</p> <code>to_record_id</code> <code>int</code> <p>Record ID in the destination trail.</p> <code>run_id</code> <code>str</code> <p>Optional workflow/task run identifier grouping this handoff.</p>"},{"location":"api/provena/#provena.TrailAggregator","title":"<code>TrailAggregator()</code>","text":"<p>Aggregates governance data across multiple ContextTrail instances.</p> <p>Each trail maintains its own independent hash chain. The aggregator provides unified querying, summary, verification, handoff tracking, and evidence gap detection across all trails.</p>"},{"location":"api/provena/#provena.TrailAggregator.labels","title":"<code>labels</code>  <code>property</code>","text":"<p>Return the registered trail labels.</p>"},{"location":"api/provena/#provena.TrailAggregator.trail_count","title":"<code>trail_count</code>  <code>property</code>","text":"<p>Number of registered trails.</p>"},{"location":"api/provena/#provena.TrailAggregator.handoffs","title":"<code>handoffs</code>  <code>property</code>","text":"<p>Return all recorded handoff edges.</p>"},{"location":"api/provena/#provena.TrailAggregator.add","title":"<code>add(label, trail)</code>","text":"<p>Register a named trail.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Unique label identifying this trail (e.g. agent name).</p> required <code>trail</code> <code>Any</code> <p>The ContextTrail instance.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the label is already registered.</p>"},{"location":"api/provena/#provena.TrailAggregator.remove","title":"<code>remove(label)</code>","text":"<p>Remove a trail by label.</p>"},{"location":"api/provena/#provena.TrailAggregator.get_trail","title":"<code>get_trail(label)</code>","text":"<p>Return the trail for a given label, or None.</p>"},{"location":"api/provena/#provena.TrailAggregator.record_handoff","title":"<code>record_handoff(from_trail, from_record_id, to_trail, to_record_id, run_id='')</code>","text":"<p>Record a context handoff between two agents' trails.</p> <p>Parameters:</p> Name Type Description Default <code>from_trail</code> <code>str</code> <p>Label of the source agent's trail.</p> required <code>from_record_id</code> <code>int</code> <p>Record ID of the output in the source trail.</p> required <code>to_trail</code> <code>str</code> <p>Label of the destination agent's trail.</p> required <code>to_record_id</code> <code>int</code> <p>Record ID of the input in the destination trail.</p> required <code>run_id</code> <code>str</code> <p>Optional workflow/task identifier.</p> <code>''</code> <p>Returns:</p> Type Description <code>HandoffEdge</code> <p>The created HandoffEdge.</p>"},{"location":"api/provena/#provena.TrailAggregator.summary","title":"<code>summary()</code>","text":"<p>Aggregate summary across all trails.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary with per-trail and aggregate counts for</p> <code>dict[str, Any]</code> <p>provenance, freshness, and source breakdowns.</p>"},{"location":"api/provena/#provena.TrailAggregator.query","title":"<code>query(*, trail_label=None, source=None, start=None, end=None, provenance_status=None, freshness_status=None, run_id=None, limit=100)</code>","text":"<p>Query across all trails with optional filters.</p> <p>Each returned record includes a <code>_trail</code> field with the trail label.</p> <p>Parameters:</p> Name Type Description Default <code>trail_label</code> <code>str | None</code> <p>Restrict to a single trail by label.</p> <code>None</code> <code>source</code> <code>ContextSource | str | None</code> <p>Filter by context source type.</p> <code>None</code> <code>start</code> <code>datetime | None</code> <p>Include only records at or after this timestamp.</p> <code>None</code> <code>end</code> <code>datetime | None</code> <p>Include only records at or before this timestamp.</p> <code>None</code> <code>provenance_status</code> <code>str | None</code> <p>Filter by provenance validation status.</p> <code>None</code> <code>freshness_status</code> <code>str | None</code> <p>Filter by freshness check status.</p> <code>None</code> <code>run_id</code> <code>str | None</code> <p>Filter by run_id (matches records involved in handoffs tagged with this run_id).</p> <code>None</code> <code>limit</code> <code>int</code> <p>Maximum total records to return.</p> <code>100</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>A list of record dicts, each tagged with <code>_trail</code>.</p>"},{"location":"api/provena/#provena.TrailAggregator.verify_chain","title":"<code>verify_chain()</code>","text":"<p>Verify chain integrity of every registered trail.</p> <p>Returns:</p> Type Description <code>AggregateVerdict</code> <p>An AggregateVerdict with per-trail results.</p>"},{"location":"api/provena/#provena.TrailAggregator.timeline","title":"<code>timeline(*, start=None, end=None, limit=200)</code>","text":"<p>Merged chronological view across all trails.</p> <p>Returns records sorted by timestamp, each tagged with <code>_trail</code>. Handoff edges are interleaved as synthetic <code>_handoff</code> entries.</p>"},{"location":"api/provena/#provena.TrailAggregator.detect_gaps","title":"<code>detect_gaps()</code>","text":"<p>Detect governance gaps across all trails.</p> <p>Checks for: - Broken hash chains - Stale context entries - Missing provenance - Unlinked handoffs (referencing non-existent records/trails)</p> <p>Returns:</p> Type Description <code>list[EvidenceGap]</code> <p>A list of EvidenceGap instances.</p>"},{"location":"api/provena/#provena.TrailAggregator.handoffs_for_run","title":"<code>handoffs_for_run(run_id)</code>","text":"<p>Return all handoff edges for a specific run/task ID.</p>"},{"location":"api/provena/#provena.TrailAggregator.close","title":"<code>close()</code>","text":"<p>Close all registered trails.</p>"},{"location":"api/provena/#provena.TrailVerdict","title":"<code>TrailVerdict(label, verdict)</code>  <code>dataclass</code>","text":"<p>Per-trail chain verification result within an aggregation.</p> <p>Attributes:</p> Name Type Description <code>label</code> <code>str</code> <p>The trail's label in the aggregator.</p> <code>verdict</code> <code>ChainVerdict</code> <p>The ChainVerdict from verifying that trail.</p>"},{"location":"api/provena/#provena.ChainVerdict","title":"<code>ChainVerdict(intact, total_records, broken_at=None, details='')</code>  <code>dataclass</code>","text":"<p>Outcome of verifying the integrity of the hash chain.</p> <p>Attributes:</p> Name Type Description <code>intact</code> <code>bool</code> <p>True if every link in the chain is valid.</p> <code>total_records</code> <code>int</code> <p>Number of records that were checked.</p> <code>broken_at</code> <code>int | None</code> <p>Record ID where the chain first broke, or None.</p> <code>details</code> <code>str</code> <p>Human-readable summary of the verification.</p>"},{"location":"api/provena/#provena.ContextEntry","title":"<code>ContextEntry(content_hash, source, source_name, timestamp, provenance=None, metadata=dict(), content_type='str', truncated=False)</code>  <code>dataclass</code>","text":"<p>Immutable representation of a single context input with its content hash.</p> <p>Attributes:</p> Name Type Description <code>content_hash</code> <code>str</code> <p>SHA-256 hex digest of the content bytes.</p> <code>source</code> <code>ContextSource</code> <p>The type of source that produced this context.</p> <code>source_name</code> <code>str</code> <p>Human-readable name identifying the specific source.</p> <code>timestamp</code> <code>datetime</code> <p>When this entry was recorded.</p> <code>provenance</code> <code>ProvenanceMetadata | None</code> <p>Optional origin metadata for validation.</p> <code>metadata</code> <code>dict[str, Any]</code> <p>Arbitrary key-value pairs attached to the entry.</p> <code>content_type</code> <code>str</code> <p>The original content type (<code>\"str\"</code>, <code>\"bytes\"</code>, or <code>\"json\"</code>).</p> <code>truncated</code> <code>bool</code> <p>Whether the content was truncated to fit max_content_bytes.</p>"},{"location":"api/provena/#provena.ContextEntry.create","title":"<code>create(content, source, source_name='', *, provenance=None, metadata=None, max_content_bytes=65536, timestamp=None)</code>  <code>classmethod</code>","text":"<p>Create a ContextEntry by hashing content and resolving the source.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>str | bytes</code> <p>The raw context content to record.</p> required <code>source</code> <code>ContextSource | str</code> <p>A ContextSource enum or string like <code>\"tool:api_name\"</code>.</p> required <code>source_name</code> <code>str</code> <p>Optional explicit name; inferred from source if omitted.</p> <code>''</code> <code>provenance</code> <code>ProvenanceMetadata | None</code> <p>Optional origin metadata for validation.</p> <code>None</code> <code>metadata</code> <code>dict[str, Any] | None</code> <p>Arbitrary key-value pairs to attach.</p> <code>None</code> <code>max_content_bytes</code> <code>int</code> <p>Truncate content beyond this size. Defaults to 64 KB.</p> <code>65536</code> <code>timestamp</code> <code>datetime | None</code> <p>Override the recording timestamp. Defaults to UTC now.</p> <code>None</code> <p>Returns:</p> Type Description <code>ContextEntry</code> <p>A new ContextEntry with a computed SHA-256 content hash.</p>"},{"location":"api/provena/#provena.ContextEntry.to_dict","title":"<code>to_dict()</code>","text":"<p>Serialize this entry to a plain dictionary.</p>"},{"location":"api/provena/#provena.ContextSource","title":"<code>ContextSource</code>","text":"<p>Enumeration of context input source types.</p>"},{"location":"api/provena/#provena.FreshnessResult","title":"<code>FreshnessResult(status, details='', detected_date=None)</code>  <code>dataclass</code>","text":"<p>Result of freshness checking for a context entry.</p> <p>Attributes:</p> Name Type Description <code>status</code> <code>Literal['FRESH', 'STALE', 'UNKNOWN']</code> <p>One of <code>\"FRESH\"</code>, <code>\"STALE\"</code>, or <code>\"UNKNOWN\"</code>.</p> <code>details</code> <code>str</code> <p>Human-readable explanation.</p> <code>detected_date</code> <code>datetime | None</code> <p>The date used for age calculation, if detected.</p>"},{"location":"api/provena/#provena.ProvenanceMetadata","title":"<code>ProvenanceMetadata(source_url=None, author=None, created_at=None, version=None, extra=dict())</code>  <code>dataclass</code>","text":"<p>Immutable metadata about the origin and authorship of a context input.</p> <p>Attributes:</p> Name Type Description <code>source_url</code> <code>str | None</code> <p>URL where the content was retrieved from.</p> <code>author</code> <code>str | None</code> <p>Author or creator of the content.</p> <code>created_at</code> <code>datetime | None</code> <p>When the content was originally created or published.</p> <code>version</code> <code>str | None</code> <p>Version identifier for the content.</p> <code>extra</code> <code>dict[str, Any]</code> <p>Additional key-value metadata.</p>"},{"location":"api/provena/#provena.ProvenanceMetadata.to_dict","title":"<code>to_dict()</code>","text":"<p>Serialize non-None fields to a plain dictionary.</p>"},{"location":"api/provena/#provena.ProvenanceMetadata.from_dict","title":"<code>from_dict(data)</code>  <code>classmethod</code>","text":"<p>Reconstruct a ProvenanceMetadata instance from a dictionary.</p>"},{"location":"api/provena/#provena.TrailRecord","title":"<code>TrailRecord(id, entry, provenance_result, freshness_result, chain_hash, previous_hash, config_hash='')</code>  <code>dataclass</code>","text":"<p>A single record in the audit trail linking a ContextEntry to chain hashes.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>int</code> <p>Sequential record identifier from the storage backend.</p> <code>entry</code> <code>ContextEntry</code> <p>The context entry that was logged.</p> <code>provenance_result</code> <code>ValidationResult | None</code> <p>Provenance validation outcome, if checked.</p> <code>freshness_result</code> <code>FreshnessResult | None</code> <p>Freshness check outcome, if checked.</p> <code>chain_hash</code> <code>str</code> <p>This record's position in the hash chain.</p> <code>previous_hash</code> <code>str</code> <p>The chain hash of the preceding record.</p> <code>config_hash</code> <code>str</code> <p>Hash of the trail configuration at recording time.</p>"},{"location":"api/provena/#provena.TrailRecord.to_dict","title":"<code>to_dict()</code>","text":"<p>Serialize this record to a plain dictionary.</p>"},{"location":"api/provena/#provena.ValidationResult","title":"<code>ValidationResult(status, missing_fields=(), details='')</code>  <code>dataclass</code>","text":"<p>Result of provenance validation for a context entry.</p> <p>Attributes:</p> Name Type Description <code>status</code> <code>Literal['VALID', 'MISSING', 'INCOMPLETE']</code> <p>One of <code>\"VALID\"</code>, <code>\"MISSING\"</code>, or <code>\"INCOMPLETE\"</code>.</p> <code>missing_fields</code> <code>tuple[str, ...]</code> <p>Names of required fields that were absent.</p> <code>details</code> <code>str</code> <p>Human-readable explanation.</p>"},{"location":"api/provena/#provena.EnforcementLevel","title":"<code>EnforcementLevel</code>","text":"<p>How a policy violation is handled.</p>"},{"location":"api/provena/#provena.Policy","title":"<code>Policy(name, check, enforcement=EnforcementLevel.LOG, description='')</code>  <code>dataclass</code>","text":"<p>A governance rule evaluated against every logged record.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Unique identifier for this policy.</p> <code>check</code> <code>Any</code> <p>Callable that returns True if the record passes.</p> <code>enforcement</code> <code>EnforcementLevel</code> <p>What happens when the check fails.</p> <code>description</code> <code>str</code> <p>Human-readable explanation of what the policy enforces.</p>"},{"location":"api/provena/#provena.PolicyCheckResult","title":"<code>PolicyCheckResult(policy_name, passed, enforcement, details='')</code>  <code>dataclass</code>","text":"<p>Outcome of a single policy check against one record.</p> <p>Attributes:</p> Name Type Description <code>policy_name</code> <code>str</code> <p>Name of the policy that was evaluated.</p> <code>passed</code> <code>bool</code> <p>Whether the record satisfied the policy.</p> <code>enforcement</code> <code>EnforcementLevel</code> <p>The enforcement level configured for this policy.</p> <code>details</code> <code>str</code> <p>Human-readable explanation.</p>"},{"location":"api/provena/#provena.PolicyEngine","title":"<code>PolicyEngine(policies=None)</code>","text":"<p>Evaluates a set of policies against trail records.</p> <p>Follows forbid-overrides-permit: if any BLOCK-level policy fails, the overall decision is DENY regardless of other policies.</p>"},{"location":"api/provena/#provena.PolicyEngine.policies","title":"<code>policies</code>  <code>property</code>","text":"<p>Return the registered policies.</p>"},{"location":"api/provena/#provena.PolicyEngine.add","title":"<code>add(policy)</code>","text":"<p>Register an additional policy.</p>"},{"location":"api/provena/#provena.PolicyEngine.evaluate","title":"<code>evaluate(record)</code>","text":"<p>Run all policies against a record and return the aggregate result.</p>"},{"location":"api/provena/#provena.PolicyEngine.from_config","title":"<code>from_config(config)</code>  <code>classmethod</code>","text":"<p>Build a PolicyEngine from a list of declarative policy dicts.</p> <p>Each dict should have keys: <code>check</code>, <code>enforcement</code> (optional, defaults to <code>\"log\"</code>), and check-specific parameters like <code>status</code>.</p> <p>Supported checks: <code>\"freshness\"</code>, <code>\"provenance\"</code>, <code>\"require_signing\"</code>, <code>\"source_allowlist\"</code>.</p>"},{"location":"api/provena/#provena.PolicyEvaluation","title":"<code>PolicyEvaluation(decision, results=())</code>  <code>dataclass</code>","text":"<p>Aggregate result of evaluating all policies against a record.</p> <p>Attributes:</p> Name Type Description <code>decision</code> <code>Literal['ALLOW', 'DENY']</code> <p>Overall outcome \u2014 <code>\"ALLOW\"</code> or <code>\"DENY\"</code>.</p> <code>results</code> <code>tuple[PolicyCheckResult, ...]</code> <p>Individual check results for each policy.</p>"},{"location":"api/provena/#provena.PolicyEvaluation.violations","title":"<code>violations</code>  <code>property</code>","text":"<p>Return only the failing check results.</p>"},{"location":"api/provena/#provena.PolicyViolation","title":"<code>PolicyViolation(policy_name, record, details='')</code>","text":"<p>Raised when a BLOCK-level policy rejects a context input.</p> <p>Attributes:</p> Name Type Description <code>policy_name</code> <p>Name of the policy that triggered the block.</p> <code>record</code> <p>The TrailRecord that was blocked (already persisted).</p> <code>details</code> <p>Human-readable explanation.</p>"},{"location":"api/provena/#provena.RetentionEngine","title":"<code>RetentionEngine(trail, *, retention_days=365, min_retention_days=EU_AI_ACT_MINIMUM_DAYS)</code>","text":"<p>Manages record lifecycle with configurable retention policies.</p> <p>Enforces EU AI Act minimum retention (180 days by default) and supports export-before-delete for compliance archival.</p>"},{"location":"api/provena/#provena.RetentionEngine.find_expired","title":"<code>find_expired(now=None)</code>","text":"<p>Find records older than the retention period.</p> <p>Parameters:</p> Name Type Description Default <code>now</code> <code>datetime | None</code> <p>Override the current time for testing.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>List of record dicts that have exceeded retention.</p>"},{"location":"api/provena/#provena.RetentionEngine.preview","title":"<code>preview(now=None)</code>","text":"<p>Preview what a retention run would do without making changes.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dict with count of records that would be deleted and</p> <code>dict[str, Any]</code> <p>their provenance/freshness breakdown.</p>"},{"location":"api/provena/#provena.RetentionEngine.execute","title":"<code>execute(*, archive_path=None, dry_run=False, now=None)</code>","text":"<p>Execute the retention policy.</p> <p>Parameters:</p> Name Type Description Default <code>archive_path</code> <code>str | None</code> <p>If provided, export expired records to this file before deleting them.</p> <code>None</code> <code>dry_run</code> <code>bool</code> <p>If True, only report what would happen.</p> <code>False</code> <code>now</code> <code>datetime | None</code> <p>Override the current time for testing.</p> <code>None</code> <p>Returns:</p> Type Description <code>RetentionResult</code> <p>A RetentionResult with counts of archived and deleted records.</p>"},{"location":"api/provena/#provena.RetentionResult","title":"<code>RetentionResult(archived, deleted, archive_path=None, details='')</code>  <code>dataclass</code>","text":"<p>Outcome of a retention policy execution.</p> <p>Attributes:</p> Name Type Description <code>archived</code> <code>int</code> <p>Number of records exported before deletion.</p> <code>deleted</code> <code>int</code> <p>Number of records actually deleted.</p> <code>archive_path</code> <code>str | None</code> <p>Path to the export archive file, if created.</p> <code>details</code> <code>str</code> <p>Human-readable summary.</p>"},{"location":"api/provena/#provena.ContextTrail","title":"<code>ContextTrail(*, storage_path='provena.db', backend='sqlite', required_fields=None, max_age_days=90, temporal_detection=True, max_content_bytes=65536, signing_key=None, otel_enabled=False, otel_service_name='provena', strict_mode=False, on_error=None, policies=None, buffered=False, buffer_size=500, flush_interval=1.0, config=None)</code>","text":"<p>Tamper-evident audit trail for AI agent context inputs.</p> <p>Logs every context input with provenance validation, freshness checking, and SHA-256 hash chaining. Supports both programmatic logging via <code>log()</code> and automatic tracking via the <code>@trail.track()</code> decorator.</p> <p>Can be used as a context manager to ensure the storage backend is closed.</p> <p>Initialize a ContextTrail.</p> <p>Parameters:</p> Name Type Description Default <code>storage_path</code> <code>str</code> <p>File path for the SQLite database.</p> <code>'provena.db'</code> <code>backend</code> <code>str</code> <p>Storage backend type, <code>\"sqlite\"</code>, <code>\"memory\"</code>, or <code>\"postgresql\"</code>.</p> <code>'sqlite'</code> <code>required_fields</code> <code>list[str] | tuple[str, ...] | None</code> <p>Provenance fields to require for VALID status.</p> <code>None</code> <code>max_age_days</code> <code>int</code> <p>Content older than this is marked STALE.</p> <code>90</code> <code>temporal_detection</code> <code>bool</code> <p>Enable regex-based date detection in content text.</p> <code>True</code> <code>max_content_bytes</code> <code>int</code> <p>Truncate content beyond this size.</p> <code>65536</code> <code>signing_key</code> <code>str | bytes | None</code> <p>HMAC key for signed hash chains. Also read from <code>PROVENA_SIGNING_KEY</code> env var.</p> <code>None</code> <code>otel_enabled</code> <code>bool</code> <p>Emit OpenTelemetry spans for each logged entry.</p> <code>False</code> <code>otel_service_name</code> <code>str</code> <p>Service name for OTel spans.</p> <code>'provena'</code> <code>strict_mode</code> <code>bool</code> <p>If True, governance errors propagate as exceptions.</p> <code>False</code> <code>on_error</code> <code>Callable[[Exception], None] | None</code> <p>Optional callback invoked on governance errors.</p> <code>None</code> <code>policies</code> <code>list[Policy] | None</code> <p>List of Policy objects to enforce on every logged entry.</p> <code>None</code> <code>buffered</code> <code>bool</code> <p>If True, batch writes via a background thread.</p> <code>False</code> <code>buffer_size</code> <code>int</code> <p>Flush when buffer reaches this many records.</p> <code>500</code> <code>flush_interval</code> <code>float</code> <p>Seconds between periodic flushes.</p> <code>1.0</code> <code>config</code> <code>dict[str, Any] | str | Path | None</code> <p>Configuration dict, or path to a <code>.toml</code>/<code>.yaml</code> file.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If max_age_days or max_content_bytes is less than 1.</p>"},{"location":"api/provena/#provena.ContextTrail.error_count","title":"<code>error_count</code>  <code>property</code>","text":"<p>Number of governance errors encountered during this trail's lifetime.</p>"},{"location":"api/provena/#provena.ContextTrail.is_signed","title":"<code>is_signed</code>  <code>property</code>","text":"<p>Whether this trail uses HMAC-signed hash chains.</p>"},{"location":"api/provena/#provena.ContextTrail.record_count","title":"<code>record_count</code>  <code>property</code>","text":"<p>Total number of records in the audit trail.</p>"},{"location":"api/provena/#provena.ContextTrail.last_record","title":"<code>last_record</code>  <code>property</code>","text":"<p>The most recently logged record, or None if the trail is empty.</p>"},{"location":"api/provena/#provena.ContextTrail.log","title":"<code>log(content, source, source_name='', *, provenance=None, metadata=None)</code>","text":"<p>Log a context input to the audit trail.</p> <p>Computes a content hash, validates provenance, checks freshness, and appends a hash-chained record to storage.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>str | bytes</code> <p>The raw context content (string or bytes).</p> required <code>source</code> <code>ContextSource | str</code> <p>A ContextSource enum value or string like <code>\"tool:api\"</code>.</p> required <code>source_name</code> <code>str</code> <p>Optional explicit source name.</p> <code>''</code> <code>provenance</code> <code>ProvenanceMetadata | None</code> <p>Optional origin metadata for validation.</p> <code>None</code> <code>metadata</code> <code>dict[str, Any] | None</code> <p>Arbitrary key-value pairs to attach to the record.</p> <code>None</code> <p>Returns:</p> Type Description <code>TrailRecord | None</code> <p>The created TrailRecord, or None if a non-strict error occurred.</p>"},{"location":"api/provena/#provena.ContextTrail.track","title":"<code>track(source, source_name='', *, content_extractor=None, policies=None)</code>","text":"<p>Decorator that automatically logs function return values to the trail.</p> <p>Supports both sync and async functions. The decorated function's return value is passed through unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>ContextSource | str</code> <p>A ContextSource enum value or string identifying the source.</p> required <code>source_name</code> <code>str</code> <p>Optional explicit source name.</p> <code>''</code> <code>content_extractor</code> <code>Callable[..., str | bytes | list[Any]] | None</code> <p>Optional callable to extract loggable content from the return value.</p> <code>None</code> <code>policies</code> <code>list[Policy] | None</code> <p>Optional per-decorator policy override. When provided, these policies are used instead of the trail-level policies for calls through this decorator.</p> <code>None</code> <p>Returns:</p> Type Description <code>Callable[[F], F]</code> <p>A decorator that wraps the target function with trail logging.</p>"},{"location":"api/provena/#provena.ContextTrail.verify_chain","title":"<code>verify_chain()</code>","text":"<p>Verify the integrity of the entire hash chain.</p> <p>Recomputes every chain hash from the genesis hash forward and checks each against the stored value.</p> <p>Returns:</p> Type Description <code>ChainVerdict</code> <p>A ChainVerdict indicating whether the chain is intact.</p>"},{"location":"api/provena/#provena.ContextTrail.query","title":"<code>query(*, source=None, start=None, end=None, provenance_status=None, freshness_status=None, limit=100)</code>","text":"<p>Query the audit trail with optional filters.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>ContextSource | str | None</code> <p>Filter by context source type.</p> <code>None</code> <code>start</code> <code>datetime | None</code> <p>Include only records at or after this timestamp.</p> <code>None</code> <code>end</code> <code>datetime | None</code> <p>Include only records at or before this timestamp.</p> <code>None</code> <code>provenance_status</code> <code>str | None</code> <p>Filter by provenance validation status.</p> <code>None</code> <code>freshness_status</code> <code>str | None</code> <p>Filter by freshness check status.</p> <code>None</code> <code>limit</code> <code>int</code> <p>Maximum number of records to return.</p> <code>100</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>A list of record dictionaries matching the filters.</p>"},{"location":"api/provena/#provena.ContextTrail.annotate","title":"<code>annotate(record_id, note, reviewer='')</code>","text":"<p>Add a human oversight annotation to a trail record.</p> <p>Parameters:</p> Name Type Description Default <code>record_id</code> <code>int</code> <p>The ID of the record to annotate.</p> required <code>note</code> <code>str</code> <p>The annotation text.</p> required <code>reviewer</code> <code>str</code> <p>Optional name of the reviewer.</p> <code>''</code> <p>Returns:</p> Type Description <code>int</code> <p>The ID of the created annotation.</p>"},{"location":"api/provena/#provena.ContextTrail.get_annotations","title":"<code>get_annotations(record_id)</code>","text":"<p>Return all annotations for <code>record_id</code> in insertion order.</p> <p>Returns an empty list if the record does not exist or has no annotations (does not raise).</p>"},{"location":"api/provena/#provena.ContextTrail.summary","title":"<code>summary()</code>","text":"<p>Generate an aggregate summary of the audit trail.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary with total count, provenance/freshness/source</p> <code>dict[str, Any]</code> <p>breakdowns, and signing status.</p>"},{"location":"api/provena/#provena.ContextTrail.export","title":"<code>export(format='json')</code>","text":"<p>Export all trail records in the specified format.</p> <p>Parameters:</p> Name Type Description Default <code>format</code> <code>str</code> <p>Output format, either <code>\"json\"</code> or <code>\"csv\"</code>.</p> <code>'json'</code> <p>Returns:</p> Type Description <code>str</code> <p>The serialized trail data as a string.</p>"},{"location":"api/provena/#provena.ContextTrail.health","title":"<code>health()</code>","text":"<p>Return a health-check dictionary for the trail.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary with status, record count, backend type,</p> <code>dict[str, Any]</code> <p>signing state, and error count.</p>"},{"location":"api/provena/#provena.ContextTrail.flush","title":"<code>flush()</code>","text":"<p>Flush buffered records to the storage backend.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of records flushed, or 0 if buffering is not enabled.</p>"},{"location":"api/provena/#provena.ContextTrail.close","title":"<code>close()</code>","text":"<p>Close the storage backend and release resources.</p>"},{"location":"api/provena/#provena.ContextTrail.__enter__","title":"<code>__enter__()</code>","text":"<p>Enter the context manager, returning this trail.</p>"},{"location":"api/provena/#provena.ContextTrail.__exit__","title":"<code>__exit__(*args)</code>","text":"<p>Exit the context manager, closing the storage backend.</p>"},{"location":"api/provena/#provena.freshness_check","title":"<code>freshness_check(*, status='STALE', enforcement=EnforcementLevel.LOG)</code>","text":"<p>Policy that fails when freshness matches the given status.</p>"},{"location":"api/provena/#provena.provenance_check","title":"<code>provenance_check(*, status='MISSING', enforcement=EnforcementLevel.LOG)</code>","text":"<p>Policy that fails when provenance matches the given status.</p>"},{"location":"api/provena/#provena.require_signing","title":"<code>require_signing(*, enforcement=EnforcementLevel.BLOCK, _signed_ref=None)</code>","text":"<p>Policy that fails when the trail is not HMAC-signed.</p> <p>Accepts an optional mutable reference <code>_signed_ref</code> (a single-element list) that can be updated after construction by the trail.</p>"},{"location":"api/provena/#provena.source_allowlist","title":"<code>source_allowlist(*, allowed, enforcement=EnforcementLevel.BLOCK)</code>","text":"<p>Policy that fails when the source is not in the allowlist.</p>"},{"location":"api/provena/#provena.generate_pdf_report","title":"<code>generate_pdf_report(trail, output_path, *, title='Provena Governance Compliance Report')</code>","text":"<p>Generate a PDF compliance report and write it to a file.</p> <p>Parameters:</p> Name Type Description Default <code>trail</code> <code>Any</code> <p>The ContextTrail instance to report on.</p> required <code>output_path</code> <code>str</code> <p>File path for the PDF output.</p> required <code>title</code> <code>str</code> <p>Report title.</p> <code>'Provena Governance Compliance Report'</code> <p>Returns:</p> Type Description <code>str</code> <p>The output file path.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If <code>fpdf2</code> is not installed.</p>"},{"location":"api/provena/#provena.generate_report","title":"<code>generate_report(trail, *, format='json', title='Provena Governance Compliance Report')</code>","text":"<p>Generate a compliance report from a ContextTrail.</p> <p>Parameters:</p> Name Type Description Default <code>trail</code> <code>Any</code> <p>The ContextTrail instance to report on.</p> required <code>format</code> <code>str</code> <p>Output format \u2014 <code>\"json\"</code>, <code>\"text\"</code>, or <code>\"pdf\"</code>.</p> <code>'json'</code> <code>title</code> <code>str</code> <p>Report title (used in text and PDF formats).</p> <code>'Provena Governance Compliance Report'</code> <p>Returns:</p> Type Description <code>str</code> <p>The report content as a string. For PDF, returns the raw bytes</p> <code>str</code> <p>as a string (use <code>generate_pdf_report</code> for file output).</p>"},{"location":"api/provena/aggregator/","title":"aggregator","text":""},{"location":"api/provena/aggregator/#provena.aggregator","title":"<code>aggregator</code>","text":"<p>Multi-trail aggregation for governing multi-agent systems.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.HandoffEdge","title":"<code>HandoffEdge(from_trail, from_record_id, to_trail, to_record_id, run_id='')</code>  <code>dataclass</code>","text":"<p>A tracked context handoff between two agents' trails.</p> <p>Attributes:</p> Name Type Description <code>from_trail</code> <code>str</code> <p>Label of the source trail.</p> <code>from_record_id</code> <code>int</code> <p>Record ID in the source trail.</p> <code>to_trail</code> <code>str</code> <p>Label of the destination trail.</p> <code>to_record_id</code> <code>int</code> <p>Record ID in the destination trail.</p> <code>run_id</code> <code>str</code> <p>Optional workflow/task run identifier grouping this handoff.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailVerdict","title":"<code>TrailVerdict(label, verdict)</code>  <code>dataclass</code>","text":"<p>Per-trail chain verification result within an aggregation.</p> <p>Attributes:</p> Name Type Description <code>label</code> <code>str</code> <p>The trail's label in the aggregator.</p> <code>verdict</code> <code>ChainVerdict</code> <p>The ChainVerdict from verifying that trail.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.AggregateVerdict","title":"<code>AggregateVerdict(all_intact, trail_verdicts, total_records)</code>  <code>dataclass</code>","text":"<p>Aggregate chain verification across all trails.</p> <p>Attributes:</p> Name Type Description <code>all_intact</code> <code>bool</code> <p>True only if every trail's chain is intact.</p> <code>trail_verdicts</code> <code>tuple[TrailVerdict, ...]</code> <p>Per-trail verification results.</p> <code>total_records</code> <code>int</code> <p>Sum of records across all trails.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.EvidenceGap","title":"<code>EvidenceGap(trail, gap_type, details, record_id=None)</code>  <code>dataclass</code>","text":"<p>A detected governance gap in the multi-agent pipeline.</p> <p>Attributes:</p> Name Type Description <code>trail</code> <code>str</code> <p>Label of the trail where the gap was found.</p> <code>gap_type</code> <code>str</code> <p>Category of gap (broken_chain, stale_context, missing_provenance, unlinked_handoff).</p> <code>details</code> <code>str</code> <p>Human-readable description.</p> <code>record_id</code> <code>int | None</code> <p>Optional record ID related to this gap.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator","title":"<code>TrailAggregator()</code>","text":"<p>Aggregates governance data across multiple ContextTrail instances.</p> <p>Each trail maintains its own independent hash chain. The aggregator provides unified querying, summary, verification, handoff tracking, and evidence gap detection across all trails.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.labels","title":"<code>labels</code>  <code>property</code>","text":"<p>Return the registered trail labels.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.trail_count","title":"<code>trail_count</code>  <code>property</code>","text":"<p>Number of registered trails.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.handoffs","title":"<code>handoffs</code>  <code>property</code>","text":"<p>Return all recorded handoff edges.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.add","title":"<code>add(label, trail)</code>","text":"<p>Register a named trail.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Unique label identifying this trail (e.g. agent name).</p> required <code>trail</code> <code>Any</code> <p>The ContextTrail instance.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the label is already registered.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.remove","title":"<code>remove(label)</code>","text":"<p>Remove a trail by label.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.get_trail","title":"<code>get_trail(label)</code>","text":"<p>Return the trail for a given label, or None.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.record_handoff","title":"<code>record_handoff(from_trail, from_record_id, to_trail, to_record_id, run_id='')</code>","text":"<p>Record a context handoff between two agents' trails.</p> <p>Parameters:</p> Name Type Description Default <code>from_trail</code> <code>str</code> <p>Label of the source agent's trail.</p> required <code>from_record_id</code> <code>int</code> <p>Record ID of the output in the source trail.</p> required <code>to_trail</code> <code>str</code> <p>Label of the destination agent's trail.</p> required <code>to_record_id</code> <code>int</code> <p>Record ID of the input in the destination trail.</p> required <code>run_id</code> <code>str</code> <p>Optional workflow/task identifier.</p> <code>''</code> <p>Returns:</p> Type Description <code>HandoffEdge</code> <p>The created HandoffEdge.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.summary","title":"<code>summary()</code>","text":"<p>Aggregate summary across all trails.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary with per-trail and aggregate counts for</p> <code>dict[str, Any]</code> <p>provenance, freshness, and source breakdowns.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.query","title":"<code>query(*, trail_label=None, source=None, start=None, end=None, provenance_status=None, freshness_status=None, run_id=None, limit=100)</code>","text":"<p>Query across all trails with optional filters.</p> <p>Each returned record includes a <code>_trail</code> field with the trail label.</p> <p>Parameters:</p> Name Type Description Default <code>trail_label</code> <code>str | None</code> <p>Restrict to a single trail by label.</p> <code>None</code> <code>source</code> <code>ContextSource | str | None</code> <p>Filter by context source type.</p> <code>None</code> <code>start</code> <code>datetime | None</code> <p>Include only records at or after this timestamp.</p> <code>None</code> <code>end</code> <code>datetime | None</code> <p>Include only records at or before this timestamp.</p> <code>None</code> <code>provenance_status</code> <code>str | None</code> <p>Filter by provenance validation status.</p> <code>None</code> <code>freshness_status</code> <code>str | None</code> <p>Filter by freshness check status.</p> <code>None</code> <code>run_id</code> <code>str | None</code> <p>Filter by run_id (matches records involved in handoffs tagged with this run_id).</p> <code>None</code> <code>limit</code> <code>int</code> <p>Maximum total records to return.</p> <code>100</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>A list of record dicts, each tagged with <code>_trail</code>.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.verify_chain","title":"<code>verify_chain()</code>","text":"<p>Verify chain integrity of every registered trail.</p> <p>Returns:</p> Type Description <code>AggregateVerdict</code> <p>An AggregateVerdict with per-trail results.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.timeline","title":"<code>timeline(*, start=None, end=None, limit=200)</code>","text":"<p>Merged chronological view across all trails.</p> <p>Returns records sorted by timestamp, each tagged with <code>_trail</code>. Handoff edges are interleaved as synthetic <code>_handoff</code> entries.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.detect_gaps","title":"<code>detect_gaps()</code>","text":"<p>Detect governance gaps across all trails.</p> <p>Checks for: - Broken hash chains - Stale context entries - Missing provenance - Unlinked handoffs (referencing non-existent records/trails)</p> <p>Returns:</p> Type Description <code>list[EvidenceGap]</code> <p>A list of EvidenceGap instances.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.handoffs_for_run","title":"<code>handoffs_for_run(run_id)</code>","text":"<p>Return all handoff edges for a specific run/task ID.</p>"},{"location":"api/provena/aggregator/#provena.aggregator.TrailAggregator.close","title":"<code>close()</code>","text":"<p>Close all registered trails.</p>"},{"location":"api/provena/buffer/","title":"buffer","text":""},{"location":"api/provena/buffer/#provena.buffer","title":"<code>buffer</code>","text":"<p>Async batch write buffer for high-throughput trail logging.</p>"},{"location":"api/provena/buffer/#provena.buffer.WriteBuffer","title":"<code>WriteBuffer(backend, buffer_size=500, flush_interval=1.0)</code>","text":"<p>Thread-safe write buffer that batches storage backend writes.</p> <p>Records are appended to an in-memory deque (sub-microsecond) and flushed to the storage backend periodically by a background daemon thread or when the buffer reaches capacity.</p>"},{"location":"api/provena/buffer/#provena.buffer.WriteBuffer.pending","title":"<code>pending</code>  <code>property</code>","text":"<p>Number of records waiting to be flushed.</p>"},{"location":"api/provena/buffer/#provena.buffer.WriteBuffer.append","title":"<code>append(record)</code>","text":"<p>Append a record to the buffer. Triggers flush if buffer is full.</p>"},{"location":"api/provena/buffer/#provena.buffer.WriteBuffer.flush","title":"<code>flush()</code>","text":"<p>Flush all buffered records to the backend. Returns count flushed.</p>"},{"location":"api/provena/buffer/#provena.buffer.WriteBuffer.close","title":"<code>close()</code>","text":"<p>Stop the background thread and flush remaining records.</p>"},{"location":"api/provena/hasher/","title":"hasher","text":""},{"location":"api/provena/hasher/#provena.hasher","title":"<code>hasher</code>","text":"<p>SHA-256 hash chain computation and verification.</p>"},{"location":"api/provena/hasher/#provena.hasher.ChainHasher","title":"<code>ChainHasher(signing_key=None)</code>","text":"<p>Computes and verifies SHA-256 hash chain links.</p> <p>Supports optional HMAC-SHA256 signing when a signing key is provided.</p> <p>Initialize the hasher with an optional HMAC signing key.</p>"},{"location":"api/provena/hasher/#provena.hasher.ChainHasher.is_signed","title":"<code>is_signed</code>  <code>property</code>","text":"<p>Whether this hasher uses HMAC signing.</p>"},{"location":"api/provena/hasher/#provena.hasher.ChainHasher.compute_chain_hash","title":"<code>compute_chain_hash(previous_hash, content_hash, source, timestamp)</code>","text":"<p>Compute the hash for a chain link.</p> <p>Parameters:</p> Name Type Description Default <code>previous_hash</code> <code>str</code> <p>The chain hash of the preceding record.</p> required <code>content_hash</code> <code>str</code> <p>SHA-256 hex digest of the content.</p> required <code>source</code> <code>str</code> <p>Source type string (e.g. <code>\"retriever\"</code>).</p> required <code>timestamp</code> <code>str</code> <p>ISO-format timestamp string.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Hex-encoded SHA-256 or HMAC-SHA256 digest of the link.</p>"},{"location":"api/provena/hasher/#provena.hasher.ChainHasher.verify_link","title":"<code>verify_link(previous_hash, content_hash, source, timestamp, expected_hash)</code>","text":"<p>Verify that a chain link matches the expected hash.</p> <p>Uses constant-time comparison to prevent timing attacks.</p> <p>Parameters:</p> Name Type Description Default <code>previous_hash</code> <code>str</code> <p>The chain hash of the preceding record.</p> required <code>content_hash</code> <code>str</code> <p>SHA-256 hex digest of the content.</p> required <code>source</code> <code>str</code> <p>Source type string.</p> required <code>timestamp</code> <code>str</code> <p>ISO-format timestamp string.</p> required <code>expected_hash</code> <code>str</code> <p>The hash value to verify against.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the recomputed hash matches expected_hash.</p>"},{"location":"api/provena/hasher/#provena.hasher.content_hash","title":"<code>content_hash(content)</code>","text":"<p>Compute the SHA-256 hex digest of raw content bytes.</p>"},{"location":"api/provena/mcp_server/","title":"mcp_server","text":""},{"location":"api/provena/mcp_server/#provena.mcp_server","title":"<code>mcp_server</code>","text":"<p>MCP server exposing governance data to agents via tools and resources.</p>"},{"location":"api/provena/mcp_server/#provena.mcp_server.configure","title":"<code>configure(trail)</code>","text":"<p>Bind a ContextTrail instance for the MCP server to use.</p>"},{"location":"api/provena/mcp_server/#provena.mcp_server.get_trail","title":"<code>get_trail()</code>","text":"<p>Return the configured trail, creating a default one if needed.</p>"},{"location":"api/provena/mcp_server/#provena.mcp_server.create_server","title":"<code>create_server(name='provena-governance')</code>","text":"<p>Create and return a configured FastMCP server instance.</p>"},{"location":"api/provena/models/","title":"models","text":""},{"location":"api/provena/models/#provena.models","title":"<code>models</code>","text":"<p>Data models for context governance records and validation results.</p>"},{"location":"api/provena/models/#provena.models.ContextSource","title":"<code>ContextSource</code>","text":"<p>Enumeration of context input source types.</p>"},{"location":"api/provena/models/#provena.models.ProvenanceMetadata","title":"<code>ProvenanceMetadata(source_url=None, author=None, created_at=None, version=None, extra=dict())</code>  <code>dataclass</code>","text":"<p>Immutable metadata about the origin and authorship of a context input.</p> <p>Attributes:</p> Name Type Description <code>source_url</code> <code>str | None</code> <p>URL where the content was retrieved from.</p> <code>author</code> <code>str | None</code> <p>Author or creator of the content.</p> <code>created_at</code> <code>datetime | None</code> <p>When the content was originally created or published.</p> <code>version</code> <code>str | None</code> <p>Version identifier for the content.</p> <code>extra</code> <code>dict[str, Any]</code> <p>Additional key-value metadata.</p>"},{"location":"api/provena/models/#provena.models.ProvenanceMetadata.to_dict","title":"<code>to_dict()</code>","text":"<p>Serialize non-None fields to a plain dictionary.</p>"},{"location":"api/provena/models/#provena.models.ProvenanceMetadata.from_dict","title":"<code>from_dict(data)</code>  <code>classmethod</code>","text":"<p>Reconstruct a ProvenanceMetadata instance from a dictionary.</p>"},{"location":"api/provena/models/#provena.models.ValidationResult","title":"<code>ValidationResult(status, missing_fields=(), details='')</code>  <code>dataclass</code>","text":"<p>Result of provenance validation for a context entry.</p> <p>Attributes:</p> Name Type Description <code>status</code> <code>Literal['VALID', 'MISSING', 'INCOMPLETE']</code> <p>One of <code>\"VALID\"</code>, <code>\"MISSING\"</code>, or <code>\"INCOMPLETE\"</code>.</p> <code>missing_fields</code> <code>tuple[str, ...]</code> <p>Names of required fields that were absent.</p> <code>details</code> <code>str</code> <p>Human-readable explanation.</p>"},{"location":"api/provena/models/#provena.models.FreshnessResult","title":"<code>FreshnessResult(status, details='', detected_date=None)</code>  <code>dataclass</code>","text":"<p>Result of freshness checking for a context entry.</p> <p>Attributes:</p> Name Type Description <code>status</code> <code>Literal['FRESH', 'STALE', 'UNKNOWN']</code> <p>One of <code>\"FRESH\"</code>, <code>\"STALE\"</code>, or <code>\"UNKNOWN\"</code>.</p> <code>details</code> <code>str</code> <p>Human-readable explanation.</p> <code>detected_date</code> <code>datetime | None</code> <p>The date used for age calculation, if detected.</p>"},{"location":"api/provena/models/#provena.models.ContextEntry","title":"<code>ContextEntry(content_hash, source, source_name, timestamp, provenance=None, metadata=dict(), content_type='str', truncated=False)</code>  <code>dataclass</code>","text":"<p>Immutable representation of a single context input with its content hash.</p> <p>Attributes:</p> Name Type Description <code>content_hash</code> <code>str</code> <p>SHA-256 hex digest of the content bytes.</p> <code>source</code> <code>ContextSource</code> <p>The type of source that produced this context.</p> <code>source_name</code> <code>str</code> <p>Human-readable name identifying the specific source.</p> <code>timestamp</code> <code>datetime</code> <p>When this entry was recorded.</p> <code>provenance</code> <code>ProvenanceMetadata | None</code> <p>Optional origin metadata for validation.</p> <code>metadata</code> <code>dict[str, Any]</code> <p>Arbitrary key-value pairs attached to the entry.</p> <code>content_type</code> <code>str</code> <p>The original content type (<code>\"str\"</code>, <code>\"bytes\"</code>, or <code>\"json\"</code>).</p> <code>truncated</code> <code>bool</code> <p>Whether the content was truncated to fit max_content_bytes.</p>"},{"location":"api/provena/models/#provena.models.ContextEntry.create","title":"<code>create(content, source, source_name='', *, provenance=None, metadata=None, max_content_bytes=65536, timestamp=None)</code>  <code>classmethod</code>","text":"<p>Create a ContextEntry by hashing content and resolving the source.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>str | bytes</code> <p>The raw context content to record.</p> required <code>source</code> <code>ContextSource | str</code> <p>A ContextSource enum or string like <code>\"tool:api_name\"</code>.</p> required <code>source_name</code> <code>str</code> <p>Optional explicit name; inferred from source if omitted.</p> <code>''</code> <code>provenance</code> <code>ProvenanceMetadata | None</code> <p>Optional origin metadata for validation.</p> <code>None</code> <code>metadata</code> <code>dict[str, Any] | None</code> <p>Arbitrary key-value pairs to attach.</p> <code>None</code> <code>max_content_bytes</code> <code>int</code> <p>Truncate content beyond this size. Defaults to 64 KB.</p> <code>65536</code> <code>timestamp</code> <code>datetime | None</code> <p>Override the recording timestamp. Defaults to UTC now.</p> <code>None</code> <p>Returns:</p> Type Description <code>ContextEntry</code> <p>A new ContextEntry with a computed SHA-256 content hash.</p>"},{"location":"api/provena/models/#provena.models.ContextEntry.to_dict","title":"<code>to_dict()</code>","text":"<p>Serialize this entry to a plain dictionary.</p>"},{"location":"api/provena/models/#provena.models.TrailRecord","title":"<code>TrailRecord(id, entry, provenance_result, freshness_result, chain_hash, previous_hash, config_hash='')</code>  <code>dataclass</code>","text":"<p>A single record in the audit trail linking a ContextEntry to chain hashes.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>int</code> <p>Sequential record identifier from the storage backend.</p> <code>entry</code> <code>ContextEntry</code> <p>The context entry that was logged.</p> <code>provenance_result</code> <code>ValidationResult | None</code> <p>Provenance validation outcome, if checked.</p> <code>freshness_result</code> <code>FreshnessResult | None</code> <p>Freshness check outcome, if checked.</p> <code>chain_hash</code> <code>str</code> <p>This record's position in the hash chain.</p> <code>previous_hash</code> <code>str</code> <p>The chain hash of the preceding record.</p> <code>config_hash</code> <code>str</code> <p>Hash of the trail configuration at recording time.</p>"},{"location":"api/provena/models/#provena.models.TrailRecord.to_dict","title":"<code>to_dict()</code>","text":"<p>Serialize this record to a plain dictionary.</p>"},{"location":"api/provena/models/#provena.models.ChainVerdict","title":"<code>ChainVerdict(intact, total_records, broken_at=None, details='')</code>  <code>dataclass</code>","text":"<p>Outcome of verifying the integrity of the hash chain.</p> <p>Attributes:</p> Name Type Description <code>intact</code> <code>bool</code> <p>True if every link in the chain is valid.</p> <code>total_records</code> <code>int</code> <p>Number of records that were checked.</p> <code>broken_at</code> <code>int | None</code> <p>Record ID where the chain first broke, or None.</p> <code>details</code> <code>str</code> <p>Human-readable summary of the verification.</p>"},{"location":"api/provena/policy/","title":"policy","text":""},{"location":"api/provena/policy/#provena.policy","title":"<code>policy</code>","text":"<p>Policy engine for governance enforcement on context inputs.</p>"},{"location":"api/provena/policy/#provena.policy.EnforcementLevel","title":"<code>EnforcementLevel</code>","text":"<p>How a policy violation is handled.</p>"},{"location":"api/provena/policy/#provena.policy.PolicyViolation","title":"<code>PolicyViolation(policy_name, record, details='')</code>","text":"<p>Raised when a BLOCK-level policy rejects a context input.</p> <p>Attributes:</p> Name Type Description <code>policy_name</code> <p>Name of the policy that triggered the block.</p> <code>record</code> <p>The TrailRecord that was blocked (already persisted).</p> <code>details</code> <p>Human-readable explanation.</p>"},{"location":"api/provena/policy/#provena.policy.PolicyCheckResult","title":"<code>PolicyCheckResult(policy_name, passed, enforcement, details='')</code>  <code>dataclass</code>","text":"<p>Outcome of a single policy check against one record.</p> <p>Attributes:</p> Name Type Description <code>policy_name</code> <code>str</code> <p>Name of the policy that was evaluated.</p> <code>passed</code> <code>bool</code> <p>Whether the record satisfied the policy.</p> <code>enforcement</code> <code>EnforcementLevel</code> <p>The enforcement level configured for this policy.</p> <code>details</code> <code>str</code> <p>Human-readable explanation.</p>"},{"location":"api/provena/policy/#provena.policy.PolicyEvaluation","title":"<code>PolicyEvaluation(decision, results=())</code>  <code>dataclass</code>","text":"<p>Aggregate result of evaluating all policies against a record.</p> <p>Attributes:</p> Name Type Description <code>decision</code> <code>Literal['ALLOW', 'DENY']</code> <p>Overall outcome \u2014 <code>\"ALLOW\"</code> or <code>\"DENY\"</code>.</p> <code>results</code> <code>tuple[PolicyCheckResult, ...]</code> <p>Individual check results for each policy.</p>"},{"location":"api/provena/policy/#provena.policy.PolicyEvaluation.violations","title":"<code>violations</code>  <code>property</code>","text":"<p>Return only the failing check results.</p>"},{"location":"api/provena/policy/#provena.policy.Policy","title":"<code>Policy(name, check, enforcement=EnforcementLevel.LOG, description='')</code>  <code>dataclass</code>","text":"<p>A governance rule evaluated against every logged record.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Unique identifier for this policy.</p> <code>check</code> <code>Any</code> <p>Callable that returns True if the record passes.</p> <code>enforcement</code> <code>EnforcementLevel</code> <p>What happens when the check fails.</p> <code>description</code> <code>str</code> <p>Human-readable explanation of what the policy enforces.</p>"},{"location":"api/provena/policy/#provena.policy.PolicyEngine","title":"<code>PolicyEngine(policies=None)</code>","text":"<p>Evaluates a set of policies against trail records.</p> <p>Follows forbid-overrides-permit: if any BLOCK-level policy fails, the overall decision is DENY regardless of other policies.</p>"},{"location":"api/provena/policy/#provena.policy.PolicyEngine.policies","title":"<code>policies</code>  <code>property</code>","text":"<p>Return the registered policies.</p>"},{"location":"api/provena/policy/#provena.policy.PolicyEngine.add","title":"<code>add(policy)</code>","text":"<p>Register an additional policy.</p>"},{"location":"api/provena/policy/#provena.policy.PolicyEngine.evaluate","title":"<code>evaluate(record)</code>","text":"<p>Run all policies against a record and return the aggregate result.</p>"},{"location":"api/provena/policy/#provena.policy.PolicyEngine.from_config","title":"<code>from_config(config)</code>  <code>classmethod</code>","text":"<p>Build a PolicyEngine from a list of declarative policy dicts.</p> <p>Each dict should have keys: <code>check</code>, <code>enforcement</code> (optional, defaults to <code>\"log\"</code>), and check-specific parameters like <code>status</code>.</p> <p>Supported checks: <code>\"freshness\"</code>, <code>\"provenance\"</code>, <code>\"require_signing\"</code>, <code>\"source_allowlist\"</code>.</p>"},{"location":"api/provena/policy/#provena.policy.freshness_check","title":"<code>freshness_check(*, status='STALE', enforcement=EnforcementLevel.LOG)</code>","text":"<p>Policy that fails when freshness matches the given status.</p>"},{"location":"api/provena/policy/#provena.policy.provenance_check","title":"<code>provenance_check(*, status='MISSING', enforcement=EnforcementLevel.LOG)</code>","text":"<p>Policy that fails when provenance matches the given status.</p>"},{"location":"api/provena/policy/#provena.policy.require_signing","title":"<code>require_signing(*, enforcement=EnforcementLevel.BLOCK, _signed_ref=None)</code>","text":"<p>Policy that fails when the trail is not HMAC-signed.</p> <p>Accepts an optional mutable reference <code>_signed_ref</code> (a single-element list) that can be updated after construction by the trail.</p>"},{"location":"api/provena/policy/#provena.policy.source_allowlist","title":"<code>source_allowlist(*, allowed, enforcement=EnforcementLevel.BLOCK)</code>","text":"<p>Policy that fails when the source is not in the allowlist.</p>"},{"location":"api/provena/report/","title":"report","text":""},{"location":"api/provena/report/#provena.report","title":"<code>report</code>","text":"<p>Compliance report generator for EU AI Act and OWASP ASI06.</p>"},{"location":"api/provena/report/#provena.report.generate_report","title":"<code>generate_report(trail, *, format='json', title='Provena Governance Compliance Report')</code>","text":"<p>Generate a compliance report from a ContextTrail.</p> <p>Parameters:</p> Name Type Description Default <code>trail</code> <code>Any</code> <p>The ContextTrail instance to report on.</p> required <code>format</code> <code>str</code> <p>Output format \u2014 <code>\"json\"</code>, <code>\"text\"</code>, or <code>\"pdf\"</code>.</p> <code>'json'</code> <code>title</code> <code>str</code> <p>Report title (used in text and PDF formats).</p> <code>'Provena Governance Compliance Report'</code> <p>Returns:</p> Type Description <code>str</code> <p>The report content as a string. For PDF, returns the raw bytes</p> <code>str</code> <p>as a string (use <code>generate_pdf_report</code> for file output).</p>"},{"location":"api/provena/report/#provena.report.generate_pdf_report","title":"<code>generate_pdf_report(trail, output_path, *, title='Provena Governance Compliance Report')</code>","text":"<p>Generate a PDF compliance report and write it to a file.</p> <p>Parameters:</p> Name Type Description Default <code>trail</code> <code>Any</code> <p>The ContextTrail instance to report on.</p> required <code>output_path</code> <code>str</code> <p>File path for the PDF output.</p> required <code>title</code> <code>str</code> <p>Report title.</p> <code>'Provena Governance Compliance Report'</code> <p>Returns:</p> Type Description <code>str</code> <p>The output file path.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If <code>fpdf2</code> is not installed.</p>"},{"location":"api/provena/retention/","title":"retention","text":""},{"location":"api/provena/retention/#provena.retention","title":"<code>retention</code>","text":"<p>Retention policy engine for automatic record lifecycle management.</p>"},{"location":"api/provena/retention/#provena.retention.RetentionResult","title":"<code>RetentionResult(archived, deleted, archive_path=None, details='')</code>  <code>dataclass</code>","text":"<p>Outcome of a retention policy execution.</p> <p>Attributes:</p> Name Type Description <code>archived</code> <code>int</code> <p>Number of records exported before deletion.</p> <code>deleted</code> <code>int</code> <p>Number of records actually deleted.</p> <code>archive_path</code> <code>str | None</code> <p>Path to the export archive file, if created.</p> <code>details</code> <code>str</code> <p>Human-readable summary.</p>"},{"location":"api/provena/retention/#provena.retention.RetentionEngine","title":"<code>RetentionEngine(trail, *, retention_days=365, min_retention_days=EU_AI_ACT_MINIMUM_DAYS)</code>","text":"<p>Manages record lifecycle with configurable retention policies.</p> <p>Enforces EU AI Act minimum retention (180 days by default) and supports export-before-delete for compliance archival.</p>"},{"location":"api/provena/retention/#provena.retention.RetentionEngine.find_expired","title":"<code>find_expired(now=None)</code>","text":"<p>Find records older than the retention period.</p> <p>Parameters:</p> Name Type Description Default <code>now</code> <code>datetime | None</code> <p>Override the current time for testing.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>List of record dicts that have exceeded retention.</p>"},{"location":"api/provena/retention/#provena.retention.RetentionEngine.preview","title":"<code>preview(now=None)</code>","text":"<p>Preview what a retention run would do without making changes.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dict with count of records that would be deleted and</p> <code>dict[str, Any]</code> <p>their provenance/freshness breakdown.</p>"},{"location":"api/provena/retention/#provena.retention.RetentionEngine.execute","title":"<code>execute(*, archive_path=None, dry_run=False, now=None)</code>","text":"<p>Execute the retention policy.</p> <p>Parameters:</p> Name Type Description Default <code>archive_path</code> <code>str | None</code> <p>If provided, export expired records to this file before deleting them.</p> <code>None</code> <code>dry_run</code> <code>bool</code> <p>If True, only report what would happen.</p> <code>False</code> <code>now</code> <code>datetime | None</code> <p>Override the current time for testing.</p> <code>None</code> <p>Returns:</p> Type Description <code>RetentionResult</code> <p>A RetentionResult with counts of archived and deleted records.</p>"},{"location":"api/provena/storage/","title":"storage","text":""},{"location":"api/provena/storage/#provena.storage","title":"<code>storage</code>","text":"<p>Storage backends for the context audit trail.</p>"},{"location":"api/provena/storage/#provena.storage.StorageBackend","title":"<code>StorageBackend</code>","text":"<p>Protocol defining the storage backend interface.</p> <p>All backends must implement these methods for appending records, querying, annotating, and lifecycle management.</p>"},{"location":"api/provena/storage/#provena.storage.StorageBackend.append","title":"<code>append(record)</code>","text":"<p>Append a record and return its assigned ID.</p>"},{"location":"api/provena/storage/#provena.storage.StorageBackend.get","title":"<code>get(record_id)</code>","text":"<p>Retrieve a record by ID, or None if not found.</p>"},{"location":"api/provena/storage/#provena.storage.StorageBackend.get_last","title":"<code>get_last()</code>","text":"<p>Retrieve the most recently appended record, or None.</p>"},{"location":"api/provena/storage/#provena.storage.StorageBackend.count","title":"<code>count()</code>","text":"<p>Return the total number of records.</p>"},{"location":"api/provena/storage/#provena.storage.StorageBackend.all_records","title":"<code>all_records()</code>","text":"<p>Return all records ordered by ID.</p>"},{"location":"api/provena/storage/#provena.storage.StorageBackend.query","title":"<code>query(*, source=None, start=None, end=None, provenance_status=None, freshness_status=None, limit=100)</code>","text":"<p>Query records with optional filters.</p>"},{"location":"api/provena/storage/#provena.storage.StorageBackend.add_annotation","title":"<code>add_annotation(record_id, note, reviewer, timestamp)</code>","text":"<p>Add an annotation to a record and return the annotation ID.</p>"},{"location":"api/provena/storage/#provena.storage.StorageBackend.close","title":"<code>close()</code>","text":"<p>Close the backend and release resources.</p>"},{"location":"api/provena/storage/#provena.storage.SQLiteBackend","title":"<code>SQLiteBackend(path='provena.db')</code>","text":"<p>SQLite-based storage backend with WAL mode and schema versioning.</p> <p>Records are stored in an append-only table with automatic schema migration. Thread-safe via per-instance locking.</p> <p>Open or create a SQLite database at the given path.</p>"},{"location":"api/provena/storage/#provena.storage.SQLiteBackend.append","title":"<code>append(record)</code>","text":"<p>Append a record and return its assigned ID.</p>"},{"location":"api/provena/storage/#provena.storage.SQLiteBackend.get","title":"<code>get(record_id)</code>","text":"<p>Retrieve a record by ID, or None if not found.</p>"},{"location":"api/provena/storage/#provena.storage.SQLiteBackend.get_last","title":"<code>get_last()</code>","text":"<p>Retrieve the most recently appended record, or None.</p>"},{"location":"api/provena/storage/#provena.storage.SQLiteBackend.count","title":"<code>count()</code>","text":"<p>Return the total number of records.</p>"},{"location":"api/provena/storage/#provena.storage.SQLiteBackend.all_records","title":"<code>all_records()</code>","text":"<p>Return all records ordered by ID.</p>"},{"location":"api/provena/storage/#provena.storage.SQLiteBackend.query","title":"<code>query(*, source=None, start=None, end=None, provenance_status=None, freshness_status=None, limit=100)</code>","text":"<p>Query records with optional filters.</p>"},{"location":"api/provena/storage/#provena.storage.SQLiteBackend.add_annotation","title":"<code>add_annotation(record_id, note, reviewer, timestamp)</code>","text":"<p>Add an annotation to a record and return the annotation ID.</p>"},{"location":"api/provena/storage/#provena.storage.SQLiteBackend.close","title":"<code>close()</code>","text":"<p>Close the backend and release resources.</p>"},{"location":"api/provena/storage/#provena.storage.InMemoryBackend","title":"<code>InMemoryBackend()</code>","text":"<p>In-memory storage backend for testing and disabled mode.</p>"},{"location":"api/provena/storage/#provena.storage.InMemoryBackend.append","title":"<code>append(record)</code>","text":"<p>Append a record and return its assigned ID.</p>"},{"location":"api/provena/storage/#provena.storage.InMemoryBackend.get","title":"<code>get(record_id)</code>","text":"<p>Retrieve a record by ID, or None if not found.</p>"},{"location":"api/provena/storage/#provena.storage.InMemoryBackend.get_last","title":"<code>get_last()</code>","text":"<p>Retrieve the most recently appended record, or None.</p>"},{"location":"api/provena/storage/#provena.storage.InMemoryBackend.count","title":"<code>count()</code>","text":"<p>Return the total number of records.</p>"},{"location":"api/provena/storage/#provena.storage.InMemoryBackend.all_records","title":"<code>all_records()</code>","text":"<p>Return all records ordered by ID.</p>"},{"location":"api/provena/storage/#provena.storage.InMemoryBackend.query","title":"<code>query(*, source=None, start=None, end=None, provenance_status=None, freshness_status=None, limit=100)</code>","text":"<p>Query records with optional filters.</p>"},{"location":"api/provena/storage/#provena.storage.InMemoryBackend.add_annotation","title":"<code>add_annotation(record_id, note, reviewer, timestamp)</code>","text":"<p>Add an annotation to a record and return the annotation ID.</p>"},{"location":"api/provena/storage/#provena.storage.InMemoryBackend.close","title":"<code>close()</code>","text":"<p>Close the backend and release resources.</p>"},{"location":"api/provena/storage_pg/","title":"storage_pg","text":""},{"location":"api/provena/storage_pg/#provena.storage_pg","title":"<code>storage_pg</code>","text":"<p>PostgreSQL storage backend for the context audit trail.</p>"},{"location":"api/provena/storage_pg/#provena.storage_pg.PostgreSQLBackend","title":"<code>PostgreSQLBackend(conninfo, pool_size=5, min_pool_size=1, hasher=None)</code>","text":"<p>PostgreSQL-based storage backend with connection pooling.</p> <p>Uses psycopg v3 with <code>psycopg_pool.ConnectionPool</code> for concurrent access. Chain hash ordering is enforced via <code>pg_advisory_xact_lock</code>.</p>"},{"location":"api/provena/trail/","title":"trail","text":""},{"location":"api/provena/trail/#provena.trail","title":"<code>trail</code>","text":"<p>Core ContextTrail class for logging and auditing context inputs.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail","title":"<code>ContextTrail(*, storage_path='provena.db', backend='sqlite', required_fields=None, max_age_days=90, temporal_detection=True, max_content_bytes=65536, signing_key=None, otel_enabled=False, otel_service_name='provena', strict_mode=False, on_error=None, policies=None, buffered=False, buffer_size=500, flush_interval=1.0, config=None)</code>","text":"<p>Tamper-evident audit trail for AI agent context inputs.</p> <p>Logs every context input with provenance validation, freshness checking, and SHA-256 hash chaining. Supports both programmatic logging via <code>log()</code> and automatic tracking via the <code>@trail.track()</code> decorator.</p> <p>Can be used as a context manager to ensure the storage backend is closed.</p> <p>Initialize a ContextTrail.</p> <p>Parameters:</p> Name Type Description Default <code>storage_path</code> <code>str</code> <p>File path for the SQLite database.</p> <code>'provena.db'</code> <code>backend</code> <code>str</code> <p>Storage backend type, <code>\"sqlite\"</code>, <code>\"memory\"</code>, or <code>\"postgresql\"</code>.</p> <code>'sqlite'</code> <code>required_fields</code> <code>list[str] | tuple[str, ...] | None</code> <p>Provenance fields to require for VALID status.</p> <code>None</code> <code>max_age_days</code> <code>int</code> <p>Content older than this is marked STALE.</p> <code>90</code> <code>temporal_detection</code> <code>bool</code> <p>Enable regex-based date detection in content text.</p> <code>True</code> <code>max_content_bytes</code> <code>int</code> <p>Truncate content beyond this size.</p> <code>65536</code> <code>signing_key</code> <code>str | bytes | None</code> <p>HMAC key for signed hash chains. Also read from <code>PROVENA_SIGNING_KEY</code> env var.</p> <code>None</code> <code>otel_enabled</code> <code>bool</code> <p>Emit OpenTelemetry spans for each logged entry.</p> <code>False</code> <code>otel_service_name</code> <code>str</code> <p>Service name for OTel spans.</p> <code>'provena'</code> <code>strict_mode</code> <code>bool</code> <p>If True, governance errors propagate as exceptions.</p> <code>False</code> <code>on_error</code> <code>Callable[[Exception], None] | None</code> <p>Optional callback invoked on governance errors.</p> <code>None</code> <code>policies</code> <code>list[Policy] | None</code> <p>List of Policy objects to enforce on every logged entry.</p> <code>None</code> <code>buffered</code> <code>bool</code> <p>If True, batch writes via a background thread.</p> <code>False</code> <code>buffer_size</code> <code>int</code> <p>Flush when buffer reaches this many records.</p> <code>500</code> <code>flush_interval</code> <code>float</code> <p>Seconds between periodic flushes.</p> <code>1.0</code> <code>config</code> <code>dict[str, Any] | str | Path | None</code> <p>Configuration dict, or path to a <code>.toml</code>/<code>.yaml</code> file.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If max_age_days or max_content_bytes is less than 1.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.error_count","title":"<code>error_count</code>  <code>property</code>","text":"<p>Number of governance errors encountered during this trail's lifetime.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.is_signed","title":"<code>is_signed</code>  <code>property</code>","text":"<p>Whether this trail uses HMAC-signed hash chains.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.record_count","title":"<code>record_count</code>  <code>property</code>","text":"<p>Total number of records in the audit trail.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.last_record","title":"<code>last_record</code>  <code>property</code>","text":"<p>The most recently logged record, or None if the trail is empty.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.log","title":"<code>log(content, source, source_name='', *, provenance=None, metadata=None)</code>","text":"<p>Log a context input to the audit trail.</p> <p>Computes a content hash, validates provenance, checks freshness, and appends a hash-chained record to storage.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>str | bytes</code> <p>The raw context content (string or bytes).</p> required <code>source</code> <code>ContextSource | str</code> <p>A ContextSource enum value or string like <code>\"tool:api\"</code>.</p> required <code>source_name</code> <code>str</code> <p>Optional explicit source name.</p> <code>''</code> <code>provenance</code> <code>ProvenanceMetadata | None</code> <p>Optional origin metadata for validation.</p> <code>None</code> <code>metadata</code> <code>dict[str, Any] | None</code> <p>Arbitrary key-value pairs to attach to the record.</p> <code>None</code> <p>Returns:</p> Type Description <code>TrailRecord | None</code> <p>The created TrailRecord, or None if a non-strict error occurred.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.track","title":"<code>track(source, source_name='', *, content_extractor=None, policies=None)</code>","text":"<p>Decorator that automatically logs function return values to the trail.</p> <p>Supports both sync and async functions. The decorated function's return value is passed through unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>ContextSource | str</code> <p>A ContextSource enum value or string identifying the source.</p> required <code>source_name</code> <code>str</code> <p>Optional explicit source name.</p> <code>''</code> <code>content_extractor</code> <code>Callable[..., str | bytes | list[Any]] | None</code> <p>Optional callable to extract loggable content from the return value.</p> <code>None</code> <code>policies</code> <code>list[Policy] | None</code> <p>Optional per-decorator policy override. When provided, these policies are used instead of the trail-level policies for calls through this decorator.</p> <code>None</code> <p>Returns:</p> Type Description <code>Callable[[F], F]</code> <p>A decorator that wraps the target function with trail logging.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.verify_chain","title":"<code>verify_chain()</code>","text":"<p>Verify the integrity of the entire hash chain.</p> <p>Recomputes every chain hash from the genesis hash forward and checks each against the stored value.</p> <p>Returns:</p> Type Description <code>ChainVerdict</code> <p>A ChainVerdict indicating whether the chain is intact.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.query","title":"<code>query(*, source=None, start=None, end=None, provenance_status=None, freshness_status=None, limit=100)</code>","text":"<p>Query the audit trail with optional filters.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>ContextSource | str | None</code> <p>Filter by context source type.</p> <code>None</code> <code>start</code> <code>datetime | None</code> <p>Include only records at or after this timestamp.</p> <code>None</code> <code>end</code> <code>datetime | None</code> <p>Include only records at or before this timestamp.</p> <code>None</code> <code>provenance_status</code> <code>str | None</code> <p>Filter by provenance validation status.</p> <code>None</code> <code>freshness_status</code> <code>str | None</code> <p>Filter by freshness check status.</p> <code>None</code> <code>limit</code> <code>int</code> <p>Maximum number of records to return.</p> <code>100</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>A list of record dictionaries matching the filters.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.annotate","title":"<code>annotate(record_id, note, reviewer='')</code>","text":"<p>Add a human oversight annotation to a trail record.</p> <p>Parameters:</p> Name Type Description Default <code>record_id</code> <code>int</code> <p>The ID of the record to annotate.</p> required <code>note</code> <code>str</code> <p>The annotation text.</p> required <code>reviewer</code> <code>str</code> <p>Optional name of the reviewer.</p> <code>''</code> <p>Returns:</p> Type Description <code>int</code> <p>The ID of the created annotation.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.get_annotations","title":"<code>get_annotations(record_id)</code>","text":"<p>Return all annotations for <code>record_id</code> in insertion order.</p> <p>Returns an empty list if the record does not exist or has no annotations (does not raise).</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.summary","title":"<code>summary()</code>","text":"<p>Generate an aggregate summary of the audit trail.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary with total count, provenance/freshness/source</p> <code>dict[str, Any]</code> <p>breakdowns, and signing status.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.export","title":"<code>export(format='json')</code>","text":"<p>Export all trail records in the specified format.</p> <p>Parameters:</p> Name Type Description Default <code>format</code> <code>str</code> <p>Output format, either <code>\"json\"</code> or <code>\"csv\"</code>.</p> <code>'json'</code> <p>Returns:</p> Type Description <code>str</code> <p>The serialized trail data as a string.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.health","title":"<code>health()</code>","text":"<p>Return a health-check dictionary for the trail.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary with status, record count, backend type,</p> <code>dict[str, Any]</code> <p>signing state, and error count.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.flush","title":"<code>flush()</code>","text":"<p>Flush buffered records to the storage backend.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of records flushed, or 0 if buffering is not enabled.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.close","title":"<code>close()</code>","text":"<p>Close the storage backend and release resources.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.__enter__","title":"<code>__enter__()</code>","text":"<p>Enter the context manager, returning this trail.</p>"},{"location":"api/provena/trail/#provena.trail.ContextTrail.__exit__","title":"<code>__exit__(*args)</code>","text":"<p>Exit the context manager, closing the storage backend.</p>"},{"location":"api/provena/cli/","title":"cli","text":""},{"location":"api/provena/cli/#provena.cli","title":"<code>cli</code>","text":""},{"location":"api/provena/cli/main/","title":"main","text":""},{"location":"api/provena/cli/main/#provena.cli.main","title":"<code>main</code>","text":""},{"location":"api/provena/cli/main/#provena.cli.main.cli","title":"<code>cli(ctx, db, config_path, signing_key)</code>","text":"<p>Provena \u2014 Context governance for agentic AI systems.</p>"},{"location":"api/provena/cli/main/#provena.cli.main.audit","title":"<code>audit(ctx, source, limit, start, end, fmt)</code>","text":"<p>Query the context governance audit log.</p>"},{"location":"api/provena/cli/main/#provena.cli.main.verify","title":"<code>verify(ctx)</code>","text":"<p>Verify the integrity of the hash-chained audit trail.</p>"},{"location":"api/provena/cli/main/#provena.cli.main.report","title":"<code>report(ctx, fmt, output)</code>","text":"<p>Generate a context governance compliance report.</p>"},{"location":"api/provena/cli/main/#provena.cli.main.retain","title":"<code>retain(ctx, max_age, archive, dry_run)</code>","text":"<p>Apply retention policy \u2014 delete old records with optional archive.</p>"},{"location":"api/provena/cli/main/#provena.cli.main.summary","title":"<code>summary(ctx)</code>","text":"<p>Show a quick summary of the audit trail.</p>"},{"location":"api/provena/cli/main/#provena.cli.main.mcp","title":"<code>mcp()</code>","text":"<p>MCP server commands.</p>"},{"location":"api/provena/cli/main/#provena.cli.main.serve","title":"<code>serve(ctx, db, transport)</code>","text":"<p>Start the Provena MCP governance server.</p>"},{"location":"api/provena/cli/main/#provena.cli.main.migrate","title":"<code>migrate(ctx, from_path, to_path, batch_size)</code>","text":"<p>Migrate trail data between storage backends.</p>"},{"location":"api/provena/exporters/","title":"exporters","text":""},{"location":"api/provena/exporters/#provena.exporters","title":"<code>exporters</code>","text":"<p>Export integrations for governance data.</p>"},{"location":"api/provena/exporters/#provena.exporters.OTelExporter","title":"<code>OTelExporter(enabled=True, service_name='provena', version='', tracer=None)</code>","text":"<p>Emits OpenTelemetry spans for each context governance event.</p> <p>Requires the <code>opentelemetry-api</code> package. When disabled or when OTel is not installed, all methods are safe no-ops.</p>"},{"location":"api/provena/exporters/#provena.exporters.OTelExporter.enabled","title":"<code>enabled</code>  <code>property</code>","text":"<p>Whether OTel export is active.</p>"},{"location":"api/provena/exporters/#provena.exporters.OTelExporter.emit","title":"<code>emit(record)</code>","text":"<p>Emit a span for the given trail record.</p>"},{"location":"api/provena/exporters/otel/","title":"otel","text":""},{"location":"api/provena/exporters/otel/#provena.exporters.otel","title":"<code>otel</code>","text":"<p>OpenTelemetry span exporter for governance events.</p>"},{"location":"api/provena/exporters/otel/#provena.exporters.otel.OTelExporter","title":"<code>OTelExporter(enabled=True, service_name='provena', version='', tracer=None)</code>","text":"<p>Emits OpenTelemetry spans for each context governance event.</p> <p>Requires the <code>opentelemetry-api</code> package. When disabled or when OTel is not installed, all methods are safe no-ops.</p>"},{"location":"api/provena/exporters/otel/#provena.exporters.otel.OTelExporter.enabled","title":"<code>enabled</code>  <code>property</code>","text":"<p>Whether OTel export is active.</p>"},{"location":"api/provena/exporters/otel/#provena.exporters.otel.OTelExporter.emit","title":"<code>emit(record)</code>","text":"<p>Emit a span for the given trail record.</p>"},{"location":"api/provena/integrations/","title":"integrations","text":""},{"location":"api/provena/integrations/#provena.integrations","title":"<code>integrations</code>","text":""},{"location":"api/provena/integrations/autogen/","title":"autogen","text":""},{"location":"api/provena/integrations/autogen/#provena.integrations.autogen","title":"<code>autogen</code>","text":"<p>AutoGen integration for logging agent messages to a Provena trail.</p>"},{"location":"api/provena/integrations/autogen/#provena.integrations.autogen.ProvenaAutoGenHook","title":"<code>ProvenaAutoGenHook(trail)</code>","text":"<p>AutoGen hook that logs agent messages to a Provena trail.</p> <p>Register via <code>agent.register_hook(\"process_message_before_send\", ...)</code>.</p> <p>Usage::</p> <pre><code>from provena import ContextTrail\nfrom provena.integrations.autogen import ProvenaAutoGenHook\n\ntrail = ContextTrail()\nhook = ProvenaAutoGenHook(trail=trail)\nagent.register_hook(\n    \"process_message_before_send\", hook.process_message\n)\n</code></pre>"},{"location":"api/provena/integrations/autogen/#provena.integrations.autogen.ProvenaAutoGenHook.process_message","title":"<code>process_message(sender, message, recipient, silent)</code>","text":"<p>Log the message content and return it unchanged.</p>"},{"location":"api/provena/integrations/crewai/","title":"crewai","text":""},{"location":"api/provena/integrations/crewai/#provena.integrations.crewai","title":"<code>crewai</code>","text":"<p>CrewAI integration for logging agent and task outputs to a Provena trail.</p>"},{"location":"api/provena/integrations/crewai/#provena.integrations.crewai.ProvenaCrewListener","title":"<code>ProvenaCrewListener(*args, **kwargs)</code>","text":"<p>Placeholder when crewai is not installed.</p>"},{"location":"api/provena/integrations/google_adk/","title":"google_adk","text":""},{"location":"api/provena/integrations/google_adk/#provena.integrations.google_adk","title":"<code>google_adk</code>","text":"<p>Google ADK integration for logging tool outputs to a Provena trail.</p>"},{"location":"api/provena/integrations/google_adk/#provena.integrations.google_adk.ProvenaADKCallback","title":"<code>ProvenaADKCallback(trail)</code>","text":"<p>Google ADK callback that logs tool outputs to a Provena trail.</p> <p>Use as <code>after_tool_callback</code> on an ADK Agent.</p> <p>Usage::</p> <pre><code>from provena import ContextTrail\nfrom provena.integrations.google_adk import ProvenaADKCallback\n\ntrail = ContextTrail()\ncallback = ProvenaADKCallback(trail=trail)\n\nagent = Agent(\n    name=\"my-agent\",\n    after_tool_callback=callback.after_tool_call,\n)\n</code></pre>"},{"location":"api/provena/integrations/google_adk/#provena.integrations.google_adk.ProvenaADKCallback.after_tool_call","title":"<code>after_tool_call(tool, args, tool_context, tool_response)</code>","text":"<p>Log the tool response and return None (pass-through).</p>"},{"location":"api/provena/integrations/langchain/","title":"langchain","text":""},{"location":"api/provena/integrations/langchain/#provena.integrations.langchain","title":"<code>langchain</code>","text":""},{"location":"api/provena/integrations/langchain/#provena.integrations.langchain.ProvenaCallback","title":"<code>ProvenaCallback(*args, **kwargs)</code>","text":"<p>Placeholder when langchain-core is not installed.</p>"},{"location":"api/provena/integrations/llamaindex/","title":"llamaindex","text":""},{"location":"api/provena/integrations/llamaindex/#provena.integrations.llamaindex","title":"<code>llamaindex</code>","text":""},{"location":"api/provena/integrations/llamaindex/#provena.integrations.llamaindex.ProvenaPostprocessor","title":"<code>ProvenaPostprocessor(*args, **kwargs)</code>","text":"<p>Placeholder when llama-index-core is not installed.</p>"},{"location":"api/provena/integrations/openai_agents/","title":"openai_agents","text":""},{"location":"api/provena/integrations/openai_agents/#provena.integrations.openai_agents","title":"<code>openai_agents</code>","text":"<p>OpenAI Agents SDK integration for logging tool and agent outputs.</p>"},{"location":"api/provena/integrations/openai_agents/#provena.integrations.openai_agents.ProvenaRunHooks","title":"<code>ProvenaRunHooks(*args, **kwargs)</code>","text":"<p>Placeholder when openai-agents is not installed.</p>"},{"location":"api/provena/validators/","title":"validators","text":""},{"location":"api/provena/validators/#provena.validators","title":"<code>validators</code>","text":"<p>Validation components for provenance and freshness.</p>"},{"location":"api/provena/validators/#provena.validators.FreshnessChecker","title":"<code>FreshnessChecker(max_age_days=90, temporal_detection=True)</code>","text":"<p>Checks whether context inputs are fresh or stale.</p> <p>Uses provenance timestamps when available, falling back to regex-based temporal marker detection in the content text.</p> <p>Initialize the freshness checker.</p> <p>Parameters:</p> Name Type Description Default <code>max_age_days</code> <code>int</code> <p>Content older than this threshold is marked STALE.</p> <code>90</code> <code>temporal_detection</code> <code>bool</code> <p>If True, scan content text for date patterns when no metadata timestamp is available.</p> <code>True</code>"},{"location":"api/provena/validators/#provena.validators.FreshnessChecker.max_age_days","title":"<code>max_age_days</code>  <code>property</code>","text":"<p>The staleness threshold in days.</p>"},{"location":"api/provena/validators/#provena.validators.FreshnessChecker.check","title":"<code>check(entry, content=None, now=None)</code>","text":"<p>Check the freshness of a context entry.</p> <p>Parameters:</p> Name Type Description Default <code>entry</code> <code>ContextEntry</code> <p>The context entry to check.</p> required <code>content</code> <code>str | None</code> <p>Optional raw text for temporal marker detection.</p> <code>None</code> <code>now</code> <code>datetime | None</code> <p>Override the current time for testing.</p> <code>None</code> <p>Returns:</p> Type Description <code>FreshnessResult</code> <p>A FreshnessResult with status FRESH, STALE, or UNKNOWN.</p>"},{"location":"api/provena/validators/#provena.validators.ProvenanceValidator","title":"<code>ProvenanceValidator(required_fields=None)</code>","text":"<p>Validates that context entries carry required provenance metadata.</p> <p>Checks each entry against a configurable list of required fields and returns a ValidationResult with status VALID, MISSING, or INCOMPLETE.</p> <p>Initialize with the fields required for VALID provenance.</p>"},{"location":"api/provena/validators/#provena.validators.ProvenanceValidator.required_fields","title":"<code>required_fields</code>  <code>property</code>","text":"<p>The tuple of field names required for VALID status.</p>"},{"location":"api/provena/validators/#provena.validators.ProvenanceValidator.validate","title":"<code>validate(entry)</code>","text":"<p>Validate the provenance metadata of a context entry.</p> <p>Parameters:</p> Name Type Description Default <code>entry</code> <code>ContextEntry</code> <p>The context entry to validate.</p> required <p>Returns:</p> Type Description <code>ValidationResult</code> <p>A ValidationResult with status VALID, MISSING, or INCOMPLETE.</p>"},{"location":"api/provena/validators/freshness/","title":"freshness","text":""},{"location":"api/provena/validators/freshness/#provena.validators.freshness","title":"<code>freshness</code>","text":"<p>Freshness checker for detecting stale context inputs.</p>"},{"location":"api/provena/validators/freshness/#provena.validators.freshness.FreshnessChecker","title":"<code>FreshnessChecker(max_age_days=90, temporal_detection=True)</code>","text":"<p>Checks whether context inputs are fresh or stale.</p> <p>Uses provenance timestamps when available, falling back to regex-based temporal marker detection in the content text.</p> <p>Initialize the freshness checker.</p> <p>Parameters:</p> Name Type Description Default <code>max_age_days</code> <code>int</code> <p>Content older than this threshold is marked STALE.</p> <code>90</code> <code>temporal_detection</code> <code>bool</code> <p>If True, scan content text for date patterns when no metadata timestamp is available.</p> <code>True</code>"},{"location":"api/provena/validators/freshness/#provena.validators.freshness.FreshnessChecker.max_age_days","title":"<code>max_age_days</code>  <code>property</code>","text":"<p>The staleness threshold in days.</p>"},{"location":"api/provena/validators/freshness/#provena.validators.freshness.FreshnessChecker.check","title":"<code>check(entry, content=None, now=None)</code>","text":"<p>Check the freshness of a context entry.</p> <p>Parameters:</p> Name Type Description Default <code>entry</code> <code>ContextEntry</code> <p>The context entry to check.</p> required <code>content</code> <code>str | None</code> <p>Optional raw text for temporal marker detection.</p> <code>None</code> <code>now</code> <code>datetime | None</code> <p>Override the current time for testing.</p> <code>None</code> <p>Returns:</p> Type Description <code>FreshnessResult</code> <p>A FreshnessResult with status FRESH, STALE, or UNKNOWN.</p>"},{"location":"api/provena/validators/provenance/","title":"provenance","text":""},{"location":"api/provena/validators/provenance/#provena.validators.provenance","title":"<code>provenance</code>","text":"<p>Provenance metadata validator for context entries.</p>"},{"location":"api/provena/validators/provenance/#provena.validators.provenance.ProvenanceValidator","title":"<code>ProvenanceValidator(required_fields=None)</code>","text":"<p>Validates that context entries carry required provenance metadata.</p> <p>Checks each entry against a configurable list of required fields and returns a ValidationResult with status VALID, MISSING, or INCOMPLETE.</p> <p>Initialize with the fields required for VALID provenance.</p>"},{"location":"api/provena/validators/provenance/#provena.validators.provenance.ProvenanceValidator.required_fields","title":"<code>required_fields</code>  <code>property</code>","text":"<p>The tuple of field names required for VALID status.</p>"},{"location":"api/provena/validators/provenance/#provena.validators.provenance.ProvenanceValidator.validate","title":"<code>validate(entry)</code>","text":"<p>Validate the provenance metadata of a context entry.</p> <p>Parameters:</p> Name Type Description Default <code>entry</code> <code>ContextEntry</code> <p>The context entry to validate.</p> required <p>Returns:</p> Type Description <code>ValidationResult</code> <p>A ValidationResult with status VALID, MISSING, or INCOMPLETE.</p>"},{"location":"compliance/eu-ai-act/","title":"EU AI Act Compliance","text":"<p>The EU AI Act (Regulation 2024/1689) establishes the world's first comprehensive legal framework for artificial intelligence. For organizations deploying high-risk AI systems, context governance is no longer optional -- it is a legal obligation. Provena maps directly to six key articles of the regulation, providing the technical controls needed to demonstrate compliance.</p> <p>Enforcement is active</p> <p>The EU AI Act entered into force on 1 August 2024. Obligations for high-risk AI systems apply from 2 August 2026. Organizations that fail to implement adequate governance controls face fines of up to EUR 15 million or 3% of global annual turnover.</p>"},{"location":"compliance/eu-ai-act/#article-by-article-mapping","title":"Article-by-Article Mapping","text":""},{"location":"compliance/eu-ai-act/#article-9-risk-management","title":"Article 9 -- Risk Management","text":"<p>What the article requires. Providers of high-risk AI systems must establish, implement, document, and maintain a risk management system. This system must identify and analyze known and reasonably foreseeable risks, estimate and evaluate those risks, and adopt appropriate and targeted risk management measures.</p> <p>How Provena satisfies it. Provena's three-layer governance pipeline -- provenance validation, freshness checking, and hash-chained audit trails -- constitutes a systematic risk management system for context inputs. Every context entry is validated against configurable provenance requirements (<code>ProvenanceValidator</code>), checked for staleness (<code>FreshnessChecker</code>), and recorded in a tamper-evident chain (<code>ChainHasher</code>). Validation results are classified as <code>VALID</code>, <code>INCOMPLETE</code>, or <code>MISSING</code> for provenance, and <code>FRESH</code>, <code>STALE</code>, or <code>UNKNOWN</code> for freshness, enabling quantitative risk assessment.</p> <pre><code>trail = ContextTrail(\n    required_fields=[\"source_url\", \"author\", \"created_at\"],\n    max_age_days=90,\n    strict_mode=True,  # Governance failures raise exceptions\n)\n</code></pre>"},{"location":"compliance/eu-ai-act/#article-10-data-governance","title":"Article 10 -- Data Governance","text":"<p>What the article requires. Training, validation, and testing datasets must be subject to appropriate data governance and management practices. Data must be relevant, sufficiently representative, and free of errors. Datasets must take into account the specific geographical, contextual, behavioral, or functional setting of the system.</p> <p>How Provena satisfies it. The <code>ProvenanceValidator</code> ensures that every context input carries source metadata -- origin URL, author, creation date, and version. Inputs without provenance are flagged as <code>MISSING</code>; inputs with incomplete metadata are flagged as <code>INCOMPLETE</code>. The <code>FreshnessChecker</code> verifies timestamps against a configurable threshold (<code>max_age_days</code>) and uses temporal pattern detection to identify date references in content text, catching stale data even when metadata timestamps are absent.</p> <pre><code>from provena.models import ProvenanceMetadata\n\nprovenance = ProvenanceMetadata(\n    source_url=\"https://api.example.com/v2/data\",\n    author=\"data-engineering-team\",\n    created_at=datetime(2026, 6, 15, tzinfo=timezone.utc),\n    version=\"2.1.0\",\n)\n\nrecord = trail.log(content, source=\"retriever\", provenance=provenance)\n# record.provenance_result.status -&gt; \"VALID\"\n# record.freshness_result.status  -&gt; \"FRESH\"\n</code></pre>"},{"location":"compliance/eu-ai-act/#article-12-record-keeping","title":"Article 12 -- Record-Keeping","text":"<p>What the article requires. High-risk AI systems must be designed and developed with capabilities enabling the automatic recording of events (logging) relevant to identifying situations that may result in risk. Logging must enable traceability of the system's operation throughout its lifecycle.</p> <p>How Provena satisfies it. Provena implements a Merkle-style hash chain using SHA-256 (or HMAC-SHA256 when a signing key is provided). Each <code>TrailRecord</code> contains:</p> <ul> <li>A SHA-256 content hash of the input</li> <li>Provenance validation status and missing fields</li> <li>Freshness check status with detected dates</li> <li>A chain hash linking to the previous record</li> <li>Timestamp, source type, and arbitrary metadata</li> </ul> <p>The chain is tamper-evident: modifying any record invalidates all subsequent hashes. The <code>verify_chain()</code> method performs full-chain verification, returning a <code>ChainVerdict</code> that identifies the exact record where integrity was broken.</p> <pre><code>verdict = trail.verify_chain()\nassert verdict.intact  # True if no tampering detected\n# verdict.broken_at    -&gt; record ID where chain broke (if any)\n# verdict.total_records -&gt; number of records verified\n</code></pre> <p>Retention requirements</p> <p>Article 12 requires log retention for a period appropriate to the intended purpose of the high-risk AI system, which is at least six months unless provided otherwise in applicable Union or national law. Provena's SQLite backend provides persistent storage. For production deployments, enable the OpenTelemetry exporter to forward governance events to durable, centrally-managed storage systems.</p>"},{"location":"compliance/eu-ai-act/#article-13-transparency","title":"Article 13 -- Transparency","text":"<p>What the article requires. High-risk AI systems must be designed and developed in such a way as to ensure that their operation is sufficiently transparent to enable deployers to interpret the system's output and use it appropriately.</p> <p>How Provena satisfies it. Every governance event recorded by Provena includes an explanation of what was validated and why. Provenance results include the list of missing fields. Freshness results include the detected date, the content age in days, and the threshold that was applied. Source attribution (<code>ContextSource</code> enum: <code>RETRIEVER</code>, <code>TOOL</code>, <code>AGENT</code>, <code>MEMORY</code>, <code>MCP</code>, <code>CUSTOM</code>) identifies the origin type for every input. The <code>trail.summary()</code> method produces aggregate breakdowns by provenance status, freshness status, and source type.</p> <pre><code>summary = trail.summary()\n# {\n#   \"total\": 142,\n#   \"provenance\": {\"VALID\": 118, \"INCOMPLETE\": 19, \"MISSING\": 5},\n#   \"freshness\": {\"FRESH\": 130, \"STALE\": 8, \"UNKNOWN\": 4},\n#   \"sources\": {\"retriever\": 95, \"tool\": 30, \"agent\": 17},\n#   \"signed\": True\n# }\n</code></pre>"},{"location":"compliance/eu-ai-act/#article-14-human-oversight","title":"Article 14 -- Human Oversight","text":"<p>What the article requires. High-risk AI systems must be designed and developed in such a way as to be effectively overseen by natural persons during their period of use. Human oversight measures must enable the individuals to whom oversight is assigned to properly understand the relevant capacities and limitations of the system and to intervene or interrupt the system as necessary.</p> <p>How Provena satisfies it. The audit trail records both accepted and flagged context inputs, with detailed reasons for each governance decision. The <code>trail.annotate()</code> method allows human reviewers to attach oversight notes to any record, creating a documented chain of human judgment:</p> <pre><code># Reviewer examines a flagged record\nflagged = trail.query(provenance_status=\"INCOMPLETE\", limit=10)\n\nfor record in flagged:\n    # Review and annotate\n    trail.annotate(\n        record_id=record[\"id\"],\n        note=\"Approved: source verified via internal registry\",\n        reviewer=\"compliance-officer@example.com\",\n    )\n</code></pre> <p>The <code>query()</code> method supports filtering by provenance status, freshness status, source type, and time range, enabling compliance officers to focus on records that require human review.</p>"},{"location":"compliance/eu-ai-act/#article-26-deployer-obligations","title":"Article 26 -- Deployer Obligations","text":"<p>What the article requires. Deployers of high-risk AI systems must monitor the operation of the system on the basis of the instructions of use. They must keep logs automatically generated by the system, to the extent such logs are under their control, for a period appropriate to the intended purpose, and for at least six months.</p> <p>How Provena satisfies it. Provena's SQLite backend (<code>storage_path</code> parameter) writes all governance events to persistent local storage. For centralized log management and long-term retention, the OpenTelemetry exporter forwards governance spans to any OTel-compatible backend:</p> <pre><code>trail = ContextTrail(\n    storage_path=\"/var/lib/provena/audit.db\",\n    otel_enabled=True,\n    otel_service_name=\"production-rag-pipeline\",\n)\n</code></pre> <p>Six-month minimum retention</p> <p>The six-month retention requirement under Article 26 is a legal floor. Configure your OTel collector and storage backend to retain governance events for at least this period. Organizations subject to sector-specific regulations (e.g., financial services, healthcare) may face longer retention requirements.</p>"},{"location":"compliance/eu-ai-act/#compliance-mapping","title":"Compliance Mapping","text":"<p>The following table maps specific EU AI Act requirements to Provena features:</p> EU AI Act Requirement Article Provena Feature Systematic risk identification and mitigation Art. 9 Three-layer validation pipeline (provenance, freshness, chain integrity) Data governance and management practices Art. 10 <code>ProvenanceValidator</code> with configurable required fields Data quality and relevance verification Art. 10 <code>FreshnessChecker</code> with temporal pattern detection Automatic event logging Art. 12 SHA-256 hash-chained <code>TrailRecord</code> for every context input Tamper-evident record-keeping Art. 12 Merkle-style chain with <code>verify_chain()</code> integrity verification Forensic reconstruction capability Art. 12 Full chain replay via <code>query()</code> and <code>export()</code> (JSON/CSV) Operational transparency Art. 13 Governance event details: validation status, missing fields, detected dates Source attribution Art. 13 <code>ContextSource</code> enum and source metadata on every record Human oversight enablement Art. 14 <code>trail.annotate()</code> for reviewer decisions on flagged records Flagged input visibility Art. 14 <code>query(provenance_status=\"INCOMPLETE\")</code> filtering Log retention (6-month minimum) Art. 26 SQLite persistent backend + OTel export to durable storage Operational monitoring Art. 26 <code>trail.health()</code> and <code>trail.summary()</code> for system status"},{"location":"compliance/eu-ai-act/#penalty-structure","title":"Penalty Structure","text":"<p>The EU AI Act establishes a tiered penalty framework. Understanding which tier applies to your obligations is essential for risk assessment.</p> Tier Maximum Fine Scope Examples Tier 1 EUR 35 million or 7% of global annual turnover Prohibited AI practices Social scoring, real-time biometric identification in public spaces Tier 2 EUR 15 million or 3% of global annual turnover High-risk AI system obligations Inadequate risk management, missing audit trails, insufficient data governance Tier 3 EUR 7.5 million or 1% of global annual turnover Misleading information Providing incorrect or incomplete information to notified bodies or authorities <p>Provena's territory is Tier 2</p> <p>Most obligations addressed by Provena fall under Tier 2. Failure to maintain adequate data governance (Article 10), record-keeping (Article 12), or deployer log retention (Article 26) exposes organizations to fines of up to EUR 15 million or 3% of global annual turnover, whichever is higher.</p>"},{"location":"compliance/eu-ai-act/#enforcement-timeline","title":"Enforcement Timeline","text":"Date Milestone 1 August 2024 Regulation enters into force 2 February 2025 Prohibited AI practices apply 2 August 2025 Obligations for general-purpose AI models apply 2 August 2026 High-risk AI system obligations apply (Provena's primary scope) 2 August 2027 Full enforcement for all AI systems, including those in Annex I"},{"location":"compliance/eu-ai-act/#enforcement-case-studies","title":"Enforcement Case Studies","text":"<p>These cases illustrate the real-world consequences of inadequate context governance</p>"},{"location":"compliance/eu-ai-act/#case-1-frankfurt-wealth-management-firm-2027","title":"Case 1: Frankfurt Wealth Management Firm (2027)","text":"<p>A Frankfurt-based wealth management firm received a EUR 4.5 million fine from the German Federal Financial Supervisory Authority (BaFin) under EU AI Act Article 10 and Article 12 obligations.</p> <p>What happened. The firm deployed a RAG-based advisory system that retrieved financial research reports and regulatory guidance to generate client recommendations. An internal audit revealed that the retrieval pipeline was serving research reports up to 14 months old without any staleness detection. The system cited a withdrawn ECB guideline in three client portfolios.</p> <p>What was missing.</p> <ul> <li>No provenance validation on retrieved documents -- the system could not   distinguish between current and superseded regulatory guidance</li> <li>No freshness checking -- stale research reports were treated identically to   current ones</li> <li>No audit trail -- when regulators requested evidence of what context inputs   informed specific client recommendations, the firm could not reconstruct the   decision chain</li> </ul> <p>How Provena would have prevented it. The <code>FreshnessChecker</code> would have flagged 14-month-old reports as <code>STALE</code>. The <code>ProvenanceValidator</code> would have required source URLs and version identifiers, making it possible to detect when a cited guideline was withdrawn. The hash-chained audit trail would have provided the forensic reconstruction capability that regulators demanded.</p>"},{"location":"compliance/eu-ai-act/#case-2-french-insurance-company-2027","title":"Case 2: French Insurance Company (2027)","text":"<p>A French insurance company had its AI-assisted claims processing system halted by the CNIL (Commission Nationale de l'Informatique et des Libertes) for violations of Article 12 and Article 26.</p> <p>What happened. The company deployed an agentic AI system that retrieved policy documents, claims history, and medical guidelines to assist claims adjusters. The system cited a withdrawn clinical guideline in a denial decision, which was challenged by the policyholder. During the resulting investigation, regulators discovered that the company's log retention policy was 30 days -- well below the six-month minimum required by Article 26.</p> <p>What was missing.</p> <ul> <li>No version tracking on retrieved clinical guidelines -- the system could not   detect that the guideline had been superseded</li> <li>30-day log retention violated the six-month minimum under Article 26</li> <li>No tamper-evident logging -- the company could not demonstrate that logs had   not been altered after the complaint was filed</li> </ul> <p>How Provena would have prevented it. The <code>ProvenanceMetadata.version</code> field and freshness checking would have flagged the withdrawn guideline. The SQLite backend with OTel export provides persistent, tamper-evident storage that meets the six-month retention requirement. The <code>verify_chain()</code> method provides cryptographic proof that logs have not been modified.</p>"},{"location":"compliance/eu-ai-act/#next-steps","title":"Next Steps","text":"<ul> <li>OWASP ASI06: Context Poisoning -- Security controls for   context integrity</li> <li>OpenTelemetry Integration -- Configuring   OTel export for durable log retention</li> <li>Provenance Validation Guide -- Detailed configuration   for provenance requirements</li> </ul>"},{"location":"compliance/owasp-asi06/","title":"OWASP ASI06: Context Poisoning","text":"<p>OWASP ASI06 -- Memory &amp; Context Poisoning -- is identified as a top-10 risk in the OWASP Top 10 for Agentic AI Systems (2026). It addresses the class of attacks where adversaries manipulate the context, memory, or state information that AI agents rely on for decision-making. Provena provides the technical controls needed to detect, prevent, and audit context poisoning attacks.</p> <p>Impact of context poisoning</p> <p>Research on agentic AI systems demonstrates that context poisoning propagates rapidly and silently:</p> <ul> <li>A single poisoned context entry corrupts 87% of downstream decisions   within 4 hours</li> <li>48% of co-running agents are compromised during a single context injection</li> <li>40% of multi-agent system failures trace to state synchronization issues</li> </ul>"},{"location":"compliance/owasp-asi06/#the-threat","title":"The Threat","text":"<p>Agentic AI systems retrieve, store, and share context across multiple components and execution cycles. Unlike traditional prompt injection -- which targets a single inference call -- context poisoning targets the persistent state layer that agents depend on for continuity, memory, and multi-step reasoning.</p> <p>The consequences are severe because poisoned context is treated as trusted input by downstream components. An agent that retrieves a tampered document, stores a manipulated memory, or receives corrupted state from a peer agent will incorporate that poisoned context into all subsequent decisions without any indication that the input was compromised.</p>"},{"location":"compliance/owasp-asi06/#attack-vectors","title":"Attack Vectors","text":"<p>ASI06 identifies three primary attack vectors. Provena addresses each with specific technical controls.</p>"},{"location":"compliance/owasp-asi06/#1-context-poisoning","title":"1. Context Poisoning","text":"<p>The attack. An adversary injects fabricated or manipulated content into the context sources that an agent retrieves from. This includes poisoning vector store entries, modifying documents in retrieval corpora, or injecting malicious content through tool responses.</p> <p>How Provena detects it. The <code>ProvenanceValidator</code> requires every context input to carry verifiable origin metadata -- source URL, author, creation date, and version. Inputs from unknown or unverified sources are flagged with a provenance status of <code>MISSING</code> or <code>INCOMPLETE</code>:</p> <pre><code>from provena import ContextTrail\n\ntrail = ContextTrail(\n    required_fields=[\"source_url\", \"author\", \"created_at\"],\n    strict_mode=True,  # Raise on internal governance errors\n)\n\n# Context from a verified source -- accepted\nrecord = trail.log(\n    content=verified_document,\n    source=\"retriever\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://docs.internal.com/policy-v3\",\n        author=\"policy-team\",\n        created_at=datetime(2026, 6, 1, tzinfo=timezone.utc),\n        version=\"3.0\",\n    ),\n)\n# record.provenance_result.status -&gt; \"VALID\"\n\n# Context from an unknown source -- flagged\nrecord = trail.log(\n    content=suspicious_content,\n    source=\"retriever\",\n    # No provenance metadata provided\n)\n# record.provenance_result.status -&gt; \"MISSING\"\n# The record is still logged -- provenance status flags the gap for review\n</code></pre>"},{"location":"compliance/owasp-asi06/#2-memory-integrity-attacks","title":"2. Memory Integrity Attacks","text":"<p>The attack. An adversary modifies context entries after they have been validated and stored. This includes tampering with database records, altering cached retrieval results, or manipulating serialized agent state between execution cycles.</p> <p>How Provena detects it. The <code>ChainHasher</code> implements a Merkle-style hash chain using SHA-256 (or HMAC-SHA256 with a signing key). Each record's chain hash incorporates the previous record's hash, the content hash, the source type, and the timestamp. Modifying any record -- even a single byte -- invalidates all subsequent chain hashes:</p> <pre><code># Verify the entire chain at any time\nverdict = trail.verify_chain()\n\nif not verdict.intact:\n    print(f\"Tampering detected at record {verdict.broken_at}\")\n    print(f\"Chain broken after {verdict.total_records} records verified\")\n</code></pre> <p>HMAC signing for stronger guarantees</p> <p>For environments where an attacker might attempt to recompute the hash chain after tampering, enable HMAC-SHA256 signing. This requires knowledge of the signing key to produce valid chain hashes:</p> <pre><code>trail = ContextTrail(signing_key=\"your-secret-key\")\n# Or via environment variable: PROVENA_SIGNING_KEY\n</code></pre>"},{"location":"compliance/owasp-asi06/#3-multi-agent-contamination","title":"3. Multi-Agent Contamination","text":"<p>The attack. In multi-agent systems, a compromised agent shares poisoned context with peer agents through shared memory, message passing, or tool outputs. A single point of compromise can cascade across the entire agent network.</p> <p>How Provena addresses it. Provena's governance is framework-agnostic. The same <code>ContextTrail</code> instance (or separate instances sharing a storage backend) can govern context inputs across LangChain, LlamaIndex, CrewAI, and custom agent frameworks. Every context input -- regardless of which agent or framework produced it -- passes through the same validation pipeline:</p> <pre><code>from provena import ContextTrail, ContextSource\n\n# Single trail governing multiple agent types\ntrail = ContextTrail(storage_path=\"shared-governance.db\")\n\n# LangChain retriever output\ntrail.log(content=langchain_result, source=ContextSource.RETRIEVER)\n\n# LlamaIndex query response\ntrail.log(content=llamaindex_response, source=ContextSource.RETRIEVER)\n\n# CrewAI agent tool output\ntrail.log(content=crewai_tool_result, source=ContextSource.AGENT)\n\n# MCP server response\ntrail.log(content=mcp_response, source=ContextSource.MCP)\n</code></pre> <p>The <code>@trail.track()</code> decorator provides automatic governance for any function that returns context, supporting both synchronous and asynchronous execution:</p> <pre><code>@trail.track(source=ContextSource.TOOL, source_name=\"market-data-api\")\nasync def fetch_market_data(symbol: str) -&gt; dict:\n    \"\"\"Every return value is automatically logged and validated.\"\"\"\n    return await api.get_quote(symbol)\n</code></pre>"},{"location":"compliance/owasp-asi06/#how-provena-addresses-asi06","title":"How Provena Addresses ASI06","text":"<p>The following summarizes Provena's defense-in-depth approach to each ASI06 concern:</p> <p>Provenance validation catches context from unknown or unverified sources. The <code>ProvenanceValidator</code> enforces configurable metadata requirements on every context input. Sources that cannot provide origin metadata (URL, author, creation date) are flagged before they enter the agent's decision-making process. In <code>strict_mode</code>, unverified context is rejected entirely.</p> <p>Freshness checking catches stale injections. Context poisoning frequently involves injecting outdated information that contradicts current data. The <code>FreshnessChecker</code> detects stale inputs through both metadata timestamps and regex-based temporal pattern detection in content text. A poisoned entry referencing \"Q1 2023\" data when the threshold is 90 days will be flagged as <code>STALE</code>.</p> <p>Hash chain detects tampering. The SHA-256 hash chain provides cryptographic evidence of whether any record has been modified after creation. The <code>verify_chain()</code> method performs a full chain walk from the genesis hash forward, identifying the exact record where integrity was broken.</p> <p>Framework-agnostic design works across all agent types. Provena governs context at the data layer, not the framework layer. It integrates with LangChain (via callback handlers), LlamaIndex (via node postprocessors), and any custom framework through the direct <code>trail.log()</code> and <code>@trail.track()</code> APIs. This ensures consistent governance even in heterogeneous multi-agent deployments.</p>"},{"location":"compliance/owasp-asi06/#owasp-compliance-mapping","title":"OWASP Compliance Mapping","text":"Requirement Standard Provena Feature Validate context source authenticity ASI06-01 <code>ProvenanceValidator</code> with configurable required fields (<code>source_url</code>, <code>author</code>, <code>created_at</code>) Detect unauthorized context modifications ASI06-02 SHA-256 / HMAC-SHA256 hash chain with <code>verify_chain()</code> full-chain integrity verification Prevent stale data injection ASI06-03 <code>FreshnessChecker</code> with metadata timestamp validation and temporal pattern detection Maintain audit trail of context inputs ASI06-04 <code>TrailRecord</code> with content hash, provenance status, freshness status, and chain hash Enable forensic analysis of context flow ASI06-05 <code>trail.query()</code> with filtering by source, time range, provenance status, and freshness status Support human review of flagged inputs ASI06-06 <code>trail.annotate()</code> for attaching reviewer decisions and notes to records Cross-framework context governance ASI06-07 Framework-agnostic design with LangChain, LlamaIndex, and direct API integrations Cryptographic integrity verification ASI06-08 Merkle-style chain with constant-time comparison (<code>hmac.compare_digest</code>) to prevent timing attacks Export governance data for external analysis ASI06-09 <code>trail.export()</code> in JSON and CSV formats; OTel span export for centralized monitoring Operational health monitoring ASI06-10 <code>trail.health()</code> returns backend status, record count, signing state, and error count"},{"location":"compliance/owasp-asi06/#next-steps","title":"Next Steps","text":"<ul> <li>EU AI Act Compliance -- Regulatory mapping for high-risk AI   systems under European law</li> <li>Chain Verification Guide -- Detailed guide to   hash chain configuration and verification</li> <li>Provenance Validation Guide -- Configuring   provenance requirements for your deployment</li> </ul>"},{"location":"guide/configuration/","title":"Configuration","text":"<p>Provena is configured through the <code>ContextTrail</code> constructor. Every parameter has a sensible default, so the simplest setup is just <code>ContextTrail()</code>. This page documents every parameter and shows how to use the <code>config</code> dict for structured configuration.</p>"},{"location":"guide/configuration/#constructor-parameters","title":"Constructor Parameters","text":""},{"location":"guide/configuration/#storage_path","title":"storage_path","text":"<p>Path to the SQLite database file.</p> <ul> <li>Type: <code>str</code></li> <li>Default: <code>\"provena.db\"</code></li> </ul> <pre><code>from provena import ContextTrail\n\ntrail = ContextTrail(storage_path=\"governance/audit.db\")\n</code></pre>"},{"location":"guide/configuration/#backend","title":"backend","text":"<p>Storage backend type.</p> <ul> <li>Type: <code>str</code></li> <li>Default: <code>\"sqlite\"</code></li> <li>Options: <code>\"sqlite\"</code>, <code>\"memory\"</code></li> </ul> <pre><code># Persistent storage (default)\ntrail = ContextTrail(backend=\"sqlite\")\n\n# In-memory storage for testing\ntrail = ContextTrail(backend=\"memory\")\n</code></pre>"},{"location":"guide/configuration/#required_fields","title":"required_fields","text":"<p>Provenance fields required for a VALID verdict. See the Provenance Validation guide for details.</p> <ul> <li>Type: <code>list[str]</code> or <code>None</code></li> <li>Default: <code>[\"source_url\", \"created_at\"]</code></li> </ul> <pre><code>trail = ContextTrail(\n    backend=\"memory\",\n    required_fields=[\"source_url\", \"author\", \"version\"],\n)\n</code></pre>"},{"location":"guide/configuration/#max_age_days","title":"max_age_days","text":"<p>Content older than this many days is marked STALE. See the Freshness Checking guide.</p> <ul> <li>Type: <code>int</code></li> <li>Default: <code>90</code></li> </ul> <pre><code>trail = ContextTrail(backend=\"memory\", max_age_days=30)\n</code></pre> <p>Must be at least 1. Raises <code>ValueError</code> otherwise.</p>"},{"location":"guide/configuration/#temporal_detection","title":"temporal_detection","text":"<p>Enable regex-based date detection in content text when no metadata timestamp is available.</p> <ul> <li>Type: <code>bool</code></li> <li>Default: <code>True</code></li> </ul> <pre><code>trail = ContextTrail(backend=\"memory\", temporal_detection=False)\n</code></pre>"},{"location":"guide/configuration/#max_content_bytes","title":"max_content_bytes","text":"<p>Maximum content size in bytes before truncation. Content exceeding this limit is truncated and the record's <code>truncated</code> flag is set to <code>True</code>.</p> <ul> <li>Type: <code>int</code></li> <li>Default: <code>65536</code> (64 KB)</li> </ul> <pre><code>trail = ContextTrail(backend=\"memory\", max_content_bytes=131072)  # 128 KB\n</code></pre> <p>Must be at least 1. Raises <code>ValueError</code> otherwise.</p>"},{"location":"guide/configuration/#signing_key","title":"signing_key","text":"<p>HMAC key for signed hash chains. When set, chain hashes use HMAC-SHA256 instead of plain SHA-256. See the Chain Verification guide.</p> <ul> <li>Type: <code>str</code>, <code>bytes</code>, or <code>None</code></li> <li>Default: <code>None</code> (falls back to <code>PROVENA_SIGNING_KEY</code> env var)</li> </ul> <pre><code>trail = ContextTrail(backend=\"memory\", signing_key=\"my-secret-key\")\n</code></pre>"},{"location":"guide/configuration/#otel_enabled","title":"otel_enabled","text":"<p>Emit OpenTelemetry spans for each logged entry. Requires the <code>opentelemetry-api</code> and <code>opentelemetry-sdk</code> packages.</p> <ul> <li>Type: <code>bool</code></li> <li>Default: <code>False</code></li> </ul> <pre><code>trail = ContextTrail(backend=\"memory\", otel_enabled=True)\n</code></pre>"},{"location":"guide/configuration/#otel_service_name","title":"otel_service_name","text":"<p>Service name used in OpenTelemetry spans.</p> <ul> <li>Type: <code>str</code></li> <li>Default: <code>\"provena\"</code></li> </ul> <pre><code>trail = ContextTrail(\n    backend=\"memory\",\n    otel_enabled=True,\n    otel_service_name=\"my-agent-service\",\n)\n</code></pre>"},{"location":"guide/configuration/#strict_mode","title":"strict_mode","text":"<p>When <code>True</code>, governance errors (provenance validation failures, storage errors) propagate as exceptions instead of being silently logged.</p> <ul> <li>Type: <code>bool</code></li> <li>Default: <code>False</code></li> </ul> <pre><code># Non-strict (default): errors are logged, trail.log() returns None\ntrail = ContextTrail(backend=\"memory\", strict_mode=False)\n\n# Strict: errors raise exceptions\ntrail = ContextTrail(backend=\"memory\", strict_mode=True)\n</code></pre> <p>Use strict mode in tests</p> <p>Set <code>strict_mode=True</code> in your test suite to catch governance errors early. In production, the default non-strict mode prevents governance from blocking your agent's primary workflow.</p>"},{"location":"guide/configuration/#on_error","title":"on_error","text":"<p>Optional callback invoked when a governance error occurs. Receives the exception as its argument. Called regardless of <code>strict_mode</code>.</p> <ul> <li>Type: <code>Callable[[Exception], None]</code> or <code>None</code></li> <li>Default: <code>None</code></li> </ul> <pre><code>errors = []\n\ntrail = ContextTrail(\n    backend=\"memory\",\n    on_error=lambda exc: errors.append(exc),\n)\n</code></pre>"},{"location":"guide/configuration/#config","title":"config","text":"<p>A dictionary that overrides all other constructor parameters. Useful for loading configuration from files (YAML, JSON, TOML).</p> <ul> <li>Type: <code>dict[str, Any]</code> or <code>None</code></li> <li>Default: <code>None</code></li> </ul> <p>When <code>config</code> is provided, all other parameters are ignored.</p>"},{"location":"guide/configuration/#config-dict-structure","title":"Config Dict Structure","text":"<p>The <code>config</code> dict uses nested sections for organized configuration:</p> <pre><code>from provena import ContextTrail\n\ntrail = ContextTrail(config={\n    \"storage\": {\n        \"backend\": \"sqlite\",\n        \"path\": \"governance/audit.db\",\n    },\n    \"provenance\": {\n        \"required_fields\": [\"source_url\", \"created_at\", \"author\"],\n    },\n    \"freshness\": {\n        \"max_age_days\": 60,\n        \"temporal_detection\": True,\n    },\n    \"hash_chain\": {\n        \"signing_key\": \"my-secret-key\",\n    },\n    \"otel\": {\n        \"enabled\": True,\n        \"service_name\": \"my-agent\",\n    },\n    \"max_content_bytes\": 131072,\n    \"strict_mode\": True,\n})\n</code></pre>"},{"location":"guide/configuration/#config-dict-reference","title":"Config dict reference","text":"Section Key Maps to parameter Default <code>storage</code> <code>backend</code> <code>backend</code> <code>\"sqlite\"</code> <code>storage</code> <code>path</code> <code>storage_path</code> <code>\"provena.db\"</code> <code>provenance</code> <code>required_fields</code> <code>required_fields</code> <code>[\"source_url\", \"created_at\"]</code> <code>freshness</code> <code>max_age_days</code> <code>max_age_days</code> <code>90</code> <code>freshness</code> <code>temporal_detection</code> <code>temporal_detection</code> <code>True</code> <code>hash_chain</code> <code>signing_key</code> <code>signing_key</code> <code>None</code> <code>otel</code> <code>enabled</code> <code>otel_enabled</code> <code>False</code> <code>otel</code> <code>service_name</code> <code>otel_service_name</code> <code>\"provena\"</code> (top-level) <code>max_content_bytes</code> <code>max_content_bytes</code> <code>65536</code> (top-level) <code>strict_mode</code> <code>strict_mode</code> <code>False</code>"},{"location":"guide/configuration/#loading-from-a-yaml-file","title":"Loading from a YAML file","text":"<pre><code>import yaml\nfrom provena import ContextTrail\n\nwith open(\"provena.yml\") as f:\n    config = yaml.safe_load(f)\n\ntrail = ContextTrail(config=config)\n</code></pre> <p>Example <code>provena.yml</code>:</p> <pre><code>storage:\n  backend: sqlite\n  path: governance/audit.db\n\nprovenance:\n  required_fields:\n    - source_url\n    - created_at\n\nfreshness:\n  max_age_days: 60\n  temporal_detection: true\n\nhash_chain:\n  signing_key: ${PROVENA_SIGNING_KEY}\n\notel:\n  enabled: false\n  service_name: my-agent\n\nmax_content_bytes: 65536\nstrict_mode: false\n</code></pre>"},{"location":"guide/configuration/#environment-variables","title":"Environment Variables","text":"<p>Provena reads these environment variables:</p> Variable Purpose Default <code>PROVENA_SIGNING_KEY</code> HMAC signing key for hash chains (none) <code>PROVENA_DISABLED</code> Set to <code>1</code>, <code>true</code>, or <code>yes</code> to disable governance entirely (none) <code>PROVENA_DB</code> Database path for the CLI <code>\"provena.db\"</code>"},{"location":"guide/configuration/#provena_signing_key","title":"PROVENA_SIGNING_KEY","text":"<p>Used as the HMAC key when no <code>signing_key</code> parameter is passed to the constructor. The constructor parameter takes precedence.</p> <pre><code>export PROVENA_SIGNING_KEY=\"production-signing-key-2025\"\n</code></pre>"},{"location":"guide/configuration/#provena_disabled","title":"PROVENA_DISABLED","text":"<p>When set, all <code>@trail.track()</code> decorators become no-ops and the in-memory backend is used regardless of the <code>backend</code> parameter. Useful for disabling governance in development or CI environments where audit overhead is unwanted.</p> <pre><code>export PROVENA_DISABLED=1\n</code></pre>"},{"location":"guide/configuration/#provena_db","title":"PROVENA_DB","text":"<p>Sets the default database path for the CLI tool. The <code>--db</code> flag overrides this.</p> <pre><code>export PROVENA_DB=/var/lib/provena/audit.db\nprovena verify  # Uses /var/lib/provena/audit.db\n</code></pre>"},{"location":"guide/freshness/","title":"Freshness Checking","text":"<p>Stale context is a silent failure mode in AI agent systems. Research shows that feeding outdated information to LLMs degrades output quality by 12-15%, often without any visible error. Provena detects stale context automatically and flags it in the audit trail so you can act on it.</p>"},{"location":"guide/freshness/#two-detection-methods","title":"Two Detection Methods","text":"<p>Provena uses two complementary strategies to determine content age:</p> <ol> <li> <p>Metadata timestamps -- If the entry has <code>ProvenanceMetadata</code> with a    <code>created_at</code> field, that date is used directly. This is the most reliable    method.</p> </li> <li> <p>Regex temporal detection -- When no metadata timestamp is available,    Provena scans the content text for date patterns (ISO dates, month-year    phrases, quarter references, and more). This catches staleness even when    upstream sources do not provide structured metadata.</p> </li> </ol> <p>Metadata timestamps always take priority. If <code>created_at</code> is present, content-based detection is skipped entirely.</p>"},{"location":"guide/freshness/#the-max_age_days-parameter","title":"The max_age_days Parameter","text":"<p>The <code>max_age_days</code> parameter sets the staleness threshold. Content older than this many days is marked <code>STALE</code>. The default is 90 days.</p> <pre><code>from provena import ContextTrail\n\n# Default: 90 days\ntrail = ContextTrail(backend=\"memory\")\n\n# Strict freshness: 30 days\ntrail_strict = ContextTrail(backend=\"memory\", max_age_days=30)\n\n# Relaxed freshness: 365 days\ntrail_relaxed = ContextTrail(backend=\"memory\", max_age_days=365)\n</code></pre>"},{"location":"guide/freshness/#freshness-verdicts","title":"Freshness Verdicts","text":"<p>Every logged entry receives one of three freshness verdicts:</p> Verdict Meaning FRESH Content age is within the <code>max_age_days</code> threshold STALE Content age exceeds the threshold UNKNOWN No timestamp metadata and no temporal markers were detected <pre><code>from provena import ContextTrail, ProvenanceMetadata\nfrom datetime import datetime, timezone\n\ntrail = ContextTrail(backend=\"memory\", max_age_days=90)\n\n# FRESH -- created 10 days ago\ntrail.log(\n    content=\"Recent deployment guide.\",\n    source=\"retriever\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://docs.example.com/deploy\",\n        created_at=datetime(2026, 7, 8, tzinfo=timezone.utc),\n    ),\n)\n\n# STALE -- created over 90 days ago\ntrail.log(\n    content=\"Legacy migration notes.\",\n    source=\"retriever\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://docs.example.com/migrate\",\n        created_at=datetime(2024, 1, 15, tzinfo=timezone.utc),\n    ),\n)\n\n# UNKNOWN -- no timestamp, no temporal markers in content\ntrail.log(\n    content=\"Some context with no date information.\",\n    source=\"retriever\",\n)\n\nsummary = trail.summary()\nprint(summary[\"freshness\"])\n# {'FRESH': 1, 'STALE': 1, 'UNKNOWN': 1}\n</code></pre>"},{"location":"guide/freshness/#temporal-patterns-detected","title":"Temporal Patterns Detected","text":"<p>When metadata timestamps are absent, Provena scans content text for these patterns:</p> Pattern Type Examples ISO dates <code>2023-06-15</code>, <code>2024/01/30</code> Month-year <code>January 2024</code>, <code>Mar 2023</code>, <code>September 2025</code> Quarters <code>Q3 2024</code>, <code>Q1 2025</code> Half-years <code>H1 2025</code>, <code>H2 2024</code> Contextual years <code>as of 2023</code>, <code>since 2024</code>, <code>circa 2025</code> Update markers <code>last updated: March 2024</code>, <code>published on 2024-01-15</code>, <code>revised December 2023</code>, <code>effective date: 2024-03-01</code> <pre><code>trail = ContextTrail(backend=\"memory\", max_age_days=180)\n\n# Temporal detection finds \"Q3 2024\" in the content\nrecord = trail.log(\n    content=\"This report covers Q3 2024 financial results.\",\n    source=\"retriever\",\n)\n# Freshness is evaluated based on Q3 2024 (July 1, 2024)\n\n# \"last updated: March 2024\" is detected\nrecord = trail.log(\n    content=\"API reference (last updated: March 2024) for the billing service.\",\n    source=\"tool:api_docs\",\n)\n# Freshness is evaluated based on March 2024\n</code></pre> <p>Oldest date wins</p> <p>When multiple temporal markers are found in a single piece of content, Provena uses the oldest detected date for the freshness calculation. This is a conservative approach that avoids false FRESH verdicts.</p>"},{"location":"guide/freshness/#disabling-temporal-detection","title":"Disabling Temporal Detection","text":"<p>If regex-based detection produces false positives for your content (for example, documents that discuss historical dates), disable it:</p> <pre><code>trail = ContextTrail(\n    backend=\"memory\",\n    temporal_detection=False,\n)\n\n# This content mentions \"2019\" but it will NOT be flagged as STALE\n# because temporal detection is off. Verdict will be UNKNOWN.\nrecord = trail.log(\n    content=\"The protocol was first standardized in 2019.\",\n    source=\"retriever\",\n)\n</code></pre> <p>With temporal detection disabled, only <code>ProvenanceMetadata.created_at</code> timestamps are used. Entries without metadata timestamps receive an UNKNOWN verdict.</p>"},{"location":"guide/freshness/#priority-metadata-overrides-content-detection","title":"Priority: Metadata Overrides Content Detection","text":"<p>When both a <code>created_at</code> metadata timestamp and content temporal markers are present, the metadata timestamp is used and content scanning is skipped:</p> <pre><code>from datetime import datetime, timezone\n\ntrail = ContextTrail(backend=\"memory\", max_age_days=90)\n\n# The content mentions \"2019\" but the metadata says it was created recently.\n# Freshness is based on created_at, not the content text.\nrecord = trail.log(\n    content=\"The TCP/IP protocol was standardized in 2019.\",\n    source=\"retriever\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://docs.example.com/networking\",\n        created_at=datetime(2026, 7, 1, tzinfo=timezone.utc),\n    ),\n)\n# Verdict: FRESH (based on metadata, not content)\n</code></pre>"},{"location":"guide/freshness/#checking-freshness-in-trailsummary","title":"Checking Freshness in trail.summary()","text":"<p>The <code>summary()</code> method includes a freshness breakdown:</p> <pre><code>summary = trail.summary()\nprint(summary[\"freshness\"])\n# {'FRESH': 25, 'STALE': 3, 'UNKNOWN': 12}\n</code></pre> <p>You can also query records filtered by freshness status:</p> <pre><code>stale_records = trail.query(freshness_status=\"STALE\", limit=50)\nfor record in stale_records:\n    print(f\"Stale: {record['source_name']} ({record['timestamp']})\")\n</code></pre> <p>Monitoring freshness over time</p> <p>Track the ratio of STALE to FRESH entries in your trail summary. A rising STALE count indicates that your knowledge sources need updating. Combine with the CLI <code>provena --db audit.db report</code> command for periodic compliance reports.</p>"},{"location":"guide/provenance/","title":"Provenance Validation","text":"<p>Provenance metadata records where a piece of context came from -- its origin URL, author, creation date, and version. Provena validates this metadata on every logged entry and assigns a verdict that tells you whether the context has a trustworthy lineage.</p>"},{"location":"guide/provenance/#provenancemetadata","title":"ProvenanceMetadata","text":"<p>The <code>ProvenanceMetadata</code> dataclass carries origin information for a context input:</p> <pre><code>from provena import ProvenanceMetadata\nfrom datetime import datetime, timezone\n\nprovenance = ProvenanceMetadata(\n    source_url=\"https://docs.example.com/api/v2\",\n    author=\"Platform Team\",\n    created_at=datetime(2025, 3, 15, tzinfo=timezone.utc),\n    version=\"2.1.0\",\n    extra={\"department\": \"engineering\", \"review_status\": \"approved\"},\n)\n</code></pre> Field Type Description <code>source_url</code> <code>str</code> or <code>None</code> URL where the content was retrieved <code>author</code> <code>str</code> or <code>None</code> Author or creator of the content <code>created_at</code> <code>datetime</code> or <code>None</code> When the content was originally published <code>version</code> <code>str</code> or <code>None</code> Version identifier <code>extra</code> <code>dict[str, Any]</code> Arbitrary additional metadata"},{"location":"guide/provenance/#validation-verdicts","title":"Validation Verdicts","text":"<p>Every logged entry receives one of three provenance verdicts:</p> Verdict Meaning VALID All required fields are present and non-empty MISSING No <code>ProvenanceMetadata</code> was attached at all INCOMPLETE Metadata was attached but one or more required fields are absent <pre><code>from provena import ContextTrail, ProvenanceMetadata\nfrom datetime import datetime, timezone\n\ntrail = ContextTrail(backend=\"memory\")\n\n# MISSING -- no provenance provided\ntrail.log(content=\"Some text\", source=\"retriever\")\n\n# INCOMPLETE -- source_url present but created_at is missing\ntrail.log(\n    content=\"Some text\",\n    source=\"retriever\",\n    provenance=ProvenanceMetadata(source_url=\"https://example.com\"),\n)\n\n# VALID -- both default required fields present\ntrail.log(\n    content=\"Some text\",\n    source=\"retriever\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://example.com\",\n        created_at=datetime(2025, 6, 1, tzinfo=timezone.utc),\n    ),\n)\n\nsummary = trail.summary()\nprint(summary[\"provenance\"])\n# {'MISSING': 1, 'INCOMPLETE': 1, 'VALID': 1}\n</code></pre>"},{"location":"guide/provenance/#default-required-fields","title":"Default Required Fields","text":"<p>By default, Provena requires two fields for a VALID verdict:</p> <ul> <li><code>source_url</code> -- where the content was retrieved</li> <li><code>created_at</code> -- when the content was created or published</li> </ul> <p>If either is <code>None</code> or an empty string, the verdict is INCOMPLETE.</p>"},{"location":"guide/provenance/#custom-required-fields","title":"Custom Required Fields","text":"<p>Override the defaults by passing <code>required_fields</code> to <code>ContextTrail</code>:</p> <pre><code>trail = ContextTrail(\n    backend=\"memory\",\n    required_fields=[\"source_url\", \"author\", \"version\"],\n)\n\n# INCOMPLETE -- author and version are missing\nrecord = trail.log(\n    content=\"API response payload\",\n    source=\"tool:api\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://api.example.com/v2/data\",\n    ),\n)\n\n# VALID -- all three custom fields present\nrecord = trail.log(\n    content=\"API response payload\",\n    source=\"tool:api\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://api.example.com/v2/data\",\n        author=\"data-service\",\n        version=\"2.4.1\",\n    ),\n)\n</code></pre>"},{"location":"guide/provenance/#attaching-provenance-to-traillog","title":"Attaching Provenance to trail.log()","text":"<p>Pass a <code>ProvenanceMetadata</code> instance to the <code>provenance</code> parameter:</p> <pre><code>from provena import ContextTrail, ProvenanceMetadata\nfrom datetime import datetime, timezone\n\ntrail = ContextTrail(backend=\"memory\")\n\nrecord = trail.log(\n    content=\"Kubernetes 1.30 deprecates PodSecurityPolicy.\",\n    source=\"retriever\",\n    source_name=\"changelog_db\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://kubernetes.io/blog/2024/k8s-1.30\",\n        author=\"Kubernetes Release Team\",\n        created_at=datetime(2024, 4, 17, tzinfo=timezone.utc),\n        version=\"1.30\",\n    ),\n)\n\nprint(record.provenance_result.status)  # \"VALID\"\n</code></pre>"},{"location":"guide/provenance/#auto-extraction-from-langchain-documents","title":"Auto-extraction from LangChain Documents","text":"<p>When using <code>@trail.track()</code> with objects that have a <code>.metadata</code> dictionary, Provena automatically builds <code>ProvenanceMetadata</code> from recognized keys:</p> <pre><code>class Document:\n    def __init__(self, page_content: str, metadata: dict):\n        self.page_content = page_content\n        self.metadata = metadata\n\n@trail.track(source=\"retriever\")\ndef retrieve(query: str) -&gt; Document:\n    return Document(\n        page_content=\"Horizontal pod autoscaling adjusts replica count.\",\n        metadata={\n            \"source\": \"https://k8s.io/docs/hpa\",\n            \"author\": \"SIG Autoscaling\",\n        },\n    )\n\ndoc = retrieve(\"autoscaling\")\n</code></pre> <p>The following <code>.metadata</code> keys are mapped automatically:</p> Document metadata key ProvenanceMetadata field <code>source</code> or <code>source_url</code> <code>source_url</code> <code>author</code> <code>author</code> <code>version</code> <code>version</code> <p>LlamaIndex nodes</p> <p>LlamaIndex nodes follow the same pattern. The <code>metadata</code> dict keys <code>source</code>, <code>file_path</code>, and <code>author</code> are extracted automatically when using the <code>ProvenaPostprocessor</code> integration.</p>"},{"location":"guide/provenance/#auto-extraction-from-llamaindex-nodes","title":"Auto-extraction from LlamaIndex Nodes","text":"<p>The <code>ProvenaPostprocessor</code> extracts provenance from LlamaIndex node metadata:</p> <pre><code>from provena import ContextTrail\nfrom provena.integrations.llamaindex import ProvenaPostprocessor\n\ntrail = ContextTrail(backend=\"memory\")\npostprocessor = ProvenaPostprocessor(trail=trail)\n\n# Nodes with metadata like {\"source\": \"...\", \"author\": \"...\"}\n# will have ProvenanceMetadata created automatically.\n</code></pre> Node metadata key ProvenanceMetadata field <code>source</code> or <code>file_path</code> <code>source_url</code> <code>author</code> <code>author</code>"},{"location":"guide/provenance/#checking-provenance-in-trailsummary","title":"Checking Provenance in trail.summary()","text":"<p>The <code>summary()</code> method provides an aggregate breakdown of provenance verdicts across all records:</p> <pre><code>summary = trail.summary()\nprint(summary[\"provenance\"])\n# Example output:\n# {'VALID': 42, 'INCOMPLETE': 3, 'MISSING': 7}\n</code></pre> <p>Use this to monitor governance health over time. A high MISSING count indicates that context sources are not providing origin metadata.</p>"},{"location":"guide/provenance/#serialization","title":"Serialization","text":"<p><code>ProvenanceMetadata</code> supports round-trip serialization:</p> <pre><code>from provena import ProvenanceMetadata\nfrom datetime import datetime, timezone\n\noriginal = ProvenanceMetadata(\n    source_url=\"https://example.com/doc\",\n    author=\"Engineering\",\n    created_at=datetime(2025, 1, 15, tzinfo=timezone.utc),\n    version=\"3.0\",\n    extra={\"classification\": \"internal\"},\n)\n\n# Serialize to dict\ndata = original.to_dict()\nprint(data)\n# {\n#     'source_url': 'https://example.com/doc',\n#     'author': 'Engineering',\n#     'created_at': '2025-01-15T00:00:00+00:00',\n#     'version': '3.0',\n#     'extra': {'classification': 'internal'}\n# }\n\n# Deserialize back\nrestored = ProvenanceMetadata.from_dict(data)\nassert restored.source_url == original.source_url\nassert restored.created_at == original.created_at\n</code></pre> <p>Storage format</p> <p>Provena stores provenance as JSON in the audit database. The <code>to_dict()</code> method omits fields that are <code>None</code>, keeping storage compact. The <code>from_dict()</code> class method handles ISO-format datetime strings automatically.</p>"},{"location":"guide/testing/","title":"Testing","text":"<p>Provena is designed to be easy to test. The in-memory backend eliminates filesystem dependencies, the <code>PROVENA_DISABLED</code> environment variable provides a kill switch, and <code>strict_mode</code> surfaces governance errors as exceptions that your test framework can catch.</p>"},{"location":"guide/testing/#inmemorybackend-for-fast-tests","title":"InMemoryBackend for Fast Tests","text":"<p>Use <code>backend=\"memory\"</code> to run tests without creating database files. The in-memory backend is functionally identical to SQLite but stores records in a Python list.</p> <pre><code>from provena import ContextTrail\n\ndef test_basic_logging():\n    trail = ContextTrail(backend=\"memory\")\n\n    record = trail.log(content=\"Test content\", source=\"retriever\")\n\n    assert record is not None\n    assert record.entry.source.value == \"retriever\"\n    assert trail.summary()[\"total\"] == 1\n</code></pre> <p>No files are created, no cleanup is needed, and tests run fast.</p>"},{"location":"guide/testing/#disabling-governance-with-provena_disabled","title":"Disabling Governance with PROVENA_DISABLED","text":"<p>Set the <code>PROVENA_DISABLED</code> environment variable to <code>1</code> to disable all governance. When disabled:</p> <ul> <li>All <code>@trail.track()</code> decorators become pass-through no-ops</li> <li>The in-memory backend is used regardless of configuration</li> <li>Decorated functions still execute and return their values normally</li> </ul> <pre><code>import os\n\ndef test_with_governance_disabled(monkeypatch):\n    monkeypatch.setenv(\"PROVENA_DISABLED\", \"1\")\n\n    # Import after setting the env var, or reload the module\n    from provena import ContextTrail\n\n    trail = ContextTrail()\n    # Trail uses InMemoryBackend even though no backend=\"memory\" was passed\n</code></pre> <p>When to disable</p> <p>Use <code>PROVENA_DISABLED=1</code> in development environments or CI jobs where governance overhead is unwanted. In your test suite, prefer <code>backend=\"memory\"</code> instead so you can still assert on governance behavior.</p>"},{"location":"guide/testing/#context-manager-pattern","title":"Context Manager Pattern","text":"<p>Use <code>ContextTrail</code> as a context manager to ensure the storage backend is closed after each test, even if the test fails:</p> <pre><code>from provena import ContextTrail, ProvenanceMetadata\nfrom datetime import datetime, timezone\n\ndef test_provenance_validation():\n    with ContextTrail(backend=\"memory\") as trail:\n        record = trail.log(\n            content=\"Governed content\",\n            source=\"retriever\",\n            provenance=ProvenanceMetadata(\n                source_url=\"https://example.com\",\n                created_at=datetime(2026, 6, 1, tzinfo=timezone.utc),\n            ),\n        )\n\n        assert record.provenance_result.status == \"VALID\"\n        assert record.freshness_result.status == \"FRESH\"\n    # trail.close() is called automatically\n</code></pre> <p>This is especially important when using the SQLite backend in tests, where unclosed connections can cause file-locking issues.</p>"},{"location":"guide/testing/#testing-with-strict_mode","title":"Testing with strict_mode","text":"<p>Enable <code>strict_mode=True</code> to turn governance errors into exceptions. In non-strict mode (the default), errors are logged and <code>trail.log()</code> returns <code>None</code>, which can mask issues in tests.</p> <pre><code>import pytest\nfrom provena import ContextTrail\n\ndef test_strict_mode_catches_errors():\n    trail = ContextTrail(backend=\"memory\", strict_mode=True)\n\n    # Valid usage works normally\n    record = trail.log(content=\"Valid content\", source=\"retriever\")\n    assert record is not None\n</code></pre> <p>With strict mode on, any internal governance error will raise rather than returning <code>None</code>, giving you immediate feedback in your test output.</p>"},{"location":"guide/testing/#verifying-chain-integrity-in-tests","title":"Verifying Chain Integrity in Tests","text":"<p>After logging multiple entries, verify that the hash chain is intact:</p> <pre><code>from provena import ContextTrail\n\ndef test_chain_integrity():\n    trail = ContextTrail(backend=\"memory\")\n\n    for i in range(10):\n        trail.log(content=f\"Entry {i}\", source=\"retriever\")\n\n    verdict = trail.verify_chain()\n\n    assert verdict.intact is True\n    assert verdict.total_records == 10\n    assert verdict.broken_at is None\n</code></pre> <p>Test with HMAC signing to verify signed chains:</p> <pre><code>def test_signed_chain():\n    trail = ContextTrail(\n        backend=\"memory\",\n        signing_key=\"test-signing-key\",\n    )\n\n    trail.log(content=\"Signed entry 1\", source=\"retriever\")\n    trail.log(content=\"Signed entry 2\", source=\"tool:api\")\n\n    assert trail.is_signed is True\n\n    verdict = trail.verify_chain()\n    assert verdict.intact is True\n</code></pre>"},{"location":"guide/testing/#checking-summary-counts-in-assertions","title":"Checking Summary Counts in Assertions","text":"<p>The <code>summary()</code> method returns a dictionary you can assert against:</p> <pre><code>from provena import ContextTrail, ProvenanceMetadata\nfrom datetime import datetime, timezone\n\ndef test_summary_counts():\n    trail = ContextTrail(backend=\"memory\")\n\n    # Log entries with different provenance states\n    trail.log(content=\"No provenance\", source=\"retriever\")\n\n    trail.log(\n        content=\"With full provenance\",\n        source=\"retriever\",\n        provenance=ProvenanceMetadata(\n            source_url=\"https://example.com\",\n            created_at=datetime(2026, 7, 1, tzinfo=timezone.utc),\n        ),\n    )\n\n    trail.log(\n        content=\"Incomplete provenance\",\n        source=\"tool:api\",\n        provenance=ProvenanceMetadata(source_url=\"https://example.com\"),\n    )\n\n    summary = trail.summary()\n\n    # Total count\n    assert summary[\"total\"] == 3\n\n    # Provenance breakdown\n    assert summary[\"provenance\"][\"MISSING\"] == 1\n    assert summary[\"provenance\"][\"VALID\"] == 1\n    assert summary[\"provenance\"][\"INCOMPLETE\"] == 1\n\n    # Source breakdown\n    assert summary[\"sources\"][\"retriever\"] == 2\n    assert summary[\"sources\"][\"tool\"] == 1\n\n    # Freshness breakdown\n    assert \"freshness\" in summary\n</code></pre>"},{"location":"guide/testing/#using-on_error-for-test-assertions","title":"Using on_error for Test Assertions","text":"<p>The <code>on_error</code> callback lets you capture governance errors without raising exceptions. This is useful when you want to verify that specific error conditions are handled:</p> <pre><code>from provena import ContextTrail\n\ndef test_error_callback():\n    captured_errors = []\n\n    trail = ContextTrail(\n        backend=\"memory\",\n        on_error=lambda exc: captured_errors.append(exc),\n    )\n\n    # Normal operations should not trigger errors\n    trail.log(content=\"Valid content\", source=\"retriever\")\n    assert len(captured_errors) == 0\n\n    # Verify the error count property\n    assert trail.error_count == 0\n</code></pre> <p>Combine <code>on_error</code> with <code>strict_mode=False</code> (the default) to collect errors without halting execution:</p> <pre><code>def test_error_collection():\n    errors = []\n\n    trail = ContextTrail(\n        backend=\"memory\",\n        strict_mode=False,\n        on_error=lambda exc: errors.append(str(exc)),\n    )\n\n    # Log multiple entries and check that no errors accumulated\n    for i in range(5):\n        trail.log(content=f\"Entry {i}\", source=\"retriever\")\n\n    assert trail.error_count == 0\n    assert len(errors) == 0\n</code></pre>"},{"location":"guide/testing/#pytest-fixture-example","title":"Pytest Fixture Example","text":"<p>A reusable pytest fixture that provides a fresh in-memory trail for each test:</p> <pre><code>import pytest\nfrom provena import ContextTrail\n\n@pytest.fixture\ndef trail():\n    \"\"\"Provide a fresh in-memory ContextTrail for each test.\"\"\"\n    with ContextTrail(backend=\"memory\", strict_mode=True) as t:\n        yield t\n\ndef test_tracking_decorator(trail):\n    @trail.track(source=\"retriever\")\n    def search(query: str) -&gt; list[str]:\n        return [\"Result one\", \"Result two\"]\n\n    results = search(\"test query\")\n\n    assert len(results) == 2\n    assert trail.summary()[\"total\"] == 2\n    assert trail.verify_chain().intact is True\n\ndef test_provenance_required(trail):\n    record = trail.log(content=\"No metadata\", source=\"retriever\")\n    assert record.provenance_result.status == \"MISSING\"\n</code></pre> <p>Fixture with custom configuration</p> <p>Parameterize the fixture for tests that need different settings:</p> <pre><code>@pytest.fixture\ndef strict_trail():\n    with ContextTrail(\n        backend=\"memory\",\n        strict_mode=True,\n        max_age_days=30,\n        required_fields=[\"source_url\", \"author\"],\n    ) as t:\n        yield t\n</code></pre>"},{"location":"guide/tracking/","title":"Tracking Context","text":"<p>Provena tracks every piece of context that flows into your AI agent. The primary interface is the <code>@trail.track()</code> decorator, which automatically logs function return values to the audit trail. For cases where a decorator does not fit, <code>trail.log()</code> provides equivalent functionality as a direct call.</p>"},{"location":"guide/tracking/#basic-usage","title":"Basic Usage","text":"<p>Decorate any function whose return value should be governed. The decorator passes the return value through unchanged -- your application logic is not affected.</p> <pre><code>from provena import ContextTrail\n\ntrail = ContextTrail(backend=\"memory\")\n\n@trail.track(source=\"retriever\")\ndef search_docs(query: str) -&gt; str:\n    return \"OpenShift 4.16 supports single-node deployments for edge.\"\n\nresult = search_docs(\"edge deployment\")\nprint(result)  # \"OpenShift 4.16 supports single-node deployments for edge.\"\n</code></pre> <p>The decorator hashes the content, validates provenance, checks freshness, and appends a hash-chained record to storage -- all before returning the original value to the caller.</p>"},{"location":"guide/tracking/#tracking-list-returns","title":"Tracking List Returns","text":"<p>When a function returns a list, each item is logged as a separate record. This is the natural fit for retriever functions that return multiple documents.</p> <pre><code>@trail.track(source=\"retriever\")\ndef search(query: str) -&gt; list[str]:\n    return [\n        \"Minimum requirements: 8 vCPUs, 32 GB RAM.\",\n        \"Single-node deployments are supported for edge.\",\n    ]\n\nresults = search(\"requirements\")\n# Two separate records are created in the audit trail\nprint(trail.summary()[\"total\"])  # 2\n</code></pre>"},{"location":"guide/tracking/#tracking-dict-returns","title":"Tracking Dict Returns","text":"<p>Dictionary return values are JSON-serialized and logged as a single record.</p> <pre><code>@trail.track(source=\"tool:weather_api\")\ndef get_weather(city: str) -&gt; dict:\n    return {\"city\": city, \"temp_c\": 22, \"conditions\": \"partly cloudy\"}\n\nweather = get_weather(\"Toronto\")\n# Logged as: '{\"city\": \"Toronto\", \"temp_c\": 22, \"conditions\": \"partly cloudy\"}'\n</code></pre>"},{"location":"guide/tracking/#custom-content-extractor","title":"Custom Content Extractor","text":"<p>When a function returns a complex object, use <code>content_extractor</code> to tell Provena how to extract the loggable content.</p> <pre><code>from dataclasses import dataclass\n\n@dataclass\nclass SearchResult:\n    text: str\n    score: float\n    metadata: dict\n\n@trail.track(\n    source=\"retriever\",\n    content_extractor=lambda r: r.text,\n)\ndef ranked_search(query: str) -&gt; SearchResult:\n    return SearchResult(\n        text=\"Pod scheduling uses node affinity rules.\",\n        score=0.95,\n        metadata={\"source\": \"k8s-docs\"},\n    )\n\nresult = ranked_search(\"scheduling\")\n# Only result.text is logged to the trail\n</code></pre> <p>The extractor can also return a list to create multiple records from one call:</p> <pre><code>@trail.track(\n    source=\"retriever\",\n    content_extractor=lambda results: [r.text for r in results],\n)\ndef batch_search(query: str) -&gt; list[SearchResult]:\n    return [\n        SearchResult(text=\"First result\", score=0.9, metadata={}),\n        SearchResult(text=\"Second result\", score=0.8, metadata={}),\n    ]\n</code></pre>"},{"location":"guide/tracking/#async-function-support","title":"Async Function Support","text":"<p>The decorator works transparently with <code>async</code> functions. No additional configuration is needed.</p> <pre><code>import asyncio\n\n@trail.track(source=\"tool:api_name\")\nasync def fetch_data(url: str) -&gt; str:\n    # In a real application, use aiohttp or httpx here\n    await asyncio.sleep(0.01)\n    return \"Response payload from external API\"\n\nresult = asyncio.run(fetch_data(\"https://api.example.com/data\"))\n</code></pre>"},{"location":"guide/tracking/#source-types","title":"Source Types","text":"<p>The <code>source</code> parameter identifies where the context came from. Use the string format <code>\"type:name\"</code> for specificity:</p> Source String Use Case <code>\"retriever\"</code> Vector store or document retrieval <code>\"tool:api_name\"</code> External API tool calls <code>\"agent:planner\"</code> Output from a sub-agent <code>\"memory:long_term\"</code> Long-term memory retrieval <code>\"mcp:filesystem\"</code> Model Context Protocol server <code>\"custom\"</code> Anything that does not fit the above <p>The part before the colon maps to a <code>ContextSource</code> enum value. The part after the colon becomes the <code>source_name</code> in the audit record.</p> <pre><code>@trail.track(source=\"mcp:filesystem\")\ndef read_config(path: str) -&gt; str:\n    with open(path) as f:\n        return f.read()\n\n@trail.track(source=\"agent:planner\")\ndef plan_next_step(state: dict) -&gt; str:\n    return \"Retrieve the latest deployment manifest.\"\n</code></pre>"},{"location":"guide/tracking/#none-returns-are-skipped","title":"None Returns Are Skipped","text":"<p>If a tracked function returns <code>None</code>, no record is created. This prevents empty entries from cluttering the audit trail.</p> <pre><code>@trail.track(source=\"retriever\")\ndef maybe_search(query: str) -&gt; str | None:\n    if not query.strip():\n        return None  # No record logged\n    return \"Found a relevant document.\"\n\nmaybe_search(\"\")   # Nothing logged\nmaybe_search(\"k8s\")  # One record logged\n</code></pre>"},{"location":"guide/tracking/#langchain-document-support","title":"LangChain Document Support","text":"<p>Objects with a <code>.page_content</code> attribute (such as LangChain <code>Document</code> instances) are handled automatically. The <code>page_content</code> string is extracted and logged.</p> <pre><code>class Document:\n    \"\"\"Minimal LangChain-compatible document.\"\"\"\n    def __init__(self, page_content: str, metadata: dict | None = None):\n        self.page_content = page_content\n        self.metadata = metadata or {}\n\n@trail.track(source=\"retriever\")\ndef langchain_retriever(query: str) -&gt; list[Document]:\n    return [\n        Document(\n            page_content=\"Pod disruption budgets protect availability.\",\n            metadata={\"source\": \"https://k8s.io/docs/pdb\"},\n        ),\n    ]\n\ndocs = langchain_retriever(\"availability\")\n# Logged content: \"Pod disruption budgets protect availability.\"\n</code></pre> <p>Provenance auto-extraction</p> <p>When the return value has a <code>.metadata</code> dict with <code>source</code> or <code>source_url</code> keys, Provena automatically creates <code>ProvenanceMetadata</code> from it. See the Provenance Validation guide for details.</p>"},{"location":"guide/tracking/#manual-logging-with-traillog","title":"Manual Logging with trail.log()","text":"<p>When a decorator does not fit your workflow, use <code>trail.log()</code> directly:</p> <pre><code>from provena import ContextTrail, ProvenanceMetadata\nfrom datetime import datetime, timezone\n\ntrail = ContextTrail(backend=\"memory\")\n\n# Log a retrieval result with full provenance\nrecord = trail.log(\n    content=\"Service mesh reduces inter-service latency by 40%.\",\n    source=\"retriever\",\n    source_name=\"knowledge_base\",\n    provenance=ProvenanceMetadata(\n        source_url=\"https://docs.example.com/mesh\",\n        author=\"Platform Team\",\n        created_at=datetime(2025, 6, 1, tzinfo=timezone.utc),\n    ),\n)\n\nprint(record.chain_hash[:16])  # First 16 chars of the chain hash\n</code></pre> <p><code>trail.log()</code> returns a <code>TrailRecord</code> on success, or <code>None</code> if a non-strict error occurred.</p> <p>Choosing between track() and log()</p> <p>Use <code>@trail.track()</code> when you control the function definition and want zero-touch logging. Use <code>trail.log()</code> when you receive content from callbacks, event handlers, or third-party code where a decorator cannot be applied.</p>"},{"location":"guide/verification/","title":"Chain Verification","text":"<p>Provena maintains a SHA-256 hash chain across all audit records, creating a tamper-evident log. If any record is modified, deleted, or reordered after the fact, the chain breaks and verification fails. This gives you cryptographic proof that your governance trail has not been altered.</p>"},{"location":"guide/verification/#how-the-hash-chain-works","title":"How the Hash Chain Works","text":"<p>Each record's chain hash is computed from four inputs:</p> <ol> <li>The previous record's chain hash (linking it to its predecessor)</li> <li>The content hash (SHA-256 of the raw context bytes)</li> <li>The source type string</li> <li>The timestamp in ISO format</li> </ol> <p>The computation follows a Merkle-style pattern:</p> <pre><code>chain_hash = SHA-256(previous_hash : content_hash : source : timestamp)\n</code></pre> <p>The very first record uses a deterministic genesis hash as its predecessor:</p> <pre><code>import hashlib\nGENESIS_HASH = hashlib.sha256(b\"provena:genesis\").hexdigest()\n</code></pre> <p>This anchors the chain to a known starting point.</p>"},{"location":"guide/verification/#verifying-chain-integrity","title":"Verifying Chain Integrity","text":"<p>Call <code>trail.verify_chain()</code> to recompute every chain hash from the genesis forward and compare against stored values:</p> <pre><code>from provena import ContextTrail\n\ntrail = ContextTrail(backend=\"memory\")\n\n# Log some context\ntrail.log(content=\"First entry\", source=\"retriever\")\ntrail.log(content=\"Second entry\", source=\"tool:api\")\ntrail.log(content=\"Third entry\", source=\"agent:planner\")\n\n# Verify the entire chain\nverdict = trail.verify_chain()\nprint(verdict.intact)        # True\nprint(verdict.total_records)  # 3\nprint(verdict.broken_at)      # None\nprint(verdict.details)        # \"Chain intact\"\n</code></pre> <p>The <code>ChainVerdict</code> dataclass returned by <code>verify_chain()</code> contains:</p> Field Type Description <code>intact</code> <code>bool</code> <code>True</code> if every link in the chain is valid <code>total_records</code> <code>int</code> Number of records that were checked <code>broken_at</code> <code>int | None</code> Record ID where the chain first broke, or <code>None</code> <code>details</code> <code>str</code> Human-readable summary of the verification"},{"location":"guide/verification/#hmac-signing","title":"HMAC Signing","text":"<p>For stronger integrity guarantees, enable HMAC-SHA256 signing. When a signing key is provided, each chain hash is computed using HMAC instead of plain SHA-256. This means an attacker cannot forge valid chain hashes without the key.</p>"},{"location":"guide/verification/#using-the-signing_key-parameter","title":"Using the signing_key parameter","text":"<pre><code>trail = ContextTrail(\n    backend=\"memory\",\n    signing_key=\"my-secret-governance-key\",\n)\n\ntrail.log(content=\"Signed entry\", source=\"retriever\")\n\nverdict = trail.verify_chain()\nprint(verdict.intact)   # True\nprint(trail.is_signed)  # True\n</code></pre>"},{"location":"guide/verification/#using-the-provena_signing_key-environment-variable","title":"Using the PROVENA_SIGNING_KEY environment variable","text":"<pre><code>export PROVENA_SIGNING_KEY=\"my-secret-governance-key\"\n</code></pre> <pre><code>import os\nos.environ[\"PROVENA_SIGNING_KEY\"] = \"my-secret-governance-key\"\n\ntrail = ContextTrail(backend=\"memory\")\nprint(trail.is_signed)  # True\n</code></pre> <p>The environment variable is checked when no <code>signing_key</code> parameter is passed. The parameter takes precedence over the environment variable.</p> <p>Key management</p> <p>Store the signing key in a secrets manager or environment variable. Do not hard-code it in application source. Rotate keys by creating a new trail with the new key -- existing trails remain verifiable with their original key.</p>"},{"location":"guide/verification/#tamper-detection-example","title":"Tamper Detection Example","text":"<p>Here is what happens when stored data is modified outside of Provena:</p> <pre><code>from provena import ContextTrail\n\ntrail = ContextTrail(backend=\"memory\")\n\ntrail.log(content=\"Original entry one\", source=\"retriever\")\ntrail.log(content=\"Original entry two\", source=\"retriever\")\ntrail.log(content=\"Original entry three\", source=\"retriever\")\n\n# Verify before tampering\nverdict = trail.verify_chain()\nassert verdict.intact is True\n\n# Simulate tampering: modify the content_hash of the second record\nrecords = trail._backend._records  # Access internal storage for demo\nrecords[1][\"content_hash\"] = \"tampered_hash_value\"\n\n# Verify after tampering\nverdict = trail.verify_chain()\nprint(verdict.intact)        # False\nprint(verdict.broken_at)      # 2\nprint(verdict.details)        # \"Chain broken at record 2\"\n</code></pre> <p>The chain breaks at the tampered record because the recomputed hash no longer matches the stored value. Every record after the tampered one would also fail if checked individually, since each depends on its predecessor.</p>"},{"location":"guide/verification/#cli-verification","title":"CLI Verification","text":"<p>Verify a trail database from the command line:</p> <pre><code># Verify with default database path\nprovena verify\n\n# Verify a specific database\nprovena --db audit.db verify\n\n# Verify with a signing key\nprovena --db audit.db --signing-key \"my-secret-key\" verify\n\n# Using environment variables\nexport PROVENA_DB=audit.db\nexport PROVENA_SIGNING_KEY=\"my-secret-key\"\nprovena verify\n</code></pre> <p>Output on success:</p> <pre><code>PASS -- Chain intact (147 records verified)\n</code></pre> <p>Output on failure:</p> <pre><code>FAIL -- Chain broken at record 42\n</code></pre> <p>The CLI exits with code 0 on success and code 1 on failure, making it suitable for CI/CD pipelines and automated compliance checks.</p>"},{"location":"guide/verification/#performance","title":"Performance","text":"<p>Chain verification is designed to be fast:</p> <ul> <li>Hashing: SHA-256 computation adds sub-millisecond overhead per record</li> <li>HMAC signing: HMAC-SHA256 adds negligible additional cost over plain SHA-256</li> <li>Verification: Full chain verification reads all records sequentially; for   a trail with 10,000 records, expect verification to complete in under one   second</li> <li>Logging: Each <code>trail.log()</code> call performs one hash computation and one   database write -- the chain does not require re-reading previous records   because the previous hash is cached in memory</li> </ul> <p>Verification in CI/CD</p> <p>Add <code>provena --db $TRAIL_DB verify</code> as a step in your deployment pipeline. This ensures that the governance trail has not been corrupted before promoting a build. The non-zero exit code on failure integrates naturally with CI systems.</p>"},{"location":"integrations/cli/","title":"CLI Reference","text":"<p>Provena ships a command-line interface for querying, verifying, and reporting on your audit trail. Install the CLI extra to get started:</p> <pre><code>pip install provena[cli]\n</code></pre> <p>This installs <code>click&gt;=8.0</code> and <code>rich&gt;=13.0</code> as dependencies. Rich provides color-coded table output; if it is unavailable, a plain-text table is used as a fallback.</p>"},{"location":"integrations/cli/#global-options","title":"Global options","text":"<p>These options apply to all subcommands and must appear before the subcommand name:</p> <pre><code>provena [OPTIONS] COMMAND [ARGS]...\n</code></pre> Option Default Env var Description <code>--db PATH</code> <code>provena.db</code> <code>PROVENA_DB</code> Path to the Provena SQLite database file <code>--signing-key TEXT</code> (none) <code>PROVENA_SIGNING_KEY</code> HMAC signing key for hash chain verification <code>--version</code> Show the installed Provena version and exit <pre><code># Use a custom database path\nprovena --db /var/data/audit.db audit\n\n# Set the signing key via environment variable\nexport PROVENA_SIGNING_KEY=\"my-secret-key\"\nprovena verify\n\n# Check the installed version\nprovena --version\n</code></pre>"},{"location":"integrations/cli/#provena-audit","title":"<code>provena audit</code>","text":"<p>Query the context governance audit log with optional filters.</p> <pre><code>provena audit [OPTIONS]\n</code></pre>"},{"location":"integrations/cli/#options","title":"Options","text":"Option Short Default Description <code>--source TEXT</code> <code>-s</code> (all) Filter by source type (<code>retriever</code>, <code>tool</code>, <code>agent</code>, <code>memory</code>, <code>mcp</code>, <code>custom</code>) <code>--limit INT</code> <code>-n</code> <code>20</code> Maximum number of records to return <code>--from DATE</code> (none) Start date filter, format <code>YYYY-MM-DD</code> <code>--to DATE</code> (none) End date filter, format <code>YYYY-MM-DD</code> <code>--format TEXT</code> <code>table</code> Output format: <code>table</code> or <code>json</code>"},{"location":"integrations/cli/#table-output","title":"Table output","text":"<p>The default <code>table</code> format uses Rich to render a color-coded table:</p> <pre><code>provena audit --limit 5\n</code></pre> <pre><code>               Provena Audit Trail\n ID   Timestamp             Source    Name       Hash          Provenance   Freshness\n-----------------------------------------------------------------------------------------------\n  1   2025-07-15T10:23:01   retriever rag        a1b2c3d4e5f6 VALID        FRESH\n  2   2025-07-15T10:23:02   retriever rag        b2c3d4e5f6a7 VALID        FRESH\n  3   2025-07-15T10:23:03   tool      pricing    c3d4e5f6a7b8 MISSING      UNKNOWN\n  4   2025-07-15T10:23:04   agent     planner    d4e5f6a7b8c9 INCOMPLETE   STALE\n  5   2025-07-15T10:23:05   retriever rag        e5f6a7b8c9d0 VALID        FRESH\n</code></pre> <p>Color-coded verdicts</p> <p>In terminal output, verdicts are color-coded for quick scanning:</p> <ul> <li>Green: <code>VALID</code> provenance, <code>FRESH</code> freshness</li> <li>Red: <code>MISSING</code> provenance, <code>STALE</code> freshness</li> <li>Yellow: <code>INCOMPLETE</code> provenance</li> <li>Dim: <code>UNKNOWN</code> freshness</li> </ul>"},{"location":"integrations/cli/#json-output","title":"JSON output","text":"<pre><code>provena audit --format json --limit 2\n</code></pre> <pre><code>[\n  {\n    \"id\": 1,\n    \"timestamp\": \"2025-07-15T10:23:01.123456+00:00\",\n    \"source\": \"retriever\",\n    \"source_name\": \"rag\",\n    \"content_hash\": \"a1b2c3d4e5f6...\",\n    \"provenance_status\": \"VALID\",\n    \"freshness_status\": \"FRESH\",\n    \"chain_hash\": \"f6a7b8c9d0e1...\"\n  },\n  {\n    \"id\": 2,\n    \"timestamp\": \"2025-07-15T10:23:02.234567+00:00\",\n    \"source\": \"retriever\",\n    \"source_name\": \"rag\",\n    \"content_hash\": \"b2c3d4e5f6a7...\",\n    \"provenance_status\": \"VALID\",\n    \"freshness_status\": \"FRESH\",\n    \"chain_hash\": \"a7b8c9d0e1f2...\"\n  }\n]\n</code></pre>"},{"location":"integrations/cli/#filtering-examples","title":"Filtering examples","text":"<pre><code># Only retriever entries\nprovena audit --source retriever\n\n# Last 50 records\nprovena audit --limit 50\n\n# Records from a date range\nprovena audit --from 2025-07-01 --to 2025-07-15\n\n# Combine filters with JSON output\nprovena audit -s tool -n 10 --from 2025-07-01 --format json\n</code></pre>"},{"location":"integrations/cli/#provena-verify","title":"<code>provena verify</code>","text":"<p>Verify the integrity of the hash-chained audit trail. Recomputes every chain hash from the genesis hash forward and compares each against the stored value.</p> <pre><code>provena verify\n</code></pre>"},{"location":"integrations/cli/#options_1","title":"Options","text":"<p>This command has no subcommand-specific options. Use global options (<code>--db</code>, <code>--signing-key</code>) to target a specific database.</p>"},{"location":"integrations/cli/#output","title":"Output","text":"<p>Intact chain:</p> <pre><code>provena verify\n</code></pre> <pre><code>PASS -- Chain intact (47 records verified)\n</code></pre> <p>Tampered or corrupted chain:</p> <pre><code>provena verify --db corrupted.db\n</code></pre> <pre><code>FAIL -- Chain broken at record 23\n</code></pre> <p>Exit codes</p> <p><code>provena verify</code> exits with code 0 on <code>PASS</code> and code 1 on <code>FAIL</code>. Use this in CI pipelines to gate deployments on audit trail integrity:</p> <pre><code>provena verify --db production.db || echo \"Audit trail compromised!\"\n</code></pre> <p>Empty trail:</p> <pre><code>provena verify --db empty.db\n</code></pre> <pre><code>EMPTY -- No records in the audit trail.\n</code></pre>"},{"location":"integrations/cli/#provena-report","title":"<code>provena report</code>","text":"<p>Generate a context governance compliance report with chain integrity verification, provenance breakdowns, and freshness statistics.</p> <pre><code>provena report [OPTIONS]\n</code></pre>"},{"location":"integrations/cli/#options_2","title":"Options","text":"Option Short Default Description <code>--format TEXT</code> <code>json</code> Output format: <code>json</code>, <code>text</code>, or <code>csv</code> <code>--output PATH</code> <code>-o</code> (stdout) Write the report to a file instead of stdout"},{"location":"integrations/cli/#json-report","title":"JSON report","text":"<pre><code>provena report\n</code></pre> <pre><code>{\n  \"generated_at\": \"2025-07-15T14:30:00.123456+00:00\",\n  \"database\": \"provena.db\",\n  \"total_records\": 47,\n  \"chain_integrity\": {\n    \"status\": \"INTACT\",\n    \"records_verified\": 47,\n    \"broken_at\": null\n  },\n  \"provenance\": {\n    \"VALID\": 35,\n    \"MISSING\": 8,\n    \"INCOMPLETE\": 4\n  },\n  \"freshness\": {\n    \"FRESH\": 40,\n    \"STALE\": 5,\n    \"UNKNOWN\": 2\n  },\n  \"sources\": {\n    \"retriever\": 30,\n    \"tool\": 12,\n    \"agent\": 5\n  },\n  \"signed\": true\n}\n</code></pre>"},{"location":"integrations/cli/#text-report","title":"Text report","text":"<pre><code>provena report --format text\n</code></pre> <pre><code>==================================================\nPROVENA GOVERNANCE REPORT\n==================================================\nGenerated: 2025-07-15T14:30:00.123456+00:00\nDatabase:  provena.db\nRecords:   47\nSigned:    Yes\n\nChain Integrity:\n  Status:   INTACT\n  Verified: 47 records\n\nProvenance:\n  INCOMPLETE   4\n  MISSING      8\n  VALID        35\n\nFreshness:\n  FRESH        40\n  STALE        5\n  UNKNOWN      2\n\nSources:\n  agent        5\n  retriever    30\n  tool         12\n==================================================\n</code></pre>"},{"location":"integrations/cli/#csv-report","title":"CSV report","text":"<pre><code>provena report --format csv --output audit_export.csv\n</code></pre> <pre><code>Report written to audit_export.csv\n</code></pre> <p>The CSV file contains one row per trail record:</p> <pre><code>id,timestamp,source,source_name,content_hash,provenance_status,freshness_status,chain_hash\n1,2025-07-15T10:23:01.123456+00:00,retriever,rag,a1b2c3d4...,VALID,FRESH,f6a7b8c9...\n2,2025-07-15T10:23:02.234567+00:00,tool,pricing,b2c3d4e5...,MISSING,UNKNOWN,a7b8c9d0...\n</code></pre>"},{"location":"integrations/cli/#writing-to-a-file","title":"Writing to a file","text":"<pre><code># JSON report to file\nprovena report --output compliance_report.json\n\n# Text report to file\nprovena report --format text -o report.txt\n</code></pre>"},{"location":"integrations/cli/#provena-summary","title":"<code>provena summary</code>","text":"<p>Show a quick summary of the audit trail: record count, backend, signing status, and breakdowns by provenance, freshness, and source.</p> <pre><code>provena summary\n</code></pre>"},{"location":"integrations/cli/#options_3","title":"Options","text":"<p>This command has no subcommand-specific options. Use global options (<code>--db</code>, <code>--signing-key</code>) to target a specific database.</p>"},{"location":"integrations/cli/#output_1","title":"Output","text":"<pre><code>provena summary\n</code></pre> <pre><code>Records:    47\nBackend:    SQLiteBackend\nSigned:     Yes\n\nProvenance:\n  INCOMPLETE   4\n  MISSING      8\n  VALID        35\n\nFreshness:\n  FRESH        40\n  STALE        5\n  UNKNOWN      2\n\nSources:\n  agent        5\n  retriever    30\n  tool         12\n</code></pre> <p>Empty trail:</p> <pre><code>provena --db empty.db summary\n</code></pre> <pre><code>Records:    0\nBackend:    SQLiteBackend\nSigned:     No\n</code></pre>"},{"location":"integrations/cli/#environment-variables","title":"Environment variables","text":"Variable Maps to Description <code>PROVENA_DB</code> <code>--db</code> Default database path <code>PROVENA_SIGNING_KEY</code> <code>--signing-key</code> HMAC signing key for chain verification <p>Environment variables are overridden by explicit command-line arguments:</p> <pre><code># These are equivalent:\nexport PROVENA_DB=/var/data/audit.db\nprovena audit\n\nprovena --db /var/data/audit.db audit\n</code></pre>"},{"location":"integrations/cli/#typical-cicd-workflow","title":"Typical CI/CD workflow","text":"<pre><code>#!/bin/bash\nset -e\n\n# Verify the audit trail is intact\nprovena --db production.db verify\n\n# Generate a compliance report\nprovena --db production.db report --format json --output report.json\n\n# Quick summary for the build log\nprovena --db production.db summary\n</code></pre>"},{"location":"integrations/langchain/","title":"LangChain","text":"<p>Provena integrates with LangChain through a callback handler that automatically logs retriever results and tool outputs to your audit trail.</p>"},{"location":"integrations/langchain/#installation","title":"Installation","text":"<pre><code>pip install provena[langchain]\n</code></pre> <p>This installs <code>langchain-core&gt;=0.2</code> as a dependency. If you already have LangChain installed, the extra ensures version compatibility.</p>"},{"location":"integrations/langchain/#quick-start","title":"Quick start","text":"<pre><code>from provena import ContextTrail\nfrom provena.integrations.langchain import ProvenaCallback\n\ntrail = ContextTrail()\ncallback = ProvenaCallback(trail=trail)\n\n# Attach to any LangChain chain\nchain = RetrievalQA.from_chain_type(\n    llm=llm,\n    retriever=retriever,\n    callbacks=[callback],\n)\n</code></pre>"},{"location":"integrations/langchain/#how-it-works","title":"How it works","text":"<p><code>ProvenaCallback</code> extends LangChain's <code>BaseCallbackHandler</code> and hooks into two lifecycle events:</p>"},{"location":"integrations/langchain/#on_retriever_end-retrieved-documents","title":"<code>on_retriever_end</code> -- retrieved documents","text":"<p>When a retriever returns documents, the callback logs each document's <code>page_content</code> as a separate trail entry with source type <code>RETRIEVER</code>:</p> <pre><code>def on_retriever_end(self, documents, *, run_id, **kwargs):\n    for doc in documents:\n        content = doc.page_content      # (1)!\n        provenance = _extract_langchain_provenance(doc)  # (2)!\n        self._trail.log(\n            content=content,\n            source=ContextSource.RETRIEVER,\n            source_name=\"langchain\",\n            provenance=provenance,\n            metadata={\"run_id\": str(run_id)},\n        )\n</code></pre> <ol> <li>Each document's <code>page_content</code> attribute is captured as the logged content.</li> <li>Provenance metadata is auto-extracted from <code>doc.metadata</code> (see below).</li> </ol>"},{"location":"integrations/langchain/#on_tool_end-tool-outputs","title":"<code>on_tool_end</code> -- tool outputs","text":"<p>When a tool completes, the callback logs its output as a trail entry with source type <code>TOOL</code>:</p> <pre><code>def on_tool_end(self, output, *, run_id, **kwargs):\n    content = str(output)\n    self._trail.log(\n        content=content,\n        source=ContextSource.TOOL,\n        source_name=\"langchain\",\n        metadata={\"run_id\": str(run_id)},\n    )\n</code></pre> <p>Run ID tracking</p> <p>Both hooks capture the LangChain <code>run_id</code> in the record's metadata, letting you correlate audit trail entries back to specific chain executions.</p>"},{"location":"integrations/langchain/#provenance-auto-extraction","title":"Provenance auto-extraction","text":"<p>Provena automatically extracts provenance metadata from each document's <code>metadata</code> dictionary:</p> Document metadata key Provena field Notes <code>source</code> <code>source_url</code> Checked first <code>source_url</code> <code>source_url</code> Fallback if <code>source</code> is absent <code>author</code> <code>author</code> Mapped directly <pre><code># A document with this metadata:\ndoc = Document(\n    page_content=\"...\",\n    metadata={\n        \"source\": \"https://docs.example.com/api.html\",\n        \"author\": \"Engineering Team\",\n    }\n)\n\n# Produces this ProvenanceMetadata:\n# ProvenanceMetadata(\n#     source_url=\"https://docs.example.com/api.html\",\n#     author=\"Engineering Team\",\n# )\n</code></pre> <p>Provenance validation</p> <p>The auto-extraction sets <code>source_url</code> and <code>author</code> but not <code>created_at</code>. Since the default required fields are <code>source_url</code> and <code>created_at</code>, records will be marked <code>INCOMPLETE</code> (missing <code>created_at</code>). To get <code>VALID</code> status, either set <code>created_at</code> in your document metadata or configure <code>required_fields=[\"source_url\"]</code> on the trail. Documents without any origin metadata will be marked <code>MISSING</code>.</p>"},{"location":"integrations/langchain/#full-working-example","title":"Full working example","text":"<pre><code>from langchain_community.document_loaders import TextLoader\nfrom langchain_community.vectorstores import FAISS\nfrom langchain_openai import ChatOpenAI, OpenAIEmbeddings\nfrom langchain.chains import RetrievalQA\n\nfrom provena import ContextTrail\nfrom provena.integrations.langchain import ProvenaCallback\n\n# --- Set up a retrieval chain ---\nloader = TextLoader(\"knowledge_base.txt\")\ndocs = loader.load()\nvectorstore = FAISS.from_documents(docs, OpenAIEmbeddings())\nretriever = vectorstore.as_retriever(search_kwargs={\"k\": 3})\n\nllm = ChatOpenAI(model=\"gpt-4o\")\n\n# --- Create the Provena trail and callback ---\ntrail = ContextTrail(storage_path=\"rag_audit.db\")\ncallback = ProvenaCallback(trail=trail)\n\n# --- Run the chain with governance ---\nchain = RetrievalQA.from_chain_type(\n    llm=llm,\n    retriever=retriever,\n    callbacks=[callback],\n)\n\nanswer = chain.invoke({\"query\": \"What is the refund policy?\"})\nprint(answer[\"result\"])\n</code></pre>"},{"location":"integrations/langchain/#verifying-the-trail","title":"Verifying the trail","text":"<p>After chain execution, inspect and verify the audit trail:</p> <pre><code># Check what was logged\nsummary = trail.summary()\nprint(f\"Total records: {summary['total']}\")\nprint(f\"Sources: {summary['sources']}\")\nprint(f\"Provenance: {summary['provenance']}\")\n\n# Verify hash chain integrity\nverdict = trail.verify_chain()\nassert verdict.intact, f\"Chain broken: {verdict.details}\"\nprint(f\"Chain intact: {verdict.total_records} records verified\")\n\n# Query specific records\nrecords = trail.query(source=\"retriever\", limit=10)\nfor r in records:\n    print(f\"  [{r['provenance_status']}] {r['source_name']} - {r['content_hash'][:12]}\")\n\ntrail.close()\n</code></pre> <p>Expected output:</p> <pre><code>Total records: 3\nSources: {'retriever': 3}\nProvenance: {'INCOMPLETE': 3}\nChain intact: 3 records verified\n  [INCOMPLETE] langchain - a1b2c3d4e5f6\n  [INCOMPLETE] langchain - b2c3d4e5f6a7\n  [INCOMPLETE] langchain - c3d4e5f6a7b8\n</code></pre>"},{"location":"integrations/langchain/#using-with-other-chain-types","title":"Using with other chain types","text":"<p>The callback works with any LangChain component that fires retriever or tool events:</p> <pre><code># With an agent that uses tools\nfrom langchain.agents import AgentExecutor\n\nagent_executor = AgentExecutor(\n    agent=agent,\n    tools=tools,\n    callbacks=[ProvenaCallback(trail=trail)],\n)\n\n# Tool calls are automatically logged as TOOL source entries\nresult = agent_executor.invoke({\"input\": \"Look up the current price of ACME stock\"})\n</code></pre>"},{"location":"integrations/langchain/#context-manager-pattern","title":"Context manager pattern","text":"<p>Use the trail as a context manager so the database is always closed:</p> <pre><code>with ContextTrail(storage_path=\"audit.db\") as trail:\n    callback = ProvenaCallback(trail=trail)\n    chain = RetrievalQA.from_chain_type(\n        llm=llm,\n        retriever=retriever,\n        callbacks=[callback],\n    )\n    chain.invoke({\"query\": \"Summarize the Q3 earnings report\"})\n\n    verdict = trail.verify_chain()\n    print(f\"Chain intact: {verdict.intact}\")\n# trail.close() is called automatically\n</code></pre>"},{"location":"integrations/llamaindex/","title":"LlamaIndex","text":"<p>Provena integrates with LlamaIndex through a node postprocessor that logs every retrieved node to your audit trail without altering query results.</p>"},{"location":"integrations/llamaindex/#installation","title":"Installation","text":"<pre><code>pip install provena[llamaindex]\n</code></pre> <p>This installs <code>llama-index-core&gt;=0.10</code> as a dependency.</p>"},{"location":"integrations/llamaindex/#quick-start","title":"Quick start","text":"<pre><code>from provena import ContextTrail\nfrom provena.integrations.llamaindex import ProvenaPostprocessor\n\ntrail = ContextTrail()\npostprocessor = ProvenaPostprocessor(trail=trail)\n\nquery_engine = index.as_query_engine(\n    node_postprocessors=[postprocessor],\n)\n</code></pre>"},{"location":"integrations/llamaindex/#how-it-works","title":"How it works","text":"<p><code>ProvenaPostprocessor</code> extends LlamaIndex's <code>BaseNodePostprocessor</code>. It implements the <code>_postprocess_nodes</code> method, which is called after retrieval and before the response synthesizer.</p>"},{"location":"integrations/llamaindex/#pass-through-behavior","title":"Pass-through behavior","text":"<p>The postprocessor returns nodes unchanged. It observes and logs each node but never modifies content, scores, or ordering. Your query results are identical whether Provena is attached or not.</p> <pre><code>def _postprocess_nodes(self, nodes, query_bundle=None):\n    for node_with_score in nodes:\n        node = node_with_score.node\n        content = node.text                               # (1)!\n        provenance = _extract_llamaindex_provenance(node)  # (2)!\n\n        metadata = {}\n        if node_with_score.score is not None:\n            metadata[\"score\"] = node_with_score.score      # (3)!\n        if query_bundle:\n            metadata[\"query\"] = query_bundle.query_str     # (4)!\n\n        self.trail.log(\n            content=content,\n            source=ContextSource.RETRIEVER,\n            source_name=\"llamaindex\",\n            provenance=provenance,\n            metadata=metadata,\n        )\n    return nodes  # unchanged\n</code></pre> <ol> <li>Each node's <code>text</code> attribute is captured as the logged content.</li> <li>Provenance metadata is auto-extracted from <code>node.metadata</code> (see below).</li> <li>The retrieval similarity score is stored in the record's metadata.</li> <li>The original query string is also captured when available.</li> </ol> <p>Score and query tracking</p> <p>Each trail record's metadata includes the retrieval <code>score</code> (float) and the <code>query</code> string that triggered the retrieval. This lets you audit not just what was retrieved but why and how relevant it was.</p>"},{"location":"integrations/llamaindex/#provenance-auto-extraction","title":"Provenance auto-extraction","text":"<p>Provena automatically extracts provenance metadata from each node's <code>metadata</code> dictionary:</p> Node metadata key Provena field Notes <code>source</code> <code>source_url</code> Checked first <code>file_path</code> <code>source_url</code> Fallback if <code>source</code> is absent <code>author</code> <code>author</code> Mapped directly <pre><code># A node with this metadata:\nnode = TextNode(\n    text=\"...\",\n    metadata={\n        \"file_path\": \"/data/contracts/agreement_v2.pdf\",\n        \"author\": \"Legal Department\",\n    },\n)\n\n# Produces this ProvenanceMetadata:\n# ProvenanceMetadata(\n#     source_url=\"/data/contracts/agreement_v2.pdf\",\n#     author=\"Legal Department\",\n# )\n</code></pre> <p>Provenance validation</p> <p>Nodes that include <code>source</code>, <code>file_path</code>, or <code>author</code> in their metadata will receive <code>VALID</code> or <code>INCOMPLETE</code> provenance status. Nodes with no origin metadata are marked <code>MISSING</code>, making gaps visible in your compliance reports.</p>"},{"location":"integrations/llamaindex/#full-working-example","title":"Full working example","text":"<pre><code>from llama_index.core import VectorStoreIndex, SimpleDirectoryReader\nfrom llama_index.llms.openai import OpenAI\n\nfrom provena import ContextTrail\nfrom provena.integrations.llamaindex import ProvenaPostprocessor\n\n# --- Load and index documents ---\ndocuments = SimpleDirectoryReader(\"./data\").load_data()\nindex = VectorStoreIndex.from_documents(documents)\n\n# --- Create the Provena trail and postprocessor ---\ntrail = ContextTrail(storage_path=\"rag_audit.db\")\npostprocessor = ProvenaPostprocessor(trail=trail)\n\n# --- Build the query engine with governance ---\nquery_engine = index.as_query_engine(\n    llm=OpenAI(model=\"gpt-4o\"),\n    similarity_top_k=3,\n    node_postprocessors=[postprocessor],\n)\n\n# --- Run a query ---\nresponse = query_engine.query(\"What are the key terms of the agreement?\")\nprint(response)\n</code></pre>"},{"location":"integrations/llamaindex/#verifying-the-trail","title":"Verifying the trail","text":"<p>After running a query, inspect and verify the audit trail:</p> <pre><code># Check what was logged\nsummary = trail.summary()\nprint(f\"Total records: {summary['total']}\")\nprint(f\"Sources: {summary['sources']}\")\nprint(f\"Provenance: {summary['provenance']}\")\n\n# Verify hash chain integrity\nverdict = trail.verify_chain()\nassert verdict.intact, f\"Chain broken: {verdict.details}\"\nprint(f\"Chain intact: {verdict.total_records} records verified\")\n\n# Inspect individual records with scores\nrecords = trail.query(source=\"retriever\", limit=10)\nfor r in records:\n    meta = r.get(\"metadata_json\", \"{}\")\n    print(\n        f\"  [{r['provenance_status']}] {r['source_name']}\"\n        f\" - score={r.get('metadata', {}).get('score', 'N/A')}\"\n    )\n\ntrail.close()\n</code></pre> <p>Expected output:</p> <pre><code>Total records: 3\nSources: {'retriever': 3}\nProvenance: {'VALID': 2, 'MISSING': 1}\nChain intact: 3 records verified\n  [VALID] llamaindex - score=0.92\n  [VALID] llamaindex - score=0.87\n  [MISSING] llamaindex - score=0.81\n</code></pre>"},{"location":"integrations/llamaindex/#combining-with-other-postprocessors","title":"Combining with other postprocessors","text":"<p><code>ProvenaPostprocessor</code> can be stacked with other LlamaIndex postprocessors. Place it last in the list so it logs the final set of nodes that reach the response synthesizer:</p> <pre><code>from llama_index.core.postprocessor import SimilarityPostprocessor\n\nquery_engine = index.as_query_engine(\n    node_postprocessors=[\n        SimilarityPostprocessor(similarity_cutoff=0.7),  # filter first\n        ProvenaPostprocessor(trail=trail),                # then log\n    ],\n)\n</code></pre>"},{"location":"integrations/llamaindex/#context-manager-pattern","title":"Context manager pattern","text":"<p>Use the trail as a context manager so the database is always closed:</p> <pre><code>with ContextTrail(storage_path=\"audit.db\") as trail:\n    postprocessor = ProvenaPostprocessor(trail=trail)\n    query_engine = index.as_query_engine(\n        node_postprocessors=[postprocessor],\n    )\n\n    response = query_engine.query(\"Summarize the compliance requirements\")\n\n    verdict = trail.verify_chain()\n    print(f\"Chain intact: {verdict.intact}\")\n# trail.close() is called automatically\n</code></pre>"},{"location":"integrations/opentelemetry/","title":"OpenTelemetry","text":"<p>Provena can emit OpenTelemetry spans for every context governance event, letting you visualize and monitor your audit trail alongside the rest of your application's distributed traces.</p>"},{"location":"integrations/opentelemetry/#installation","title":"Installation","text":"<pre><code>pip install provena[otel]\n</code></pre> <p>This installs <code>opentelemetry-api&gt;=1.20</code> as a dependency. You also need an OpenTelemetry SDK and exporter for your backend (Jaeger, OTLP, etc.).</p>"},{"location":"integrations/opentelemetry/#enabling-opentelemetry","title":"Enabling OpenTelemetry","text":""},{"location":"integrations/opentelemetry/#constructor-parameters","title":"Constructor parameters","text":"<pre><code>from provena import ContextTrail\n\ntrail = ContextTrail(\n    otel_enabled=True,\n    otel_service_name=\"my-agent\",\n)\n</code></pre>"},{"location":"integrations/opentelemetry/#config-dict","title":"Config dict","text":"<pre><code>trail = ContextTrail(\n    config={\n        \"storage\": {\"backend\": \"sqlite\", \"path\": \"audit.db\"},\n        \"otel\": {\n            \"enabled\": True,\n            \"service_name\": \"my-agent\",\n        },\n    }\n)\n</code></pre>"},{"location":"integrations/opentelemetry/#span-format","title":"Span format","text":"<p>Each call to <code>trail.log()</code> (or each entry logged by an integration callback) emits a single span with the following name pattern:</p> <pre><code>provena.track.&lt;source_name&gt;\n</code></pre> <p>For example, a retriever source named <code>\"rag\"</code> produces the span name <code>provena.track.rag</code>. A tool source named <code>\"pricing_api\"</code> produces <code>provena.track.pricing_api</code>.</p>"},{"location":"integrations/opentelemetry/#span-attributes","title":"Span attributes","text":"<p>Every span includes these attributes:</p> Attribute Type Description <code>provena.source</code> <code>string</code> Source type enum value (<code>retriever</code>, <code>tool</code>, <code>agent</code>, etc.) <code>provena.source_name</code> <code>string</code> Human-readable source name <code>provena.content_hash</code> <code>string</code> SHA-256 hex digest of the content <code>provena.chain_hash</code> <code>string</code> This record's hash chain position <code>provena.timestamp</code> <code>string</code> ISO 8601 timestamp of when the entry was recorded <code>provena.content_type</code> <code>string</code> Content type (<code>str</code>, <code>bytes</code>, or <code>json</code>) <code>provena.truncated</code> <code>bool</code> Whether the content was truncated to fit size limits <p>These attributes are added conditionally when validation results are available:</p> Attribute Type Description <code>provena.provenance_status</code> <code>string</code> <code>VALID</code>, <code>MISSING</code>, or <code>INCOMPLETE</code> <code>provena.freshness_status</code> <code>string</code> <code>FRESH</code>, <code>STALE</code>, or <code>UNKNOWN</code>"},{"location":"integrations/opentelemetry/#example-with-jaegerotlp-exporter","title":"Example with Jaeger/OTLP exporter","text":"<p>A complete setup that sends Provena spans to a Jaeger instance via OTLP:</p> <pre><code>from opentelemetry import trace\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import BatchSpanProcessor\nfrom opentelemetry.sdk.resources import Resource\nfrom opentelemetry.exporter.otlp.proto.grpc.trace_exporter import (\n    OTLPSpanExporter,\n)\n\nfrom provena import ContextTrail\n\n# --- Configure the OTel SDK ---\nresource = Resource.create({\"service.name\": \"my-agent\"})\nprovider = TracerProvider(resource=resource)\notlp_exporter = OTLPSpanExporter(\n    endpoint=\"http://localhost:4317\",  # Jaeger OTLP gRPC endpoint\n    insecure=True,\n)\nprovider.add_span_processor(BatchSpanProcessor(otlp_exporter))\ntrace.set_tracer_provider(provider)\n\n# --- Create the trail with OTel enabled ---\ntrail = ContextTrail(\n    storage_path=\"audit.db\",\n    otel_enabled=True,\n    otel_service_name=\"my-agent\",\n)\n\n# --- Log context entries (spans are emitted automatically) ---\ntrail.log(\"retrieved document content\", source=\"retriever\", source_name=\"rag\")\ntrail.log(\"tool API response\", source=\"tool:pricing_api\")\ntrail.log(\"agent memory recall\", source=\"agent:planner\")\n\n# --- Flush and clean up ---\ntrail.close()\nprovider.shutdown()\n</code></pre> <p>After running the above, you can view the spans in Jaeger at <code>http://localhost:16686</code>, searching for service <code>my-agent</code>.</p> <p>In-memory testing</p> <p>For unit tests, use <code>InMemorySpanExporter</code> from the OTel SDK to capture and assert on emitted spans without a running collector:</p> <pre><code>from opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import SimpleSpanProcessor\nfrom opentelemetry.sdk.trace.export.in_memory_span_exporter import (\n    InMemorySpanExporter,\n)\nfrom provena.exporters.otel import OTelExporter\n\nmem = InMemorySpanExporter()\nprovider = TracerProvider()\nprovider.add_span_processor(SimpleSpanProcessor(mem))\ntracer = provider.get_tracer(\"provena-test\")\n\nexporter = OTelExporter(enabled=True, tracer=tracer)\n# ... emit records, then assert:\nspans = mem.get_finished_spans()\nassert len(spans) == 1\nassert spans[0].name == \"provena.track.rag\"\n</code></pre>"},{"location":"integrations/opentelemetry/#non-fatal-error-handling","title":"Non-fatal error handling","text":"<p>OTel errors never crash the trail. If the OpenTelemetry SDK raises an exception during span creation or export, Provena catches it silently and continues logging to the audit database:</p> <pre><code>trail = ContextTrail(backend=\"memory\", otel_enabled=True)\n\n# Even if OTel is misconfigured or the collector is down,\n# trail.log() still succeeds and returns a valid record:\nrecord = trail.log(\"important context\", source=\"retriever\")\nassert record is not None\nassert trail.summary()[\"total\"] == 1\n</code></pre> <p>Silent failures</p> <p>Because OTel errors are suppressed, check your OTel collector logs if spans are not appearing. Provena logs a debug-level message via the <code>provena</code> logger when an OTel emit fails.</p>"},{"location":"integrations/opentelemetry/#combining-with-framework-integrations","title":"Combining with framework integrations","text":"<p>OTel works alongside the LangChain and LlamaIndex integrations. Enable OTel on the trail, and every entry logged by <code>ProvenaCallback</code> or <code>ProvenaPostprocessor</code> will also emit a span:</p> <pre><code>from provena import ContextTrail\nfrom provena.integrations.langchain import ProvenaCallback\n\ntrail = ContextTrail(\n    storage_path=\"audit.db\",\n    otel_enabled=True,\n    otel_service_name=\"rag-pipeline\",\n)\n\ncallback = ProvenaCallback(trail=trail)\n\n# Every document retrieved by LangChain will:\n# 1. Be logged to the SQLite audit trail\n# 2. Emit an OTel span named \"provena.track.langchain\"\nchain = RetrievalQA.from_chain_type(\n    llm=llm,\n    retriever=retriever,\n    callbacks=[callback],\n)\nchain.invoke({\"query\": \"What are our SLA commitments?\"})\n\ntrail.close()\n</code></pre>"},{"location":"integrations/opentelemetry/#otel-without-the-extra","title":"OTel without the extra","text":"<p>If <code>opentelemetry-api</code> is not installed, the <code>OTelExporter</code> internally marks itself as disabled and all <code>emit()</code> calls become no-ops. You can safely set <code>otel_enabled=True</code> in environments where OTel is not available --- no <code>ImportError</code> will be raised at runtime.</p>"}]}